# EXP0260 active checkpoint

Source codex/exp-0260-w4f16-decode-active-row-layout HEAD1ab2916; native20049ac, ABI122 provisional within this experiment. R=/mnt/d/llm_exp/results/qwen3-block-htp/exp0260. All1276 C64 files and remote files.sha256 reverified, manifest7de4f0758d83f2ba3b58696c695bfbfed72a25dd3bf308475abee0a0f0575a89. No mllm. Parent4fad27d.

QBH_W4F16_DECODE_OPT0 old,1 batched exact FP16/FP32 Softmax conversion with same scalar expf/sum/division/half rounding,2 same plus HVX probabilityzero and complete attention-to-O copy. Only W4F16 M1 dispatch changes. No active-row HMX/projection math change. Protocol title includes active-row scope, actual implemented first candidate is conversion plus exact layout movement. OPT2 selected as the complete bounded candidate before any paired timing; OPT1 is untimed diagnostic intermediate. Others frozen.

Initial global numerical audit switch rejected by existing Host argument policy before candidate compute; retained layer_batch failure. Added separate QBH_W4F16_DECODE_AUDIT. Real probabilities/live hidden/fullKV exact. Independent exhaustive63488finite half and46080probability-midpoint neighbours found ONLY negativezero lost by V79vcvt; fix explicitly preserves sign. Failed layer_vector_audit and layer_sentinel_diagnostic retained; final layer_vector_exact passes allsentinels plus oldsoftmax allphysicalrows. Raw29files/audit live output and eachstepKV identical to layer_control; finalruntime pointer l1_attempt5 (verify current file).

Single diagnostic olddecode3.55ms, conversion3.19ms, fullvector2.40ms; attention1.187ms->.163ms. Oldattentionunattributed.897ms mostly bulkcopy now.045ms. These are unpaired diagnostics, not formalstats. Current code/weights/math frozen before timing. Next profile_exp0260.py numerical then short/formal --count1 (five/ten rotated pairs repeats1/10). If gate passes build3/stage3 and slice_a0/a2 --dump audit, comparecomplete hidden/KV; then build28/stage28 full_control/full_candidate smoke and full short/formal. Full runs use frozenC64 token sequence and evaluation replay to support repeat10. Prove oldEXP0230 generation sequence andselectedFP16codes, then alltimed exact. Complete profiles, evidence seal andmemoryclosure. No newPPL or baselinepromotion.
