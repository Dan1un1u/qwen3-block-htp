# A3 static review

Diagnostic format mask4 is exclusive with modular interface bits1/2. Currentmask0 unchanged, prefill unchanged. Shared header layout/ABI134 unchanged; validation expands only to the explicit diagnostic flag, existing nonzero-control nativeSP2/FP32/no-rotation restrictions retained.

Decode SwiGLU low writes first128bytes of each2048byte tile. Independent high writes first128bytes of each2048byte tile in existing buffers->sp2_high; copack high remains middle+128. Qwen high aliases hmx_activation, dead after attention/O, GateUp input in q arena, Down input separate middle, raw scratch normalized, residual and cache disjoint. high span64*6144 fits hmx_activation and alignment2048 retained. High producer uses same vector LUT/pack and readiness, no extra conversion. Invalid padded rows never consumed by epilogue (fp32_rows1), as current copack padded rows; HMX rows independent.

Each Down tile repeats identical U8xW4 accumulation for high only in splitdecode, uses existing raw high scratch, same exact signed32 reconstruction and FP32 epilogue. RawS24 and reconstructedS32 proofs unchanged because all U8 values already bounded. Weights/bias reused in same residenttile, same doublebuffer and descriptorbytes. Existing hmx_u8s8_tile_pair_count includes extra Down pass; hmx_command_count remains worker submissions, explicitly not physical matrix passes. Expected perdecodedlayer difference ktiles192*ntiles64=12288 physical tilepairs, no weightbyte increase. Two arms selected and full oracle gates required before timing; static proof alone is not hardware verification.
