# EXP0277 static fairness and numerical scope

Frozen no-rotation native W4/FP32 residual2, ordinary A8 versus SP2. Restored only original EXP0257 middle qparams and SwiGLU LUT; immutable shared weights/scales and all other parameters. Fresh independent ordinary fixtures preserve original FP32 residual order. Ordinary uses its middle zero/scale and single raw Down pass, not SP2 compensation. FP32 raw scratch allocation now legal with SP2 disabled; pool high plane disabled for ordinary. Both arms use dual pipelined LUT gather, Gate/Up readiness, Down prefill streaming, raw-output overlap, and intrinsic DMA/HMX double buffering. Decode ordinary one stream versus SP2 physical-row copack, both identical tile/weight work. No forced format conversions or tensor DDR/spill.

Independent layer14 and consecutive3 exact checks, repeat10 agreement. Full28 physical audit plus actual full-model residual -> independent finalNorm/full-vocabulary head. This is NOT a newly computed full28 CPU transformer equivalence oracle. SP2 frontend matches sealed EXP0276; A8 own full-model frontend audit frozen before timing. Both fixed and actual greedy paths reproduce their frozen selected token/logit codes throughout every timed round. Changed activations need not produce same text. No PPL/quality acceptance.

Mode3 introduces explicit forced input-token replay with unchanged full greedy-head computation, omitting evaluation NLL work (mode1 computes NLL). This matches same work as actual greedy mode2 while controlling token input sequence; KV tensors still differ because arithmetic differs. Fixed and greedy rounds reported separately. ABI layout unchanged134. All native binaries TRACE=OFF; incomplete historical FARF traces are not reused as utilization evidence. Models and runtime binaries separately hashed; new metadata regular files and unchanged immutable device symlinks verified. No baseline promotion.
