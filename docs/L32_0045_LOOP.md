# L32-0045 loop
Frozen L32-0044 opt2c-l28/head32/AVrows4 control, same payloads/reference.
Candidate A: 3B Q/K/V native W4 batch8 instead of batch4. Reuse existing 1.5MiB weight slots, existing bias slots sized8. Identical arithmetic/bytes; reduce DMA/HMX submissions, test full28 rather than extrapolation. Preserve1B compile branch. No performance stopping threshold.

Candidate B: QKV batch32; phase-dead Gate buffer stores two bias slots (2*32*256 bytes), existing expanded weight buffers each1.5MiB. Reuse whole-weight DMA contract already used by Gate/Up. Guard3B, unchanged1B. Test identical HMX dot products and complete E2E; avoid adding storage or overlapping live RoPE data.

Candidate C: retain batch32; start first Q weight+bias DMA before FP32 input Norm and join before auditing/consuming. On final Q/K HMX batch, DMA first K/V batch into the alternate slot; carry exact slot ownership to next projection. Same bytes/math/buffers, no persistent intermediate DDR. All outstanding DMA joins before error return or slot reuse. Full reference and matched E2E required.

Second bounded batch, candidate D: retain C; prefetch first Down8 weight/bias batch into alternate weight slot during last Up HMX, before SwiGLU join. Down consumes that recorded slot without repeat DMA. Scope3B unrotated FP32 SP2mode8 only; do not consume middle before producers join. Identical bytes/arithmetic/arena. Compare three fixed paired repeat10 E2E rounds against frozen C, then formal best versus original0044.
