# Conditional AWQ-style per-channel recovery draft

Prepared under PC051 while EXP231 independent scoring runs. Not an active experiment or implementation. Activate only after EXP231 closes insufficient; otherwise stop escalation.

Use the stronger C64 per-channel candidate as direct control. Single E64 candidate: fresh original weights, original-coordinate signed[-7,7], oneFP32scale/outputrow, unchanged CPU GPTQ actorder/damping/three output-aware ranges/true-sequential65536calibration tokens. This avoids confusing a new coordinate transform with reverting to the inferior8K calibration budget. Original embedding/final norm and inherited W4 head frozen; other recipes/runtime frozen.

AWQ-style positive diagonal input-channel scaling, official mit-han-lab/llm-awq auto_scale.py method reference (pin commit/files before implementation). Fixed20ratios0,.05,...,.95, mean absolute input activation to that power, clamp1e-4 and geometric min/max normalization. Choose local activation-output MSE using symmetric per-channel RTN proxy, no new clipping search; export with the actual unchanged GPTQ quantizer. Report proxy versus exported result distinctly. Derive scales on fixed first64C64 calibration windows (the nestedC8 token subset) and frozen C64 quantized calibration hidden trajectories; no final/validation text. No model/checkpoint candidate search. Each layer uses three legal compensation families: input norm gamma /D1 with QKV columns *D1; post norm gamma /D2 with Gate/Up columns *D2; Up output rows /D3 with Down columns *D3. Last compensation acts on the linear Up branch and never commutes through SiLU. Skip V/O scaling for GQA, as in official reference shape guard. Pin exact objective/input capture in the executable experiment before searching.

Require FP64 synthetic algebra oracles, independent NumPy RTN/scale checks, all real-layer FP16 equivalence and whole-model FP16 invariance at the existing numerical tolerances before quantized export, fresh original shard hashes and exact frozen-head/embedding/finalnorm identity. Separate coordinate/rounding failures from quality failure; no threshold changes. Export coordinate metadata, row scales/codes, norm tensors, all56or28appropriate checkpoints and source commands. Software-only diagnostic first; no device performance claims from dequantized forward.

Reuse prior128document development as descriptive only, new primary512/reserve512 documents (exclude all prior including EXP230/231 roles) frozen before scoring. F/C8/C64/E64 canonical FP16 software, matched masks/context and repeat/causal/CE checks. Overall5%/everystratum10%, paired bootstrap5000seed232; reserve allcontrols only if E64 interval crosses any gate, maximum16384targets. Final outcome closes the approved three-step sequence; no further optimization or baseline promotion without discussion. DSP deployment/measurement of a successful software candidate is a separate next decision.

Primary references checked2026-09-06: https://arxiv.org/abs/2306.00978 and https://github.com/mit-han-lab/llm-awq/blob/main/awq/quantize/auto_scale.py . No upstream folded or quantized weights are inputs.
