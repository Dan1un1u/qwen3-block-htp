# EXP-0244 — Fixed-prefix layer and boundary A8 ablation

User 2026-09-08 approved layer-by-layer and boundary ablation after prefix treatment, emphasizing late SwiGLU, Down and residual, before deciding local R4 or residual R1. PC059 authorizes software attribution only. Parent source 7d7c46022a4d83c6faa700d24588bc02977bd374; target branch codex/exp-0244-prefix-a8-boundary-ablation. Registration may temporarily bind the clean parent branch solely to authorize branch setup.

## Frozen before inference

C64 manifest 7de4f0758d83f2ba3b58696c695bfbfed72a25dd3bf308475abee0a0f0575a89; canonical F16 manifest 0f8a359b559f252cb13329f57d4cabd56f17ca9bfb64bc5643f4d6014795070f. C64 whole state digest 1a798eca2237d2e31158c0574c44255484cb2890b6ef3cbba8f71d975ed62d3d. Fixed EOS token 151645 and exact EXP0243 calibration/eos.json MSE parameters under ledger f570b447e6f7bd45f39d5786e447bceb53a23cf4b43202d1dc37fc18d857ff6b. No new calibration, scale search, weight changes, rotations, training, grouping, mixed-precision deployment or device execution. Original norms, embedding and head stay fixed. A16 restoration is an attribution intervention, not a deployable candidate.

Correct proxy semantics before attribution: EXP0242/243 residual_mid pre-hook quantized only the post-attention norm input, leaving the residual skip unquantized. Retain that historical proxy as an explicit control, never rewrite sealed evidence. Corrected midpoint quantization executes once before fanout, supplying identical QDQ values to norm and residual skip. All masks use corrected semantics. Verify disabled instrumentation exact against canonical forward, shared-fanout sentinel, old-proxy reproduction and unchanged whole-state hashes.

Fresh seed244 data: four English/Chinese news/wiki cells, 96 documents each; first32/cell development (128docs,2048targets), remaining64/cell final (256docs,4096targets). Exclude all retained training/calibration/development/evaluation documents, text hashes and 32-token overlap through EXP0243, plus prior Wikitext titles. Prefer verified retained public corpora, bounded Wikipedia offsets74000..84000 only if needed. Freeze IDs, tokenizer, protocol and datasets before inference. Every sample has identical64 body context plus16 targets, EOS unscored, no body truncation.

Bulk development is exploratory screening only: controls F16, C64A16, corrected fullA8 and legacy proxy; all28 whole-layer restores (17 named sites/layer, embedding separate); all28 single-layer SwiGLU, Down output and residual(mid+out) restores; disjoint global groups norm_inputs, qk_projection, kv_and_qrope, attention, o_projection, gate_up, swiglu, down, residual, embedding, head_input; fixed global union swiglu+down+residual; four fixed7-layer segment restores. V output and V cache are one tied logical boundary; K/V restoration explicitly uses diagnostic FP16 cache at affected layers, otherwise real uint8 storage. All other online boundaries retain frozen affine U8 FP32 reciprocal/round-half-up/saturate semantics. Same model-only prefix is quantized according to each mask before seeding cache, independently of body/future tokens.

Sequential development rerank: first8 development docs/cell (32docs,512targets). Controls, all global groups, fixed union, top2 local masks from each focus family and top2 whole layers from bulk. Rank by sequential equal-cell meanNLL; deterministic tie within1e-6 prefers fewer restored logical sites then lexical name. Freeze best local, best layer, best global and deduplicated final list before final scoring. Bulk/sequential FP16 GEMM differences are reported, never assumed exact. Final sequential variants: F16, C64A16, corrected fullA8, fixed global SwiGLU/Down/residual/union, selected best local/layer/global, deduplicated (maximum10). Actual prefill64 then15 teacher-forced cache append/read steps scores16 targets. No final-data-driven selection or additional masks.

Checks: every mode validates cache length/append count and per-layer K/V dtype, immutable prefix; disabled and all-restored controls exact, independent same-shape float-QDQ cache oracle, scalar quantizer oracle, repeated and future-token causality checks, independent CE and independent aggregate NLL, whole state before/after. Preserve failed attempts; PC037 routine repairs may correct implementation without modifying frozen semantics, data, thresholds or evidence.

Report paired stratified-document bootstrap10000 seed244 for NLL differences/PPL ratios, four cells and excess-A8-NLL removed relative to same-prefix C64A16. Restoration effects are conditional/nonadditive; compare fixed union with individual restores without attributing additive causal percentages. Existing overall5percent/per-cell10percent versus F16 gate remains descriptive and unchanged; no deployment acceptance. Provide layer/boundary heatmap and final PPL figure, full N/A profiling closure (E2E token/s N/A), complete evidence ledger and project-memory closure. Discuss whether evidence prioritizes local R4, residual R1 or another boundary; do not implement a new transform automatically.
