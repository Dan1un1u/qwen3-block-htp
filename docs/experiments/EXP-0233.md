# EXP-0233: independent official AutoRound INT4 reference

User 2026-09-07: 好的，那就先做这三项吧。 First phase of PC052; next EXP234 group12864K and EXP235 C64 sensitivity remain approved.

## Design frozen before implementation and scoring

Use official intel/auto-round v0.5.1 (resolve annotated tag to commit, hash and archive source/license), isolated dependency environment retaining torch2.8.0/transformers4.51.0 execution where compatible. No quantization solver copied from this project's GPTQ. Original Qwen3-origin shards and tokenizer verified against existing authority. Two predeclared candidates: AR-P uses AutoRound blockwise learned rounding with an explicitly adapted symmetric[-7,7] quantization function, group_size=-1 and FP32 scales; AR-G uses unmodified official native int_sym group128 and FP32 scales. AR-P quantization formula adapter is independently checked against NumPy, declared adapted-tool evidence rather than untouched upstream. AR-G is native-tool format reference, not an isolated grouping/solver attribution. No LPBQ, rotation, AWQ composition or full-model QAT. All196 transformer linear matrices change; original gamma/embedding/final norm/QK norms and inherited EXP221A head stay exactly equal to C64. Frozen-head policy is deliberately matched, so neither candidate is claimed to be the best unconstrained full-tool recipe.

Use identical existing EXP230 C64 calibration512windows x128tokens =65536 unique positions. AutoRound200iterations/block, batch8, nsamples512, seed233, learning rate1/200, minmax learning enabled with same rate, nblocks1, quantized-input propagation enabled, AMP FP16, no torch.compile, no norm/bias tuning. The set and token budget match C64, but iterative optimization compute and possible FP16 input-weight rounding differ from GPTQ; do not attribute the result solely to solver. Retain effective arguments, sample hashes, tool commit/files, finite checks and export rounding representation. Never choose candidates/hyperparameters from PPL scores; both reported.

## Data and correctness

Reuse EXP230 development128 as exposed descriptive regression only; F/C64 outputs must reproduce per-token scores. Freeze new balanced en/zh x wiki/news512primary+512reserve documents,64prompt+16targets, exclude all prior calibration/training/validation/evaluation documents and32grams including EXP232. Independently reconstruct every source window. This same predeclared dataset may be reused as a disclosed paired evaluation panel by EXP234/235, with no parameter/checkpoint selection from these scores; sensitivity ranking is diagnostic and not a deployed selected model.

Check independent quantization function/code-scale reconstruction, all196 finite FP16 effective weights and roundtrips, unchanged non-transformer tensors, repeat-exact forward, future-token causal mask, independent cross entropy<5e-6. Evaluate all F/C64/AR-P/AR-G on identical canonical FP16 HF eager forward; this is software quality, not DSP implementation certification. Original final head remains frozen. Record native negative scale/full-range semantics; never force native[-8,7] values into project[-7,7] device format.

## Decision and closure

No candidate selection: both final models frozen before primary scores. Paired stratified document bootstrap5000 seed233; overallPPL ratio<=1.05 and all language/domain/cell<=1.10 against F. Trigger pre-frozen reserve for all4 if either candidate/F has any CI straddling a gate. Point fail=fail; all points and upperCI pass=pass; points pass with crossingCI at max budget=inconclusive. Maximum16384targets. Development scores never tune. Record uncertainty and C64 comparisons separately. Final report includes source/package/evidence ledgers and N/A hardware/profiling with EXP230 reference; no new E2E speed claims. Close EXP233 then execute separately registered EXP234 andEXP235. No baseline promotion.
