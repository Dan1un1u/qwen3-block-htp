# EXP0276 — decode SP2 physical-row copacking ablation

User-approved A3. Freeze EXP0272/275 no-rotation native W4 SP2mode8 FP32residual2, M64+15/cache128, original weights/LUTs/scales and exact ordered arithmetic. Two arms: current decode low/high copacked in idle HMX rows versus two independent HMX streams. Independent stream producer writes high directly to a legal existing dead VTCM buffer; no redundant conversion, new DDR tensor, scalar kernel or weight unfolding. Both use identical vector SP2 epilogue, precomputed sums, DMA tiling, weight reuse and all other legal pipeline optimizations. Prefill remains identical. Add explicit validated diagnostic control with telemetry identifying actual matrix passes, not just worker dispatch counts. Preserve OFF/current default. Source branch may be created after initial clean parent preflight, then rebind lock.

First static lifetime/alignment/accumulator proof, selected layer14 component/full-block output and KV byte-exact to EXP275; chain3 and full28 exact residual/head/generated-token evidence. At most3 attributable repair attempts, retain failures. Then fullmodel5short10formal balanced alternating two-arm pairs, repeat10 main, one repeat1 auxiliary perarm. Same physical8MiB/no tensorDDR/spill/complete ledger and column-sum correction, no tolerance relaxation. Since intentional diagnostic extra compute, speed>10percent does not stop measurement; no candidate promotion. Count actual additional HMX tiles/passes and same weight-read work. Separate kernel attribution from real fullHost/tps; no quality claim. Pairedbootstrap20000seed276, fixed rounds all retained. Complete module tables, E2E, provenance ledger, report/closure. No new trace repair required: EXP275 trace coverage N/A and not an engine-utilization claim. Device serialized with L32-0030; local coding/build may proceed while Llama measures.
