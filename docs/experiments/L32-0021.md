# L32-0021 R4 layout and SP2 finish overlap
User authorizes continuing optimization after0020. Owner llama32-no-rotation.
Up to three bounded scheduling/layout candidates; do not repeat rejected0020
register transpose. First overlap previous SP2 finish on two HVX workers with
next R4 layout on caller while single HMX owner processes the current batch.
Preserve dense H64 R3/H16 x H512 R4 arithmetic, SP2 mode8, FP32 residual,
weights/calibration/LUT and no-rotation defaults. No FWHT/butterfly arithmetic.
8MiB VTCM, no timed intermediate DDR/spill, disjoint slots/scratch, correct
publication barriers. Independently validate layer0/7/15 OFF/R3/R4/both:
components<=1FP16ULP+minnormal, actual-tail exact, ideal row cosine>=.999;
old exact CLI failure retained. Match0020 output hashes as additional check.
Use verified0019 rotated and0016 OFF packages read-only. Binaries and prior
control protocol validated against0020 sealed ledger; no unexplained hash acceptance.
After auxiliary candidate selection run fixed10 cyclic five-arm processes:
current OFF/R3/R4/both and sealed0020 both control. One warmup+10 measured
M64+decode1 replays/process. Bootstrap paired process means20000 draws seed21021,
CI95upper<=1.10 versus currentOFF both phases. No optional stopping or reruns
to chase a pass. Prior conditional/ideal audits required; original numerical
failure status retained by diagnostic replay; physical/RPC/structure abort.
For each rotation arm, both phase gates must pass before extending that arm
to consecutive3 then16/frontend and fullmodel M64+15 fixed10 comparison, as
already specified per-candidate by0019. Failing arms remain paused. Generate missing
Llama rotated Down packages afresh from original only, same frozen train method.
Stop and discuss any unresolved gate failure before fullmodel. No PPL/default
or quality promotion. Preserve all attempts in l32-0021 and0020 evidence unchanged.

Before formal profiling: clarify per-arm eligibility above, retaining both original
phase thresholds. Candidate3 final consumes SP2 results directly from VTCM;
superseded8-row array generated stack activation staging and is excluded from
physical/performance acceptance. Preserve its arithmetic evidence and disassembly.
