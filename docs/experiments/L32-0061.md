# L32-0061: AV-to-O scale folding probe

User approves one bounded configuration after discussion of eliminating post-HMX AV requantization. Llama3.2-1B, no rotation, FP32 residual, uniform INT16 Down, M64 prefill and M1 decode with64 cached tokens. First layer0 (multiplier9); all16-layer saved qparams may be audited offline. No fullmodel E2E, baseline promotion, PPL or other recipe changes in this probe.

Control retains AV centered multiplier/saturation. Candidate consumes unchanged HMX AV U8 carrier at zero128 and folds av_multiplier into O FP32 output scale; retain HMX AV shift and center. Check all head groups agree before using a scalar folded scale. Frozen W4 codes/scales; only fresh regeneration in new experiment directory allowed after intentional historical artifact retirement. Verify regenerated inputs separately against preserved original hashes where possible. No reinterpretation of missing old payloads as valid new evidence.

Independent exact integer identities on non-saturating inputs, saturation frequency/amplitude on actual AV outputs, separate new-contract reference for saturated values, O signed24 partial and signed32 accumulation bounds, FP32 rounding and zero-point checks before timing. Capture runs untimed. Same-binary opt-in control/candidate; ordinary default unchanged. Selected single-layer full attention+O and block timing if available; component-only fallback is explicitly labeled, never extrapolated to E2E. Five short, ten formal paired AB/BA repeat10; bootstrap seed610061. No optional stopping or quality claims; record all failed attempts. Physical8MiB VTCM/no timed intermediate DDR spill, one HMX owner, device ownership serialized after checking other-project lock. Routine owned repairs autonomous. Complete results before discussing expansion.
