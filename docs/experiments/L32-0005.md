# L32-0005: A8 relative speed

User authorizes investigation and fixes for A8 lacking expected speed advantage.
Own no-rotation, unchanged Llama weights/calibration/quantization/quality status.
L32-0004 ledger identifies repeated per-head V LUT construction, scalar padding
work, disabled row4 common/SwiGLU/softmax paths, and A8 RoPE sparse-repair scanning.
Reuse exact existing arithmetic/carriers; no rotations or new quantization scheme.
Freeze and hash existing16-layer binaries before changes. A8 single/3/16 exact
output/KV gates precede full generation; retain all physical/provenance checks.
Run independent padding/softmax audit where enabling old fast paths. W4 frozen
reference remains unchanged. No silent change to quality or acceptance gates.

After correctness, run fixed10 rotated three-arm cycles: old A8, candidate A8,
W4A16 OPT2 reference, all same original prompt M64 and7 continuous decode tokens.
Rotate ABC/BCA/CAB; no optional stopping or repeat1 gate. Candidate/old A8 paired
bootstrap95 upper<=1.10 both prefill/decode; compare candidate/W4 under same shape
and disclose data-dependent recipe outputs. Preserve raw full15-decode L32-0004
results as historical, not matched reference. Complete additive module ledger and
real E2E incl embedding/norm/head/greedy/FastRPC; exclude load/tokenizer. Changes
with no speed benefit can remain unselected; no quality baseline promotion.
After verification propagate common source with ownership transfer, seal results.
