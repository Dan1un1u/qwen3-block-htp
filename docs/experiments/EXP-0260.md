# EXP-0260 — W4F16 decode active-row and layout optimization

User authorizes optimizing W4A16 decode by transferring established W4A8 implementation lessons. Parent EXP0259 source4fad27dc0a50a6d541f1d23ecf15018ec1099df6. Freeze C64 per-output-channel W4 package from EXP0230, all scales/gamma/head, FP16 activation and FP16 HMX arithmetic, stable scalar expf Softmax formula/ordered FP32 sum and FP16 probability rounding, tokenizer and cache semantics. No quantizer/rotation/grouping changes. F16F16 and W4U8 paths remain unchanged; no mllm work or automatic baseline promotion.

First attribution: existing W4F16 dynamic decode Attention and scalar FP16 conversions/data movement. Implement one bounded exact candidate with effective-row packing/unpacking, batch exact FP16 conversion and safe vector transfers. Keep original control selectable in the same binary. Dense weight expansion and all nonlinear arithmetic remain unchanged. Do not substitute approximate exp/rsqrt or change reduction order. Additional scheduling optimization only after measuring this bounded candidate; unrelated projection redesign remains a later registered experiment.

Before timing verify independently specified packing/conversion sentinel cases, correct valid row and zero/preserved padding, then real layer0 M64+eightM1 and continuous three-layer replay. Candidate versus control live hidden rows and persistent KV must be byte-exact; padded scratch not consumed by live rows. Independent frozen C64 reference/old-runtime outputs must reproduce, and final16-token feedback plus selected FP16 logit codes exact. Add telemetry proving selective path invocation and unchanged physical scope. Decode existing native FP16 cache supports80 capacity; test M64 plus15decode and no extrapolation to long context. Numerical/physical thresholds unchanged. Same8MiB, no timed intermediateDDR/spill/full logits, oneRPC per complete step, oneHMX owner.

Collect fresh control on current ABI first. Five short then ten formal rotated control/candidate pairs repeat1/repeat10. Single-layer correctness gate precedes slice then full28layer generation. Fullmodel candidate allowed only if layer decode speed improves and prefill95percent pairedCIupper<=1.10; otherwise report bounded failure without promotion. Fullmodel primary rank is repeat10 continuous-decode Hostwall/token/s; prefill regression diagnostic within10percent, repeat1 and full generation loop included. Fixed10additional pairs only if confidence near relevant threshold, registered before extra captures; no tuning/discarding/further optional sampling. Preserve every failed attempt and exact gate boundary. C64 remains a quality reference, not newly accepted model; this bit-exact optimization does not require a new PPL dataset or claim new PPL acceptance.

Complete raw ledgers/counters, provenance and evidence seal; stable three-recipe module overview includes new W4F16 result clearly distinguished from unchanged Selected EXP0166, plus actual measured E2E. PC037 permits attributable repairs with retained failures and no further permission requests. Close source/memory clean and synchronized.
