# EXP-0259 — Dense R3 data movement and pipeline optimization

User authorizes optimization of the current R3 implementation only; no mllm inspection. Parent EXP0258 source000509dbce670b5ac4b0014db82c5c891474381b and sealed evidence875397fc4185340c7ada2f35aa09c2e3d6f1799e4f0566187fe02d9ca1c3cd79. Freeze C64 W4, OFF/R3_ALL qparams, wideNR64, prefix, HMX dense arithmetic and ABI120 head lifetime fix. No butterfly, new calibration, refinement or mathematical change. Preserve old R3 as control.

Bounded stages: prepacked constant dense sign matrix and layout-specific HVX moves, then restore per-head preparation on QKV readiness events using existing workers and verified dead scratch. Explicit lifetime/ownership checks, same8MiB VTCM, no timed intermediate DDR or added RPC. Scalar sqrtf/arithmetic/rounding remain unchanged; batching approximate rsqrt is excluded. New telemetry accounts overlap without double charging.

First same-input real layer14 M64 and eightM1 diagnostics: raw postRoPEFP16, rotatedFP16,Q/Kcodes,integer attention and final output must exactly match current R3 implementation, with independent component/reference provenance fromEXP0258. Known idealFloat64 wholelayer mismatch remains failed; this optimization never claims accuracy acceptance. Compare noR3,oldR3,each bounded optimization. Five short then ten rotated paired formal rounds repeat1/repeat10. Speed eligibility remains noR3+10percent maximum for both prefill/decode. If final optimization confidently exceeds10percent, stop fullmodel and report/discuss. If within10percent, three-layer gate then fullmodel paired trueE2E is authorized by the user's current optimization request, retaining known numerical failure as diagnosis only. Never promote baseline or claim PPL acceptance.

Freeze workloads before timing, preserve failed attempts, no deletion/skipping, provenance and byte-exact checks before timing. PC037 attributable repairs autonomous. Report stable three-recipe module table, measured E2E only if actually collected. Close source/memory and evidence clean/synced.
