# EXP-0243 — First-position causality and fixed-prefix static A8

User 2026-09-08 approved starting the updated direction. PC058 authorizes this bounded first-position and prefix experiment. Parent EXP0242 source394253ea340e1bdd1ddca1923dc0e9cb0bd9c13b. No training, rotation, weight changes, grouping, mixed-precision deployment or device work in this phase. Conditional later methods require their own registered protocol after attribution.

## Frozen before inference

C64 manifest7de4f0758d83f2ba3b58696c695bfbfed72a25dd3bf308475abee0a0f0575a89 and canonical F16 manifest0f8a359b559f252cb13329f57d4cabd56f17ca9bfb64bc5643f4d6014795070f. Reuse verified EXP0230 loaders, original norms/embedding/frozen W4 head. Exact whole-state digest unchanged. Static affine U8 FP32 reciprocal/round-half-up/saturate semantics from EXP0242; all478 online boundary sites remain, prefix-only offline hidden intermediates excluded because only fixed prefix KV is reused. No per-token dynamic activation scale or K-group W4.

Calibration: EXP0230 first32 calibration documents/cell,128x128=16384tokens, same verified identities as EXP0242. Development: EXP0230 existing128 docs/2048targets. Fresh final: seed243,128 new documents/cell,512docs/8192targets at64bodycontext+16targets; exclude all retained datasets through EXP0238 (EXP0239 shares238, EXP0242 shares230), document/text/32-token overlap plus Wikitext titles. Sample only retained verified public corpora first; bounded new Wikipedia offsets64000..74000 if necessary. Freeze all IDs/tokenizer/template/protocol/data hashes and final data before inference; do not score fresh final until selection frozen. Historical panels remain exposed controls.

Candidate prefixes, deterministically constructed before inference and deduplicated: empty; one ordinary dot token; one tokenizer EOS token; most frequent first token among the128 calibration windows (tie smallest token ID); actual Qwen3 chat-template text preceding a user-content marker. Prefix length <=16; if natural header exceeds16 retain it as a diagnostic, not deployable candidate. No learned prefix/cache. Same fixed body tokens across PPL variants, no body truncation, prefix unscored. Real complete chat-template diagnostics separately tokenize decoded calibration text with the actual saved template.

Causal diagnostics: F16 and C64, first4calibration docs/cell. Trace per-token maxima and normal-token statistics at all28layers for norm inputs, SwiGLU, Down and residual. Compare bare128, bare64, window-shift1 length64, fixed first-token replacement, actual complete chat template, and cached64+64 continuation. Observe whether the same content spikes when restarting an empty cache. This phase does not claim identified attention-sink causality solely from correlation. Preserve root cause alternatives F16 intrinsic versus W4 amplification versus template/session mismatch.

For each candidate prefix, construct independent prefix-only FP16 software KV under the corresponding fixed model; cache contains no body or future data. For C64, collect full online-body calibration distributions with that prefix, choose only three predeclared policies minmax/MSE/p99.99 using EXP0242's fixed range grid. Merge prefix extrema into stored K/V ranges and share V projection/cache qparams so reused prefix and body use the same static carrier. Serialize runtime cache as actual U8 codes with scales/zero points; dequantize for FP16 software attention arithmetic. No permanently unquantized prefix KV is eligible. Incoming post-RoPE K and V are quantized before cache append; retained prefixes must not be mutated across independent samples. Validate RoPE offsets, sequence length, masks, cache append count and U8 contents. If a required format is incompatible, report the boundary instead of inserting an undeclared FP16/grouped path.

Development: each prefix gets paired F16/C64-A16 and three full-boundary C64-A8 policies. Choose lowest equal-cell A8 meanNLL, ties1e-6 prefer fewer prefix tokens then fixed candidate order/minmax-MSE-percentile. Also freeze best empty-prefix policy. Report matching-prefix A8/A16 delta and prefix-induced F16/A16 change separately. Final compare F16 and C64 no-prefix and selected-prefix, selected full-A8 with prefix, and empty-prefix minmax/best policy (deduplicate). Primary final scoring uses actual cache prefill64 followed by15teacher-forced append/read steps, scoring16targets. No token/s from evaluator elapsed time. Paired stratified-document bootstrap10000 seed243, four-cell PPL, NLL deltas and CIs; historical5%/10% total-F16 gate unchanged, no automatic acceptance because A8 matches an already-imperfect C64.

Independent controls: disabled instrumentation exact logits; exact scalar U8 oracle; actual uint8 prefix/body storage and roundtrip; deterministic repeated scores; independent CE accumulation; causality under future-token replacement; prefix-only cache reuse versus full-prefix conditioning; bulk versus sequential FP16 GEMM finite-precision discrepancy recorded separately, not forced byte-exact across different GEMM shapes. In FP32 oracle, cached/uncached causal algebra checked with declared tolerance1e-5 absolute/relative. Existing canonical controls repeat under original computation for reproduction. Weights/previous evidence immutable. PC037 routine recovery applies, failed attempts retained; no silent threshold or scope changes.

Deliver PPL/attribution report, updated first-token and cache distributions/plots, complete N/A profiling closure and hash ledger. If software quality fails, discuss findings before a different transformation; if it recovers, separately register the authorized hardware feasibility continuation and retain >10% single-layer regression stop rule, counting prefix setup/cache copy/extra attention cost. No hardware or other-recipe changes in EXP0243.
