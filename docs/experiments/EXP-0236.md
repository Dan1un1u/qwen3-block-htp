# EXP-0236: G64 W4-only LM head repair

User2026-09-07 approves validating LM-head accuracy repair on G64 and explicitly defers mixed precision. PC053 implements this bounded host-only quantization experiment. No FP16-restored or W8 head candidate, transformer changes, rotation, QAT, DSP deployment or other-recipe work.

## Fixed variants and calibration

F: canonical floating software reference. G64: byte-identical EXP234 transformer and inherited EXP221A per-channel W4 head. P64: same G64 with freshly quantized per-channel W4 head. H64: same G64 with freshly quantized static original-column group128 W4 head. Both candidates reload original FP32 head; grid[-7,7], FP32 scales, FP16 activation/export semantics. Preserve all other parameters/buffers and transformer package hashes. Neither candidate is a mixed-precision restoration.

Reuse verified EXP230 512x128 balanced en/zh Wiki/news calibration,65536positions. Verify EXP234 last-layer calibration checkpoint against its artifact ledger; apply the unchanged actual FP16 final RMSNorm to obtain head inputs. Independently replay selected calibration windows through the canonical G64 transformer to validate retained inputs (NRMSE<=.003,cosine>=.99999, all finite); CPU/GPU execution differences are explicit. Same inputs and Hessian for both candidates. Existing CPU factor: global stable act-order,damping.01,FP64Cholesky thenFP32. P64 uses EXP224 three full GPTQ trials: absmax, midpoint, weight-L2.4-clipped range. H64 uses identical EXP234 static-group version. Select per-output-row solely by complete65536-position local projection output SSE with FP16 reconstructed weights; tie first. No global hyperparameter/model selection by PPL, no head gamma folding, no teacher-hidden trajectory replacement. Export row chunks1024 using unchanged quantizers; exact packing and independent NumPy reconstruction across every row. Frozen original head is not modified in place.

## Data roles and evaluation

Before either head export or scoring, freeze a new balanced1024-document/16384-target panel (seed236;256/cell en/zh Wiki/news), M64+16, excluding all earlier calibration/training/development/test documents,text hashes and32-token spans throughPC052. Reuse verified public source corpora; fetch additional fixed Wikipedia pages only if cached unused documents are insufficient. Independently reconstruct every token window and exclusions. All1024 documents are one fixed final test, no adaptive reserve or selection; equal total budget to prior8192+8192 protocol. Calibration unchanged. OldEXP230development128 and exposedPC052panel are disclosed regression-only, not independent tests or tuning inputs. Both fixed W4 candidates receive full final evaluation regardless of development scores.

Canonical FP32CPU model load thenFP16CPU thenGPU includingRoPE, batch4 teacher forcing,TF32off, deterministic algorithms, same tokenizer/mask/16targets. Evaluate F/G64/P64/H64 on the new panel. Reproduce F/G64 development controls exactly. Evaluate both candidates on the disclosed PC052 panel after export freeze for paired historical comparison; retain verified F/G64 same-panel scores. All PPL main comparisons include matched F and G64, language/domain/cells, paired stratified-document5000bootstrap seed236; overall5%, everylanguage/domain/cell10% unchanged. Pass requires all point estimates and upper95%CI within thresholds; failingpoint=fail, remaining cases=inconclusive atfixedmaxbudget. Effectiveness vsG64 separately uses pairedratioCI, never automatic acceptance/promotion. Confidence intervals are pointwise, not simultaneous across two candidates. No further export/tuning after final results.

## Correctness and closure

Verify originalshards/calibration/tokenizer/G64 package/checkpoint provenance before reuse. Existing independent quantizer Schur-elimination/clipping/SSE oracles; complete independent unpack/dequant equality; record candidate choices/scales/SSE. Verify head-input finalnorm against independent FP32 reduction with FP16 boundary and replay. Complete model non-head identity audit for allvariants; repeat exact, causalmask exact, finite logits/NLL and independent CE<5e-6. Final restoredG64 development sentinel must reproduce all per-token NLL/top1. Independently reduce every reported PPL from rawtoken losses; verify all source/command/artifact/evidence ledgers.

Software-only quality validation: fullprofiling record has explicit N/A for allnewDSP/physical/speed sections and verifiedEXP230 historical references. No newdevice speed or inference from softwareelapsed time. Otherrecipes frozen; no baseline promotion. Close and discuss outcomes; no automatic MLP or mixed-precision escalation. PC037 routine repairs preserve failures and rerun affected gates without unnecessary approval.
