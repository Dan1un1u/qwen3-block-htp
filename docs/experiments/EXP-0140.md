# EXP-0140 — W4F16 Down join-only revalidation after subgroup4

## Decision and hypothesis

The user's continuing autonomous W4F16/W4U8 specialization loop authorizes
this bounded W4F16 experiment on 2026-09-01. It branches from EXP-0136 source
commit `30fdf531939b8b96dce428c0d890adbf17714a10`, preserving its four expansion
contexts, four locked worker plus main-context SwiGLU drain, and four-tile
consumer subgroups.

EXP-0116 proved that removing per-region publication fences from the
nonstreamed Down expansion makes Down itself about 2.6% faster, but its older
shared-pool lifecycle increased upstream expansion and join pressure enough to
regress complete Host wall. EXP-0136 materially changes that failure
condition: Gate/Up's final stream join tail is reduced about 98.6%, all four
locked workers participate in the drain, and the main context enters Down only
after a generation-counted subgroup completion boundary.

The hypothesis is that the existing Down join-only expansion helper can now
remove redundant per-region ordering without feeding contention back into
Gate/Up. This is a declared revalidation under a changed worker lifecycle, not
a repetition of EXP-0116 on the same scheduler.

## Gates

The control is the complete EXP-0136 subgroup4 candidate with ordered Down
publication. The candidate changes only Down W4-to-FP16 expansion to the
existing relaxed helper; its final pool-join visibility barrier remains.
Gate/Up expansion, DMA, HMX batch-eight commands, four-tile SwiGLU subgroups,
worker count and assignments, QKV carrier, Attention, O, residuals, weights,
scales, FP16 activations and the frozen Public Common Layer remain fixed.

Both cells must produce byte-exact output `f18b9abbe1487231`, zero W4F16
expansion mismatch and zero maximum LSB error. They must request exact 8 MiB
VTCM, retain zero intermediate DDR and spill/fill, one FastRPC Execution Unit,
one HMX owner, no QNN, and identical weight bytes, DMA descriptors, HMX
commands, tile work, carriers and peak plan. Static evidence must show relaxed
publication applies only to Down and that the final worker join barrier remains.

The candidate passes only if Down projection and complete Host wall strictly
improve in ordinary and paired repeat-one and repeat-ten medians over five
rotated rounds, while Gate/Up does not regress by more than 0.5% in either
ordinary or paired medians. PC-027 and PC-028 are mandatory. A local pass
remains pending user decision and cannot promote a Selected Baseline.

## Formal result

Formal evidence was collected on stable boot
`77ccafe0-8817-4976-bdae-15af5608d4ff` from source commit
`e75563457293de9430f8c9d20a04ced6147e4737`. Both cells produced output hash
`f18b9abbe1487231`, zero maximum LSB error and zero W4F16 expansion mismatch.
Both requested and acquired exactly 8 MiB VTCM, retained zero intermediate DDR
traffic and spill/fill, one FastRPC execution unit, one HMX owner, and identical
weight bytes, DMA descriptors, HMX commands, tile work, carriers and peak plan.

The corrected single-variable candidate passed every speed cell. At repeat one,
Down improved 6.055% ordinary and 6.976% paired while complete Host wall
improved 0.537% ordinary and 0.760% paired. At repeat ten, Down improved 6.796%
ordinary and 6.899% paired while complete Host wall improved 1.225% ordinary
and 1.026% paired. Gate/Up changed by only +0.235%/+0.092% at repeat one and
+0.199%/+0.081% at repeat ten, within the declared 0.5% preservation bound.

This result also resolves the historical EXP-0116 ambiguity: its
`join_only_down` selector unintentionally disabled Gate/Up join-only behavior,
whereas EXP-0140 preserves the full EXP-0136 subgroup4 Gate/Up contract and
changes only Down publication. The local gate therefore passes and the
candidate is eligible as a provisional parent, but adoption remains pending
explicit user decision and no Selected Baseline is changed automatically.

Formal results:
`D:/llm_exp/results/qwen3-block-htp/exp0140/20260901T040452Z_e75563457293_formal`.
Retained artifacts:
`D:/llm_exp/models/qwen3-block-htp/exp0140/artifacts/e75563457293/20260901T040452Z_formal`.

## User promotion under the repeat-ten ranking rule

On 2026-09-01 the user explicitly promoted the eligible EXP-0140 candidate as
the selected fastest W4F16 recipe-specialized baseline. The promotion follows
PC-029: among candidates that have already passed correctness, physical,
evidence, and completion gates, formal rotated-pair repeat-ten complete-block
Host wall is the ranking metric.

The promoted source is commit
`e75563457293de9430f8c9d20a04ced6147e4737`, fixed by annotated tag
`baseline-w4f16-fastest-exp0140-subgroup4-down-join-only`. Its repeat-ten Host
wall is `1,955,000.0 ns/block`; repeat-one is `2,391,250.0 ns/block`. Relative
to the previous selected fastest W4F16 implementation EXP-0110
(`2,122.7 us/block` at repeat ten), this is a 7.900% latency reduction, or an
8.578% throughput increase. The frozen EXP-0109 fair-comparison baseline is
retained separately and is not replaced by this recipe-specialized promotion.
