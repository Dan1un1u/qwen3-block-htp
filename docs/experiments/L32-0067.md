# L32-0067: residual boundary fusion ablation

User approves only this four-arm experiment. Llama3.2-1B full16, M64+42 fixed tokens, uniform INT16 Down, native per-channel W4, FP32 residual/Norm, no rotation. Use latest eligible L32-0065 M folded package and frozen independent reference. No QK method changes, fresh calibration, other model/recipe work, or model-quality/PPL claims.

Arms: fused; N split Norm->Q (normalize to FP32 scratch then vector quantize and direct native packing); R split O/Down->residual (integer correction/reconstruction and FP32 scaling produce FP32 scratch then vector residual add); NR both. Preserve exact operations/rounding, masks/padding/live-row work, quantizers, weights, all common vector kernels/basic DMA buffering and production scheduling where dependencies permit. No additional A8 narrowing, artificial repack/roundtrip or scalar controls. No full-operator barrier unless needed by the natural split. Scratch is on-chip and accounted within8MiB; no timed intermediateDDR/spill, one HMX owner. Record actual scratch/lifetime/granularity and isolated boundary read/write volume; do not equate overlapping counters with critical-path shares.

Before timing prove unchanged fused regression and exact same-contract split outputs, independent selected/three/full16 references and deterministic padding; preserve failed evidence. Selected/3layer audit precedes full16. Five short then ten fixed formal balanced rotated four-arm repeat10 cycles, warmup and fixed trace, no optional stopping. Complete Host wall prefill64/decode42 includes embedding/finalNorm/head/greedy/FastRPC. Repeat1 auxiliary. Diagnostic split arms may exceed10percent without veto. Paired bootstrap20000 across cycle means. Report each intervention versus fused and interaction, complete modules, plus optional split internal attribution if reliable. No softmax or AV changes.

Results /mnt/d/llm_exp/results/llama32-htp/l32-0067. Device ownership serialized; Qwen inactive at registration. Existing results immutable. Source/memory normal commits and pushes, clean preflight before build/deploy. Routine owned fixes authorized. Completion includes reports/evidence seal, workbook ablation archive if available, source/memory synchronization and device release. No automatic baseline or main-table refresh.

## Completed
Exact1/3/16 and full16 padding audits passed; five short/ten formal balanced four-arm repeat10 completed. Uniform INT16 Down, unchanged weights and all unrelated boundaries. a01 arithmetic passed1/3 but compiler vector-array stack storage was repaired identically for all arms before timing; a02 is the only performance evidence. No PPL/baseline promotion. Sourcea0d6d8f504d360009333c0d0b7c649e49ce69aa2; ledger4ca3b60a027cc4ac6b0435a2f4ee0f99e30b069bf49a3c94d293f5385dbdfc68; device released. Workbook H/I/J appended only; A-G unchanged. See RESULTS/MODULES.
