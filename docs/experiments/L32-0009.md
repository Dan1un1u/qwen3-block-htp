# L32-0009: fused SP2 Down full-block integration then performance

User agrees to next step and requests performance optimization after correctness.
Owner no-rotation; parent0a00e0a. Preserve original-derived W4 per-output scale,
all other quantizers, W16/W4A16 defaults, and frozen Qwen. No baseline promotion.

Freeze L32-0008 241-level signed SP2 codebook and calibration-only sharedscale.
Fuse Gate/Up U8 lookup into low/high native U8 production, no old middle U8
rounding or intermediate logical signed16 array. Integrate exact native W4
integer dot; Down output requantization uses declared Q31 per-channel positive
multiplier M=round(alpha*weight_scale/down_scale*2^31), signed rounded multiply,
then existing outputzero/clamp and unchanged Q14 residual. Independent int64
oracle must match every output; record Q31 approximation separately from SP2.
No group scales, S8 expansion or rotation. SP2 selection is explicit and defaultoff.

Correctness first: independently export full-block prefill64/decode1 past64
fixtures for Llama layers0/7/15 and compare Down/final output and KV. Unchanged
U8 regression must match its sealed original output/KV. Check acquired8MiB,
all buffer lifetimes, one HMX owner/RPC perdeclaredblock, no timed tensorDDR/spill.
Retain failed attempts. Gate/Up LUT enumeration and signed/carry tests validate
producer; actual layer checks validate integration rather than mirroring code.

Only after correctness passes optimize at most three bounded scheduling/layout
iterations, preserving codes and using new immutable attempts. Fix final ten
AB/BA paired complete Hostwall measurements on a predeclared representative
layer0 at M64+1 decode,past64,capacity80,baseline current unrotated A8 vsSP2.
Both phase paired-bootstrap95% upper<=1.10 required before expansion. Repeat1
auxiliary only; do not infer fullmodel token/s or PPL. This task ends at validated
single-layer/performance result; fullmodel work only after the gate and a later
scope decision. Preserve metadata/additive ledgers; no evidence from failedbuilds.

Evidence/models under llama32-htp/l32-0009; original packages read-only. Build
success and binary seals checked beforedeployment; ordinary fixes autonomous.
