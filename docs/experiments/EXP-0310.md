# EXP-0310: V79 independent bandwidth microbenchmarks

Authorized by user 2026-09-23. No model or recipe change; no baseline promotion.

Use standalone FastRPC prepared 8 MiB VTCM session on SM8750 V79. Sweep HVX read/write/copy sizes and 1/2/4 worker contexts, HMX FP16/U8S8/U8S4 stream lengths and operand working sets, and DMA DRAM-to-VTCM chunk and chain depths over 128 MiB source. Verify outputs outside timing, inspect assembly, archive binary hashes, software version and thermal state. Use DSP qtimer for wall time; warmup then ten repeat measurements for selected cases. Report median, best observed, spread; physical payload bytes only. DMA src bypass tested with large source footprint; no claim that cache bypass proves every byte reaches DRAM controller.

The objective is an observed sustainable ceiling under disclosed conditions, not an undocumented theoretical hardware limit. HMX operand read rate may include matrix execution constraints. No end-to-end token/s exists for these microbenchmarks, so model module tables and quality gates are N/A. Performance thresholds are descriptive, correctness remains required. Preserve failed attempts and repair attributable implementation issues under PC037.

## Closure
Completed; all 45 formal invocations pass output checks. See EXP-0310-RESULTS.md for byte accounting, limits, and exact evidence. No baseline promotion. Source closure 1eb5abc4b7b9c60e9b37cccedd27a780f8d1213f.
