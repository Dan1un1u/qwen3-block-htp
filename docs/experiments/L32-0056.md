# L32-0056: generic A16 long-context throughput repair


User 2026-09-19 requests sequential long-sequence slowdown repair except Qwen3-0.6B, retaining10% gate. Order Qwen3-1.7B, Llama1B, Llama3B, single device owner. Primary targets are W16A16/W4A16 B536+46/C741+3 after EXP0299/L32-0055. Existing A8/SP2 prefill passes outside excluded0.6 and is frozen unless an identified shared regression requires repair. Fixed project fixtures, no named datasets/PPL/quantizer changes/rotation. Preserve all weights, residual and existing mathematical contracts, original M64 reference and history. Generic HVX/layout/DMA double buffering and resource scheduling only; no model-specific specialization or artificial baseline degradation.

First profile existing attention subcounters and inspect generic physical packing. Bounded up to four engineering candidates per model: (1) exact vector packing/conversion, (2) exact softmax preparation/layout fusion, (3) bounded parallel preparation/softmax using existing workers, (4) safe DMA/compute overlap. Keep original code as selectable exact control. Freeze chosen candidate before five short and ten formal rotated paired repeat10 rounds, preserving every sample. Stop model tuning on confidence pass, or after bounded attempts with honest remaining-gap report. Port only proven generic mechanisms to next model.

Gate: prefill long/M64 TPS paired-bootstrap95% lower>=0.90. Select fastest eligible same-round unchanged native or long64 reference, never slow M64 to pass. Matched long-shape candidate/control decode Host-wall paired95% upper<=1.10. Cross-context decode differences separately reported, not hidden or mislabeled; retain same-length regression guard. Short repeat1 auxiliary only. No numerical/physical tolerance relaxation or automatic baseline promotion.

Exact same-input regression against pinned prior binary at64/65/128/129/536+46/741+3 and poisoned unused KV; independent actual-operand softmax/norm/head and inherited layer references. Preserve historical full-floating failure status. Full model valid outputs/cache/head exact for data-movement candidates. One HMX owner,8MiB VTCM, one RPC/chunk or token, no new intermediate DDR/spill, bounded scratch ownership; existing segmented3B weights/mapping costs included. Existing original M64 and A8/SP2 smoke regressions when shared code changes; excluded Qwen0.6 unchanged by dispatch guards. Do not duplicate prior fullweight exports or model datasets.

Each project keeps its own source/memory preflight and result root. Qwen source initially bound to clean EXP0299 parent only to create codex/exp-0300-a16-long; then rebind before edits. Llama no-rotation branch retained. Reuse frozen parent packages read-only, archive new runtime/commands/hash proofs and additive modules, update only affected workbook ABC rows after formal validation; curated D and excluded0.6 retained. Routine attributable repair and ordinary commits/pushes authorized; preserve failed evidence. No unexplained new hashes, destructive recovery or stale authority fallback.

## Completed result
See L32-0056-RESULTS.md and L32-0056-MODULES.md. Implementation checks and paired measurements completed; no quality promotion. Desktop workbook updated and read back. Evidence SHA256: 0d94ed728ba1b6499c9a5a19ebac6b05f87ba5b77f89e5117c9704125fc55b2a.
