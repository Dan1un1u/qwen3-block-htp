# L32-0015: Llama dense R3 and R4 with native SP2 mode8

User approves migration from L32-0012 and specifically accepts SwiGLU producing
FP16 HMX layout, dense R4 then SP2 native low/high planes, fresh inverse-R4
Down folding and recalibration (2026-09-14). No high-precision residual/head
changes in this experiment. R1/R2/smoothing and L32-0013/0014 weights excluded.

Use current source worktree with isolated opt-in Llama paths; immutable matched
baseline is L32-0012 mode8 tested ddbc07f422693534f7d29e554f1c92c1b87cea18,
frontend l32-0010/frontend-a01. Preserve its SP2 241 levels/radix256, other W4,
embedding/head, nonlinear/residual/KV contracts and calibrated bounds as baseline.
Down R4 weights must start from original floating Down weights, absorb full8192
H16 x H512 inverse then quantize per-output-channel; never rotate existing W4
codes or reuse Qwen tensors. Fit new SP2 range on train-only calibration.
R3 is dense64 post-RoPE, no Llama Q/K norm; R4 uses explicit dense factors,
no activation butterfly/FWHT. Unrotated defaults and frozen branches unchanged.

First check tools/device/storage and component feasibility. Validate dimensions,
rotation identity/reference, SP2 endpoints/encoding/tile layouts and full integer
Down. Independent actual-arithmetic oracle versus ideal floating reference must
be distinguished; no numerical gate weakening. All old failed evidence retained.
Validate layers0/7/15 prefill64/decode1 past64, then consecutive3 and full16 only
when previous gates permit. No timing claim from software emulation.

Performance arms: baseline mode8, R3, R4, R3+R4. Fixed ten paired AB/BA Host-wall
cycles per candidate on singlelayer; both prefill and decode bootstrap95 upper
ratio <=1.10 versus baseline. R3 near-zero overhead is a hypothesis reported as
measured, not an assumed pass. Repeat1 auxiliary only. Stop failed speed candidates
before fullmodel and discuss; any new material contract conflict pause/discuss.
Eligible arms proceed same-build fixed ten rotated paired fullmodel M64+15 cycles.
Retain8MiBVTCM,oneHMXowner,no timed intermediateDDR/spill,oneRPC per boundary,
additive ledgers, no W4 expansion. FP16 HMX allowed for declared rotations only;
never count rotation MACs as prohibited projection fallback.

Results /mnt/d/llm_exp/results/llama32-htp/l32-0015; models corresponding models root;
builds under owning worktree. Record all attempts,seals,hashes and device counts.
No quality/default promotion. No fullPPL requirement for this speed migration.

## Checkpoint 2026-09-14

See [arithmetic and gate report](../LLAMA32_R3_R4_SP2_CHECKPOINT.md).
Paused for decode ideal cosine gate discussion; active lock retained.

## Authorized diagnostic continuation
User approves the proposed low-amplitude/SP2-threshold diagnosis (2026-09-14).
Keep current weights, scales, native implementation and numerical gates frozen.
Inspect all64 prefill rows plus decode row in each already-prepared layer0/7/15
fixture, obtain layer7/15 native single-layer audits as diagnostic samples only,
and repeat layer0 once for reproducibility. No consecutive-layer/fullmodel or speed
gate advancement. Trace FP16 factor rounding, SP2 threshold crossings, exact signed
Down dot, Q31 output rounding, Q14 residual and dequantized output energy/cosine.
Contrast per-row with pooled cosine and quantization floor; any proposed gate change
remains a discussion item. Keep previous failed gate and sealed evidence immutable.

## Diagnostic outcome

See [complete diagnosis](../LLAMA32_R4_SP2_DIAGNOSIS.md). User-authorized diagnostic
continuation complete; original gate unchanged, further progression pending discussion.
