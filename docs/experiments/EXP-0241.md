# EXP-0241: LPBQ32 exact direct-W4 accumulation

User approved2026-09-08 algorithmic reuse of HMX4bit with exact grouped semantics; threshold unchanged. Parent EXP0240 sourcee02d01c0d40603bb80fcc9e292d106a7d72be0b9. ProtocolPC056.

For integer multiplier m in1..16, q*m = sum_b 2^b*(q if bit_b(m) else0). Construct five packed-W4 masks in bounded VTCM, reuse each mask2^b times in the same HMX accumulator, and convert once with unchanged bias. This has no S8 materialization and exactly preserves LPBQ integer accumulation; it trades expansion for masked-W4 buffering and up to31 HMX passes. No claim that it is the optimal algorithm. Use SIMD mask generation and double-buffered scheduling where current arena permits. Freeze all LPBQ/control values and independently verify masks, raw integer sum, native conversion and complete layer against EXP0240. Audit timing is separate.

Primary comparison is equivalent-scope per-channel direct-W4. Retain LPBQ S8-expansion as secondary semantic/performance control. Five short then ten alternating formal rounds for repeat1/repeat10, paired bootstrap50000 seed241, no outlier deletion. Stable>10percent in either complete Host wall stops full-model. Near gate allows10additional pairs, unresolved pauses. Full model requires both within threshold. Quantization quality is not measured; no E2E extrapolation. All metadata/masking/extra HMX included,8MiB VTCM,zero intermediate DDR/spill,one RPC. No baseline promotion.

State: completed, evidence valid, speed gate fail, adoption pending. Full model stopped before starting. Results /mnt/d/llm_exp/results/qwen3-block-htp/exp0241; model artifacts /mnt/d/llm_exp/models/qwen3-block-htp/exp0241. Existing LPBQ packages and all EXP0240 evidence immutable.

Closure and current handoff: [results](EXP-0241-RESULTS.md), [complete profile](EXP-0241-PROFILE.md).
