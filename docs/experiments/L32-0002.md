# L32-0002: W4A16 functional integration

User authorizes fresh original Llama W4A16 then W4A8, deferring performance
optimization. This experiment owns W4A16 only. W4A8 follows a separate experiment.
Preserve signed [-7,7], one scale per output channel, fresh C64 GPTQ method
calibration, no Qwen payloads or grouped weights. Original tied embedding remains
FP16 lookup, LM head quantized W4 as inherited recipe; no silent mixed precision.
Validate quantized arithmetic independently at single layer, consecutive layers,
then full text and PPL against directly loaded original BF16 teacher. Freeze a
larger independent EN/ZH heldout set before quantization/selection. Existing PPL
thresholds overall5%, each language/domain10% remain reportable quality criteria;
if failed report honestly, do not call usable or select on heldout. Routine owned
repairs allowed, retain failures. No performance acceptance claim or formal
profiling required; warm speed auxiliary if collected. Physical/numerical checks
remain mandatory. Common code may propagate after validation with ownership
transfer. Next W4A8 has no model-quality threshold per explicit user instruction;
arithmetic/physical gates and reporting actual output/PPL remain applicable.
