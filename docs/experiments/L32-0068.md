# L32-0068: Table A A8 / INT16 Down matched comparison

User requests three Table A model pairs; this experiment covers only Llama3.2-1B. Qwen follow-up separately registered and serialized. Llama3B excluded. M64+42 fixed token trajectory, full16 layers, native W4 per-channel, FP32 residual/Norm, no rotation. Two arms ordinary uniform A8 Down and uniform INT16 Down. Same current integer binary, weights, non-Down quantizers, token trajectory, KV capacity, runtime pipeline/layout/softmax and all eligible shared optimizations. Only necessary Down input quantizer/LUT and one/two byte MAC mode differ. Use fused production mask0; no residual ablation scratch reservation. AV scale folding must pass original saturation check independently for A8 trajectory before using it on both arms; otherwise preserve common valid-row standalone RQ and record limitation. Never bypass Qwen saturation rejection. Reuse historically sealed same-arithmetic references only after payload verification; new A8 references independently generated. Validate selected/three/full layers and complete fixed trajectory, deterministic padding, exact independent arithmetic, integer bounds,8MiBVTCM/no timed intermediateDDR/spill, one HMX owner.

Five short paired AB/BA repeat10 then ten formal paired AB/BA repeat10, common warmup, no optional stopping; repeat1 auxiliary. Record full Host wall prefill64/decode42 and additive module counters, Down and GateUp/SwiGLU attribution; overlapping counters not critical-path shares. Report paired bootstrap20000 across round means. Do not attribute cross-run speed changes to code without paired proof. Repair only identified missing common optimizations, no arbitrary specialization or quality claim. Recovered retired payloads must verify against existing manifests; no replacement historical hashes. Source/memory normal clean commits/pushes and preflight before build/device. Results /mnt/d/llm_exp/results/llama32-htp/l32-0068. User authorizes update rebuilt workbook A corresponding two rows after successful measurement; preserve other edits and all original evidence. No Selected baseline promotion implied.

## Completion
See L32-0068-RESULTS.md. All scoped pairs completed; own-reference exact/physical gates pass. Full E2E and module evidence archived; rebuilt workbook updated. No baseline or quality promotion. Source 399cc083f6c586e91695edef2d64c4105e3c4da6; evidence ledger 651f24415c1b96de3bf60d3a91d0101f680fac8a40fe2bf472c9ca6e1bc87fc2; device released.
