# L32-0058: SP2 versus uniform INT16 Down, Llama3.2-3B
User2026-09-20 requests two representative configurations beyond EXP0284/L32-0040 historical M64+15. This phase owns Qwen0.6B native64+42; companion L32-0058 will own Llama3B741+3 after device release.
Reuse current no-rotation SP2+FP32 residual runtime; same binary and flags for both arms. Freeze W4 codes/scales, all non-Down quantizers and token fixtures. Only Down input LUT and middle reconstruction scale change using EXP0284 symmetric INT16[-32767,32767] ties-to-even and the same nominal clipping endpoints. No DSP optimization, calibration/PPL, source default or baseline promotion.
Allow immutable new packages using verified hardlinks; never overwrite a linked parent file. Independently exhaust65536 LUT pairs per layer, raw signed24 and reconstructed signed32 bounds, signed16 decomposition. Actual-operand hardware audits check changed Down arithmetic on selected layers/continuous3 and full model, both prefill/decode, using independent INT64 dot plus exact FP32 scale/add reference. Verify unchanged upstream boundaries and original SP2 against sealed reference. No comparison requires SP2 and INT16 outputs to be equal.
After numerical/physical gates, one warmup and repeat1 auxiliary,5 short paired repeat10 rounds then10 formal AB/BA repeat10 rounds for fixed64+42. Retain all timing, no optional stopping. Record complete Host wall, additive module ledger, HMX counts/DMA bytes/VTCM. Paired bootstrap20000 fixed seed304058; report both-direction10percent closeness (CI contained in[1/1.10,1.10]), exact ratios also mandatory. 8MiB,oneHMXowner,oneRPC/token,no timed intermediateDDR/spill unchanged.
Timing uses the same verified current binary in both arms; archived binary reuse allowed with pinned source/hash and explicit provenance, never claim historical binary was rebuilt at currentHEAD. Routine recovery PC037. No workbook overwrite needed; supplementary comparison report.

This Llama phase owns Llama3B741+3, not Qwen0.6. Offline preparation may overlap Qwen measurement; no device operation before EXP0304 releases device. Use verified L32-0054 3B binary and long option8095 as explicit archived latest measured 3B SP2 control; later source work addresses other recipes/model sizes and is not claimed as this binary's provenance. Frozen long package741 cache832, all other flags identical between SP2 and INT16. Full independent causal744-row CPU oracle validates all chunk/decode layer hashes, KV and head against each representation's own contract before timing. Five short and10 formal repeat10 paired rounds. Only metadata generation/helpers; no source kernels or quality/default changes.
