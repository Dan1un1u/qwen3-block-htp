# L32-0001: original Llama 3.2 1B Instruct W16A16 port

Approved 2026-09-10 by user instruction to proceed in the proposed order.
Owner: /home/daniuniu/work/llama32-htp, codex/llama32-no-rotation.

## Protocol
1. Seal six original files against ModelScope SHA256 and record model semantics.
2. Implement a Llama-specific reference/export path; compare independent layer
   arithmetic with Transformers using original tensors and FP16-converted tensors.
3. Adapt native dimensions, GQA 4, head dimension 64, no Q/K norm, epsilon 1e-5,
   llama3 RoPE, 16 layers, 8192 FFN, vocabulary 128256 and tied embedding/head.
   Keep raw BF16 files untouched. Build under isolated SDK; inspect VTCM/tile
   bounds and reject unsupported paths explicitly.
4. Single-layer hardware prefill and decode with historical KV, then 2-3 layers,
   then full-model text and independent heldout PPL. Freeze fixture definitions
   and numerical thresholds before hardware scoring; retain all failed attempts.
5. Record actual scope and timings. No throughput extrapolation, no quality
   promotion, no W4 or A8 evaluation before this floating port is validated.

Results root: /mnt/d/llm_exp/results/llama32-htp/l32-0001.
Generated packages root: /mnt/d/llm_exp/models/llama32-htp/l32-0001.
Current status: registered; implementation not yet started.

## Progress checkpoint

Original six model files SHA256 sealed in tools/llama_reference.py; model descriptor
filled from downloaded config. Independent FP32/FP16 eager oracle checks for all
16 layers at offsets 0/8192/32768 and decode with past64 pass. FP16 teacher text:
The capital of France is Paris. This is host output, not device generation.

Native Llama build ABI128 implements hidden2048/FFN8192/Q32/KV8/head64/layers16,
llama3 precomputed RoPE, no Q/K norm, eps1e-5, attention scale1/8 and GQA-aware
scan scratch. A8 and head128 optimized GQA/QKV carriers explicitly rejected.
Initial Llama RoPE-only helper is scalar DSP arithmetic; no CPU fallback.
Raw Q/K norm slots and qparams remain unused ABI padding, not model parameters.

Device layer0 prefill a01 failed NaN: native RMS input carrier was reused for Q
but serial K/V incorrectly read uninitialized row-major normalized storage.
Fixed K/V to reuse the existing packed norm carrier. Failure retained.
Device layer0 decode a01 passed floating output metrics but old scan CLI required
byte equality to independent floating reference. Llama scan now uses existing
composition_v2 constants (NRMSE<=0.003, cosine>=0.99999), cache mixed-bound fraction
<=0.01, and exact untouched prefix/padding. Raw mismatch counters retained. This
is reference-type routing, not a widened numerical tolerance. No A8 changes.

Accepted local attempts: device-layer0-prefill-a03, device-layer0-decode-a02,
device-layer7-prefill-a01, device-layer7-decode-a01, device-layer15-prefill-a01,
device-layer15-decode-a01. Output NRMSE 0.0000468..0.0011197.
Device-stack3-a01 uses unmodified FP16 composition_v2 replay gate: prefill NRMSE
0.00100938, decode0.00175108, no prefix/structure mismatch, exact8MiB VTCM, no
intermediate DDR read/write or spill. Full model still pending, no PPL or
performance acceptance. The raw runtime legacy experiment labels are historical
identifiers; L32-0001 protocol.json binds actual source/build/package identity.

## Validated model and common-branch propagation

Full16 replay passes: prefill NRMSE0.00232789, decode0.00166363. Native KV
prefill for serial attention initially failed (device-frontend-a01); added
explicit VTCM packing into the persistent native cache. Device-frontend-a02
generated Paris then stopped on misnamed generation RoPE file. Added byte-identical
generation aliases in frontend-a02, preserving frontend-a01 and its manifest.
Device-frontend-a03 passes16 exact FP16-reference token matches and all128 NLL
steps. Text: Paris is the capital of France. Original BF16 teacher was reloaded
directly from safetensors, correcting an intermediate FP16->BF16 roundtrip
teacher; retain the earlier result but use validation_summary.json for final
BF16 comparisons. Final PPL device25.1047703/original25.0220618 (+0.33054%).
English+0.31575%, Chinese+0.34534%. Functional M64+15 speeds395.4202/5.7742 tok/s
are auxiliary, not formal profiling. No overall quantization quality promotion.

The owner switches to /home/daniuniu/work/llama32-htp-rotation solely for the
authorized common-code propagation, after no-rotation validation and all device
work finished. Source no-rotation a5d2e8ca3f2f98787b36652db3d8f6f7e74d11f5 clean/synced.
Cherry-pick only common changes since organization; preserve config/branch.json
R3 default and keep A8 unvalidated. No repeated hardware experiment on identical
source. Then verify both branches, seal evidence, close this experiment.
