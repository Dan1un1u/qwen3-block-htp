# EXP-0280 decode layout optimization

User explicitly authorizes this optimization on 2026-09-15 after the completed paper campaign. Parent f8b0a7508876ac26f8191eea55c9adc86ae7954e. Initial existing branch binding permits creation of codex/exp-0280-decode-native-layout after successful preflight, then authority rebind before edits/builds.

Sequential roadmap: (1) diagnose native-versus-compact decode boundaries on Qwen and Llama, optimize exact implementations or select compact only for decode if faster; (2) optimize Llama log2 against same-build FP vector control; (3) bounded full-model generalization on latest optimized versions. Register later phases under this explicit user authorization without repeated permission. Do not rerun entire factorial. Historical evidence remains immutable; no PPL/quality or automatic Selected Baseline promotion.

EXP0280 only handles Qwen decode. Freeze native W4, SP2mode8, FP32residual2, wide4, no rotation, original weights/scales and prefill schedule. Diagnose individual Norm bit1 and SwiGLU bit2 format switches with pipeline enabled. At most three exact native implementation candidates, preserve original same-binary control and compact consumer. No artificial work, no arithmetic/threshold changes, no hidden intermediateDDR, same8MiB/oneHMXowner/oneRPC. Inherited old numerical evidence cannot replace new movement validation. Independent selected0/14/27, chain3 and full paired output/head gates before formal. Use targeted component/layer diagnosis, then fixed5short10formal alternating paired repeat10 full M64+15 for final eligible candidate vs original, repeat1 auxiliary. Retain all attempts. Timing deltas near noise remain inconclusive; do not tune on outliers. If compact objectively wins, select decode-only compact and preserve prefill native. Report modules and actual E2E; scope-limited local ablations are authorized.

Llama and device are idle at entry. EXP0280 owns device while active device runs; no concurrent model profiling. Existing 8MiB numeric/physical gates mandatory. Generalization after both optimization stages may add necessary exact runtime shape support; target two longer prompts and continuous >=128decode where VTCM permits, never extrapolate unsupported shapes. Final bounded protocol freezes fixtures/shapes before timing.
