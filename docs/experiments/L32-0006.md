# L32-0006 authorized pipeline iterations

User requests several speed iterations, ideally near historical Qwen3. Own Llama
no-rotation W4A8 OFF; preserve original weights/qparams, exact arithmetic,8MiB,
no timed intermediate DDR/spill and single HMX owner. W4/W16 quality/rotation
status unchanged. Qwen frozen manifest is read-only historical comparison,
not a matched model benchmark or permission to resume Qwen research.

Bound three rounds: (1) prefill nonlinear/LUT work, (2) native KV layout conversion,
(3) reuse direct-W4 DMA/MLP continuous scheduling and batching. Inspect source
and use targeted device evidence to choose concrete implementations. Preserve
failed candidates and numerical gates. Test each relevant change single layer
against sealed independent integer reference, then3/16 composition and full
8/16 generation before final profiling. Padding/scalar equivalence audits where
changing sparse row or vector operations. No PPL rerun if all codes identical.

Archive L32-0005 candidate16 as immutable baseline. Intermediate timings are
auxiliary for bottleneck attribution; do not veto solely on repeat1. Freeze final
candidate before fixed10 rotated3-arm complete-model M64+7 runs (L32-0005 A8,
new A8, W4 OPT2 reference). Paired bootstrap95 upper<=1.10 both stages remains
performance gate, no optional stopping. Additional M64+15 A8 fixed10 paired
runs compare continuation sensitivity with old A8; Qwen historic M64+15 numbers
remain non-paired different-model reference. Report additive module tables and
real warm E2E, loading/tokenizer separate. Propagate common code only after owner
transfer and verification, seal all attempts, both source heads and memory.
