# EXP-0290 — Qwen0.6 W4A16 staircase-speed diagnosis
User asks to investigate why W4A16 is slower than F16 and expects increasing speed with lower bitwidth.
Read-only audit identifies primary excess in QKV and secondary decode native KV packing. Existing OPT2 and head prefetch are active; both A16 paths retain M64-sized projection/common work at decode.
Authorize bounded existing-config diagnosis: unchanged EXP0289 fast binary and weights, canonical versus head-aligned QKV batch4 and smaller region8/16 (up to4 diagnostic candidates). Same fixed M64+42/cache128 token trajectory, repeat3 diagnostics, own token/logit exactness and all physical/cache/ledger invariants. Failed options retained. No deliberate F16 slowdown, no W4A8 or other model modification, no new numerical math, PPL or baseline promotion. Prior user authorization permits provisional speed with known full-reference failures retained.
If candidate clearly improves, independently compare untimed full hidden/norm/cache dumps bytewise and run5short10formal repeat10 balanced order against original W4F16, primary complete Host wall. Diagnostic repeat1/3 not a final baseline. Freeze F16 historical reference; report whether staircase actually restored rather than presupposing it.
Source config/harness edits only within approved diagnosis; native kernel changes require concrete findings and remain bounded/no deep tuning. PC037 autonomous recovery applies. Results isolated exp0290; reuse exact immutable exp0289 models and archived binaries.

## Diagnostic refinement
The archived CLI rejects region8 with the selected adaptive pipeline before computation. Preserve diagnostic/region8 as invalid-configuration evidence. Head-aligned batch4 passes exact token/logit checks but gains only about 1.4% decode in repeat3. Replace the invalid region candidates with existing row-major QKV boundary (norms) and head4 + row-major QKV to isolate conversion cost, up to four valid diagnostic configurations total. No native math change. If any numerical output changes, retain and reject that candidate; no relaxed equivalence.
