# EXP-0161 — Long-KV scaling and segmented HMX-native W4U8 cache

## Decision and purpose

EXP-0160 is the user-promoted Selected Full-Stack W4U8 Cache-Native Baseline.
It overlaps compact-base DMA and HVX tail reconstruction across GQA groups and
reduces positions 64-71 decode to 49.042 ms/token. That evidence is bounded to
an eight-token delta journal and cannot establish long-context behavior.

EXP-0161 first measures the selected monolithic reconstruction design at
logical KV lengths 64, 256, 1024 and 4096. If full-length reconstruction or
the two-slot overlay becomes material, the experiment implements an immutable
32-token HMX-native segment format plus one compact mutable tail. QK and AV
then consume segments without constructing a total-length carrier in VTCM.

## Execution and attribution boundary

The immutable source parent is EXP-0160 commit
`b33e88e3971c35851450d0737a0689bd0bd077aa`. Each point is a synthetic single
real layer-14 decode snapshot, not continuous decode and not a full-stack
throughput claim.

During the formal audit, EXP-0147's current-token K/V reference was found to be
an accepted device dump rather than an independent host reference. The
segmented and monolithic implementations agreed with each other but both
disagreed with that stale dump. A diagnostic Attention-boundary export proved
that the current DSP Q, K and V agree byte-for-byte with the independent
HMX/HVX exact simulator, and that segmented-cache append agrees byte-for-byte
with those DSP boundaries. EXP-0161 therefore republishes only the
current-token K/V row and resulting block output from the independent exact
host simulator. The inherited synthetic prefix remains unchanged test input.

Phase A converts the existing row-major fixtures into the accepted compact
M64 base plus delta-journal representation and characterizes serial and
two-slot reconstruction. Phase B is activated only if the evidence shows a
material scaling or VTCM-capacity limitation. Its sealed segments are
immutable; only the current sub-32-token tail is appended or sealed.

W4 values, asymmetric U8 qparams, QK/log2-Softmax/AV mathematics, projection
kernels, output contracts and the single HMX owner remain fixed. A16 recipes
are outside this experiment.

## Gates

Every measured length must match the independent block output and current
token cache append with zero U8 LSB error. Formal runs require exactly 8 MiB
VTCM, zero intermediate DDR, zero spill/fill, one FastRPC invocation per block,
one HMX owner and no QNN.

The segmented candidate may not construct or clear a total-length K/V carrier
or perform a full-prefix pack. Its Attention scratch must be bounded by segment
size rather than total KV length; sealed segments remain immutable and only the
active tail may change. It must not slow L64 and must beat the fastest valid
monolithic control at L256, L1024 and L4096 to pass the speed gate.

The speed statistic is the median of five within-round candidate-minus-control
repeat10 differences. This paired statistic preserves the formal alternating
run order and avoids classifying sub-microsecond L64 noise by two unrelated
sample medians. The table still reports the independent medians for context.

Any correctness or physical-contract failure stops candidate integration
before it reaches the full-stack runtime. Passing this micrograph does not
change a Selected Baseline without another explicit user decision.

## Decision state

Execution completed at source commit
`efea24712241dbcb62f468665b98c758c5ef5461`.

Formal evidence:
`D:/llm_exp/results/qwen3-block-htp/exp0161/20260902T191200Z_efea24712241_phase_b`

All gates passed. There were five valid alternating runs in every repeat1 and
repeat10 cell, no unavailable runs, no device reboot, exactly 8 MiB VTCM, zero
intermediate DDR, zero spill/fill, zero full-prefix pack and a closed tick
ledger. Candidate block output and current K/V append are byte exact at L64,
L256, L1024 and L4096. The L4096 monolithic HMX control differs from the exact
host output by at most 2 LSB, while its cache append remains byte exact; it is
retained only as the timing comparator. The segmented candidate remains byte
exact.

| Logical KV length | Monolithic control | Segmented candidate | Paired speed change |
|---:|---:|---:|---:|
| 64 | 1802.604 us | 1807.953 us | +0.038% |
| 256 | 3022.594 us | 2226.078 us | +35.260% |
| 1024 | 7869.089 us | 3798.958 us | +106.612% |
| 4096 | 27280.359 us | 9291.208 us | +193.865% |

The L64 paired median is 0.693 us faster even though the two independent
medians differ by 5.349 us. At L1024 and L4096 the candidate uses the same
fixed 2,641,920-byte Attention overlay, proving scratch does not scale with KV
length.

On 2026-09-03 the user explicitly promoted EXP-0161 as the Selected W4U8
Long-KV Segmented-Cache Baseline. This is a layer-14 long-context baseline and
does not silently replace the different-scope Selected Full-Stack W4U8
Baseline, which remains EXP-0160 until the segmented lifecycle passes a
28-layer continuous-decode experiment.
