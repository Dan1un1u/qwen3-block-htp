# EXP-0246 — Online R3 using dense matrix multiplication

User2026-09-08 authorizes online R3, explicitly requiring naive dense matrix multiplication rather than butterfly/FWHT computation. PC061. Parent df75945f9a14a7c5ab4f32dd1db27161e865a476 and EXP0245 ledger8a59e580907adbf35a440460f49d4753f73e2273a18704ab5022a10099fc5bff. Target branch codex/exp-0246-dense-online-r3, temporary parent binding only for clean branch setup.

## Fixed algorithm and roles

Preserve verified C64 per-channel W4 manifest7de4f0758d83f2ba3b58696c695bfbfed72a25dd3bf308475abee0a0f0575a89, F16 manifest0f8a359b559f252cb13329f57d4cabd56f17ca9bfb64bc5643f4d6014795070f, EOS151645, EXP0244 shared residual fanout, actual U8 cache and all non-Q/K EXP0243 MSE parameters. No weight changes, training, grouping, LPBQ, R1/R2/R4 or baseline promotion. Fixed Sylvester sign matrix S128[i,j]=(-1)^popcount(i&j), H=S/sqrt128. Online implementation is explicit dense GEMM (FP32 accumulation software, normalized before FP16 handoff); no butterfly, fast Hadamard transform, learned matrix or seed search. Same H after RoPE and before Q/K quantization for every affected Q/K head. Prefix K transformed exactly once into the same basis; no cached-history retransformation. Original raw prefix preserved, V unchanged.

Scopes L0 and ALL28. Frozen 128x128 EXP0243 calibration rows, no evaluation calibration. Collect raw unquantized Q/K in identical batch4 prefill64 plus64 single-token steps; include prefix K extrema. Recompute only q_rope/k_cache MSE parameters using unchanged bounded histogram/scale chooser. Compare existing A8, unrotated matched recalibration at L0 and ALL, and R3 plus recalibration at L0 and ALL. All other parameters remain byte-equivalent. No clipping hyperparameter search. Record distribution tails and quantization MSE before/after transform.

Exposed development: EXP0245 development32 docs/512targets, exact order. Freeze development/configuration/parameter maps before scoring. Development9: F,C64,A8,recal_L0,recal_ALL,R3_L0,R3_ALL,C64_R3_L0,C64_R3_ALL. The last two are unquantized activation numerical controls only; they do not modify frozen W4A16 recipe. Freeze final7: F,C64,A8,recal_L0,recal_ALL,R3_L0,R3_ALL, no selection or candidate changes from scores. Fresh final seed246,256docs/4096targets balanced four en/zh wiki/news cells, disjoint document/text/32-token overlap through EXP0245 and all calibration. Verified retained corpora preferred; bounded Wikipedia offsets94000..104000 if necessary. Identical batch4 prefill64 plus15 real cached steps, no bulk scoring, no prefix loss.

## Checks and evidence

Dense matrix generation independently checked by NumPy parity; H H^T identity FP64 maxerror<=1e-12. Dense FP32 multiply against FP64 direct matrix oracle relativeRMS<=2e-6, preserve unquantized paired attention within relativeRMS2e-5 in FP32. No transform branch exactly reproduces parent F/C64/A8 per-token development results. A16 R3 controls report logit error and development deltaNLL; require finite values and abs(delta meanNLL)<=0.005 versus C64 before final candidate scoring. Failures diagnose implementation without loosening thresholds. Independent U8 versus same-shape floating-QDQ cache, prefix-once transform, per-layer dtype/length/append, repeat/causal/independent CE, full weight digests and unchanged remaining parameters required.

Report seven final PPL and four-cell PPL, paired stratified-document bootstrap10000 seed246. Compare each R3 to matching unrotated recalibration and original A8/C64/F; distinguish calibration and rotation effects. Existing overall5%/cell10% confidence gates unchanged. Software improvement for progressing a device candidate requires nominal95% upper PPL ratio<1 versus both A8 and matching recalibration. If neither scope meets this, close software evidence and discuss rather than spend device work on a non-improving method. Software results are not DSP arithmetic or speed evidence.

User authorizes conditional dense-GEMM HMX integration and one-real-layer M64/M1 performance work after software improvement, registered separately with exact device arithmetic/packing gates and frozen parameters. Preserve native HMX W4 and no S8 expansion of W4. Include online rotation, packing, metadata, normalization and cache costs in full layer Host wall. Existing five short/ten paired formal rounds and >10% stable prefill OR decode regression stop rule remain; no full-model escalation before this gate. Other recipes stay frozen. Complete report, applicable/N-A profiling, evidence hashes and project-memory closure. PC037 attributable autonomous recovery applies; no auto-promotion.
