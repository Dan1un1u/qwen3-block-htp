# L32-0004: native pipeline speed reuse

User now authorizes speed optimization, superseding earlier deferral. Own
no-rotation, both quantized recipes, unchanged sealed Llama weights/qparams and
quality conclusions. Restore existing W4A16 OPT2 launch selection; implement
head64 RoPE using existing HVX arithmetic/carriers and avoid dead decode rows.
Reuse safe existing DMA/HMX/HVX pipeline options where dimensions permit.
No new rotations/grouped quantization/quality tuning or Qwen3 changes.

Before changing binaries preserve current16-layer builds with hashes. Validate
single layer then3/16 and feedback generation. A8 exact output/KV/token/code
gates retained, W4A16 existing composition gates plus unchanged baseline outputs
where arithmetic identical. Preserve8MiB/no spill/single HMX owner and complete
additive ledger. Any rounding difference must be investigated, not hidden.

Use fixed10 paired runs per recipe (alternating AB/BA) after correctness, same
package and continuous generation counts: W4A16 M64+7 to EOS, A8 M64+15. Preserve
all pairs, bootstrap95% CI upper <=1.10 for prefill and decode slowdown gate;
repeat1 diagnostic only. Report module Host-wall shares and real E2E throughput.
Quality threshold remains failed for W4A16 and absent for A8; no promotion.
Full PPL rerun only if arithmetic changes warrant it. Propagate accepted common
code after ownership transfer, seal results and update reproducible launchers.

## Completed result

Both recipes pass all numerical/physical checks and fixed10AB/BA paired speed
gates. Native head64 HVX RoPE and sparse exact A8 code repair, existing prefill
head workers, head64 K masked scatter and V LUT/native delta packing reused.
W4 OPT2 generalized from two to four Q heads. No weights/calibration changes.

W4 prefill436.6657 ->1261.0661tok/s, decode6.35436 ->23.44369;
A8 prefill215.8940 ->1079.7596, decode2.57298 ->16.47835. Candidate/baseline
Host-wall95% CI: W4 prefill[.344949,.347405],decode[.270094,.271933]; A8
prefill[.199314,.200660],decode[.155545,.156634]. All upper<=1.10.

W4 three/16 outputs byte-identical and numerical metrics unchanged; A8 exact
1/3/16 output/KV; all formal generated IDs and selected logit codes equal baseline.
W4 OPT2 independent conversion audit passes; W16 full16 shared-code regression
is byte-identical. All480 timed ledgers reconcile. No timed intermediate DDR/spill,
exact8MiB, native W4 and single HMX ownership retained. No PPL rerun/promotion.
55 successful native host processes /548 token boundaries, of which40 formal
processes /480 timed boundaries. One failed compile (duplicate local identifiers)
fixed without changing gates; log retained. Both binaries and all295 evidence files
sealed; source heads and hash in status/index. Runtime ownership transferred before
common propagation; only config/branch.json differs. No background jobs remain.
