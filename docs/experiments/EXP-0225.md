# EXP-0225 — Learned offline R1/R2


## PC-045 — Learned offline R1/R2 for W4F16 (user approved 2026-09-06)

EXP-0225 implements the user-approved SpinQuant no-online-Hadamard learning plan: freeze original Qwen3 weights; jointly learn global R1 and per-layer value-head R2 using Hadamard initialization, Cayley SGD/Stiefel updates, next-token CE and W4 per-output-channel STE fake quantization with signed [-7,7] and FP16 execution semantics. Adapt official code with pinned provenance to Qwen3 Q/K norms and GQA. Re-fold original checkpoint including embedding/final norm/head for each rotation; no upstream folded weights or LPBQ/grouping. Keep other recipes, DSP runtime, physical contracts, and GPTQ8192 calibration frozen. New fixed independent bilingual rotation-training and checkpoint-validation text is authorized, with document/token overlap checks and hashes before training; qbh-lite-v1/holdout cannot train or select checkpoints. Bounded100steps, predetermined checkpoint schedule, actual GPTQ plus EXP0224 output-scale export for validation. Compare final candidate to EXP0224 A and fixed-rotation controls. Verify orthogonality, unquantized invariance, independent quantization/packing and software/device quality; eligible device candidates receive5short/10formal profiling. Routine implementation/environment recovery follows PC037. No automatic promotion or further optimization direction.

Implementation begins from EXP0224 cf8a239a636bb33dd9bf49c7aa03760df9af6c81. Training/data/checkpoint details must be fixed before gradients or validation results. Retain initial rotation and every selected checkpoint, optimizer/RNG state, upstream provenance, independent equivalence/oracle checks, actual-export validation, and final DSP evidence. Training loss alone cannot establish effectiveness.

## Frozen learning protocol (before gradients)

```json
{
  "steps": 100,
  "batch_size": 8,
  "sequence_length": 128,
  "seed": 225,
  "learning_rate": 1.5,
  "lr_schedule": "cosine",
  "checkpoints": [
    0,
    50,
    100
  ],
  "training_samples": 800,
  "validation_samples": 32,
  "quantizer": "STE_RTN_absmax_per_output_row_minus7_to7_scale_FP32_dequant_FP16",
  "export_quantizer": "EXP0224_post_GPTQ_three_output_scales_transformer_GPTQ_absmax_head",
  "rotation_compute": "FP32_TF32_disabled",
  "trainable": "R1_and_per_layer_R2_only",
  "checkpoint_selection": "minimum_actual_export_bilingual_validation_NLL_tie_earliest",
  "validation_language_guard": "report_each_language_and_regressions_against_unrotated_A",
  "evaluation": "qbh-lite-v1_final_only_never_checkpoint_selection"
}
```

Data SHA256 d7642fef571ab0077ffd3d8ef98670dd8b679ee002072a90bb6de081f012978c. 800 training windows (400 per language), 32 independent validation windows (16 per language), length128. Same-length windows are deliberately shorter than official2048; this is a resource-bounded Qwen3 adaptation, not a full paper reproduction. Document split and32gram exclusion against calibration, qbh-lite full/holdout verified. Train uses absmax STE as the differentiable surrogate; actual checkpoint export uses EXP0224 transformer output-scale GPTQ and EXP0221 absmax GPTQ head. No online R3/R4. Original embedding/head refold for each checkpoint. Official optimizer pinned at8f47aa3f00e8662caf1a484153920a07e5281c3a. GPU environment isolated from CPU reference venv.

Recovery: Hugging Face429 resumed from retained raw pages. Dynamic-versus-folded FP16 logits initially NRMSE0.00303065; original BF16 values were prematurely rounded to FP16 before rotation. Keep original BF16 buffers through FP32 rotation to final FP16 reconstruction. Weight differences and initial failure retained in dynamic_fold_diagnostic.json. Full-model gate rerun without changing threshold.

Before any gradients or validation scores, data coverage check found initial16English validation windows came from1document. Retained initial data as learning_data_initial_low_document_coverage.json; changed selection to16distinct documents per language and refroze at SHA256 bcb67126349e8d086f29619807e9ab7ebba06253792b7fce4e5468f9d39e8280. Sample counts, sequence length, train windows, GPTQ calibration, seeds and checkpoint rules unchanged. This is an attributable preparation fix, not score-based tuning or unexplained hash acceptance. Original value preservation makes all196projection FP16 weights identical; matching the existing exported software RoPE inv_freq FP16 buffer makes complete dynamic/folded logits exactly equal. Diagnostics retained; mathematical invariance threshold unchanged.

CE diagnosis correction: aligned2D/3D head shape did not explain the difference. Full FP32 torch cross_entropy=7.215673446655273, full FP64 CE=7.215693485810834, streaming FP64-merged CE=7.215693473815918. Actual loss agrees with FP64 within1.20e-8; the lower-precision CPU reference differs2.00e-5. Repair the new independent checker to FP64, keeping its1e-5 threshold. No DSP numerical/reference thresholds changed and no old evidence overwritten.

## Exact Cayley numerical recovery (before final evaluation)

Original100step run remains under models/exp0225/training and training_complete.json. Its step050 fails unquantized full-model invariance (NRMSE0.027186 English /0.010852 Chinese); entrywise R1 Gram error8.7769e-6 concealed spectral error0.00143381 and singular range[0.999409345,1.000716649]. Official5iteration approximate Cayley plus stochasticQR accumulates measurable constraint drift. Replace that numerical solve with FP64 exact Cayley linear solve using the same tangent, norm-bound step, learning-rate schedule, seed, data, and objective. Add Frobenius Gram error<1e-4 (stronger check), preserve original full-model thresholds. Retain failed result as failed_step050_approx_cayley. Re-run100valid steps under training_exact after an independent NumPy update oracle and smoke_exact. This repairs implementation correctness, not a new hyperparameter search. Existing step000 export remains a valid unchanged-initialization control; verify identical initial rotations across runs. All later exports use training_exact. No final validation/qbh scores have been inspected.

Exact100step recovery completed in360.314s with peak GPU4,218,659,328bytes. Final R1 Frobenius1.5637048699e-5, R2 3.9698871040e-6. Step050 full-model invariance now passes NRMSE2.7403736047e-6 /2.5041601312e-6. Initial raw/exact R1/R2 tensors are identical (initial_rotation_identity.json). Exact checkpoint hashes and source/environment archive retained. Actual GPTQ exports and independent validation are next; no effectiveness claim from training loss.

## Completion - 2026-09-06

All100 valid exact-Cayley steps, actual exports0/50/100, independent validation selection, final software/DSP quality,5short and10two-way formal profiling completed. Selected step100. Validation NLL0:3.355730 ->50:3.297332 ->100:3.266698, still worse than unrotated A3.204854. DSP PPL50.224494 versus A38.873624 (+29.1994%); tasks20/24 versus19/24. Quick/full/repeat deterministic; mathematical/physical/packing gates pass. Evidence valid, local effectiveness gate fails, adoption pending user discussion; no promotion. Final source0900322f71b8cbc6071a3a34e3f23679464bbfeb.334 result files and694 intermediates covered by closure ledgers. See [full results](EXP-0225-RESULTS.md) and [complete profiling](EXP-0225-PROFILE.md).
