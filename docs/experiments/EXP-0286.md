# EXP-0286 — inference DRAM measurement
User authorizes existing W4A8-SP2 inference DRAM peak measurement, VTCM excluded.
Use retained original EXP0284/eaea89c no-rotation SP2 mode8, FP32 residual mode2,
Qwen3-1.7B, fixed M64+15, cache128, latest shared scheduling. Preserve model and
kernel binaries/hashes; no optimization, PPL, baseline or quality promotion.
This external measurement-only experiment stays on the current clean source branch;
the measured sealed runtime is explicitly different from current FP16 experiment build.
Qwen owns device exclusively after checking idle state; Llama unchanged.

Inspect /proc RSS/PSS/smaps, fdinfo/dma-buf and FastRPC accounting permissions.
Measure loaded-inference interval separately from loading; include weights, KV,
host/runtime memory; exclude VTCM and unrelated system allocations. Deduplicate
shared physical allocations by buffer identity and avoid adding counted RSS twice.
Do not claim RSS alone equals DSP/Host combined physical DRAM, nor sum unrelated
component peaks. Unknown DSP/private firmware memory remains an explicit coverage
limitation. Preserve raw snapshots/timestamps and allocation ledger.
Run at least three fixed trajectories for repeatability if device available.
Instrumentation timing is diagnostic, not a new formal speed baseline.
If measurement hooks are necessary, host-only instrumentation may be introduced
with unchanged DSP/weight identity and independently checked selected token/hash
sequence; record the perturbation and original binary control.
Results under D:/llm_exp/results/qwen3-block-htp/exp0286.
Routine tooling recovery PC037; no resource/global cache reset or unrelated app kill.
