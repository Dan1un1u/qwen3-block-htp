# EXP-0297: Qwen3-1.7B decode-focused optimization

User explicitly authorizes focused Qwen1.7 optimization, especially the apparent decode regression versus desktop D history. Freeze Qwen0.6, Llama, all non-SP2 recipes, original weights/calibration, SP2mode8, FP32residual2, no rotation. Initial source binding0296 for preflight, then new codex/exp-0297-qwen17-decode branch.

Distinguish historical M64+15 EXP0284 47.117789TPS from long536+46/741+3. Current0296 native64+3 reproduces47.031907; long64+3 has44.373612, long53639.031403/74137.117252. Do not describe the whole length difference as code regression. Use same-binary control927 and untouched native-short path, fixed identical token trajectories. Add matched64+15 checks to connect the historical scope without replacing sealed historical data.

Bounded up to four engineering candidates, starting with existing valid-row AV requantization and eliminating overwritten clears; then exact V packing/LUT and K layout preparation, finally release granularity/native-cache scheduling if justified. Shared prefill fixes may include proven partial-tail parallel Norm. No arithmetic/tolerance/weight changes or hidden extra DDR. Any persistent cache layout must be explicitly versioned, accounted and independently decoded/verified; never silently reinterpret a canonical cache. Failed attempts preserved. Selection by geometric mean of decode536 and741 wall among numerically valid candidates, with matched prefill wall upper95<=1.10 for final candidate; no raw best-run selection or repeated formal sampling of failed frozen candidate.

Prerequisites: frozen prior actual-arithmetic independent reference, all layer/KV/head checks with poisoned future storage at64/65/128/129/536+46/741+3; add extended64 decode reference where needed. Exactly8MiB, single HMX/DMA owner, no timed intermediate DDR/spill, one RPC per fullmodel chunk. Existing gates unchanged. Final selected candidate: five short cycles then ten rotated formal repeat10 cycles for control/candidate536+46,741+3 and matched-short references. Prefill parity lower95>=1 against fastest same-binary64 remains separately reported. Decode10percent guard and additive elapsed module ledger retained. All samples retained; no PPL/quality or automatic baseline promotion.

Update only relevant Qwen1.7 B/C workbook rows after formal evidence, append prior rows to D; historical M64+15 remains historical. Preserve manual workbook edits. Commit/push normal source and authority changes; routine owned repairs perPC037.
