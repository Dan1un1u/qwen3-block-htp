# EXP-0242 — Fixed C64 static A8 localization and clipping

User approved 2026-09-08 the first direction only: fix the best retained per-channel W4 package, locate activation error, test static clipping and visualize typical distributions. Protocol PC057. Parent EXP0241. Software diagnostic; no DSP/device work or baseline promotion.

## Frozen numerical and data protocol (before inference)

C64 manifest SHA256 7de4f0758d83f2ba3b58696c695bfbfed72a25dd3bf308475abee0a0f0575a89, original norms/embedding and frozen W4 LM head. Matched canonical FP16 teacher manifest 0f8a359b559f252cb13329f57d4cabd56f17ca9bfb64bc5643f4d6014795070f. Reuse verified canonical EXP0230 packed-weight loader and CPU-constructed FP16 RoPE buffers; never alter parameters. Original-coordinate signed [-7,7], one scale per output row; no new weights, grouping, equalization, rotation, LET, learning or mixed-precision deployment.

EXP0230 dataset SHA256 1d908de405d4bcf4c46f0acdf4de0ab57cb45b6da298a6dbe28fd4cdb0dff8b0. Calibration = first32 calibration documents in each of four cells, 128 docs x128 tokens. Development = existing128 disjoint docs, 2048 scored targets; primary = existing512 disjoint docs,8192 targets. All historical panels are exposed regression evidence, not newly unseen tests. Calibration and development may choose thresholds; primary cannot. Same64context+16targets, batch4, deterministic eager FP16 software as historical reference, PPL primary and four-cell breakdown. No short-answer tuning. No runtime-to-token/s conversion.

Collect all-layer linear inputs, Q/K/V and O/Gate/Up/Down projection outputs, post-RoPE Q/K, V/cache values, attention probabilities/context, SwiGLU output/Down input, residual stream and LM-head input. Preserve causal masks and exact zero probabilities. Trace per-site min/max, full log histogram, moments, per-channel and per-token outlier statistics and bounded representative heatmaps. Instrumentation disabled must reproduce exact original logits. QDQ uses static per-layer/per-site affine U8 (zero included, integer zero point, round half up, saturate0..255), FP32 arithmetic then original FP16 carrier. Floating GEMMs/nonlinear operations emulate activation quantization only; not exact DSP/HMX epilogue/LUT/cache validation.

Baseline minmax uses calibration extrema. Two bounded clipping policies: MSE chooses independently per site by weighted full-distribution log-histogram error from range ratios [1,.9,.8,.7,.6,.5,.4,.3,.2,.1,.05] and absolute-percentile cutoffs99.9/99.99/99.999; percentile policy uses99.99. Choose combined policy among minmax/MSE/percentile by lowest development equal-cell NLL, ties prefer minmax then MSE. Exact tensor MSE on independent development traces validates histogram decisions; local tensor error need not imply better PPL. Attention probability uses zero-based range; do not quantize mask infinities or renormalize quantized probabilities.

Bounded localization: each of eight families individually under minmax and MSE; all families under all three policies; cumulative first1/2/4/8/14/21/28 layers with minmax for first-error depth; layerwise residual drift and one-step tensor perturbation metrics on fixed development traces. Optional at most two leave-one-family-out runs using worst individual minmax NLL families for interaction diagnosis only, never deploy this mixed activation scope. Families = normalized linear inputs; QKV outputs; O/Gate/Up/Down outputs; SwiGLU; post-RoPE QK and stored KV carrier; attention probability/context; residual stream; LM-head input. V projection output/cache shares a boundary and is quantized once if both families enabled; duplicated identical QDQ is avoided.

Freeze selected complete-scope policy and all threshold hashes before primary. Final panel compares F16, C64 W4A16, all-minmax and selected complete-scope clipped policy (if identical only once). Paired document bootstrap10000 seed242, PPL ratios and cell deltas. Existing overall5%/each-cell10% floating-reference gates may be displayed for context, but this software diagnostic is not device acceptance. Repeat logits/scoring and causal independence checks, independent scalar QDQ oracle, no-parameter-mutation digest, dataset role checks required. Failed attributable implementation attempts retained and repaired under PC037, preserving frozen protocol. Nonfinite model outputs are a diagnostic outcome, never silently filtered.

Deliver standalone distribution/heatmap and PPL/error plots plus archived results, provenance and SHA256 ledger. Profiling and E2E tokens/s explicitly N/A because no new device run. Hardware continuation requires a separately registered experiment and retains >10% single-layer regression stop threshold. Discuss diagnostic findings before selecting another method.

## Closure (2026-09-08)

Completed; evidence valid; software quality gate fail; adoption pending. All 33 score repeat/causal/independent-CE checks pass, C64 parameters unchanged. Calibration and inference implementation repairs retained under PC037; no protocol search changes. Final source 394253ea340e1bdd1ddca1923dc0e9cb0bd9c13b. See [results](EXP-0242-RESULTS.md) and [complete profile](EXP-0242-PROFILE.md). Evidence ledger 5c6e1f92088a9e7ff49aa27cf1ab6a59f13ace3b46ef34f997474ec022d92977. No active work remains; discuss first-position outliers and residual quantization before a new experiment.
