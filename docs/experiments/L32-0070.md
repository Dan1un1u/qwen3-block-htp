# L32-0070: full-model ordinary-A8 HVX butterfly R4
User2026-09-23 requests full E2E continuation after0069. Authorizes opt-in integration of full H8192 HVX butterfly at post-SwiGLU/pre-A8, fresh original Down folding/requantization and full Llama1B16-layer speed measurement. This supersedes0069 component-only/no-new-weights and historical dense-only restrictions solely here. Preserve other recipes/Qwen/default baselines; no quality/PPL or automatic promotion.

Control current0068 ordinaryA8, FP32residual, R3off, AVfolded matching configuration. Candidate same binary/schedule/settings except required R4 production/consumption/Down weights. Candidate Down original BF16 -> normalized H8192 -> per-output symmetric W4[-7,7] RTN. Other weights unchanged. Ordinary single-scale U8 middle, symmetric range derived deterministically from original frozen middle range; no fitting to evaluation. FP16 post-SwiGLU LUT, FP32 butterfly and same explicit ordinaryU8 rounding/native stores as0069. No SP2/INT16 Down and no HMX Hadamard. Four persistent HVX contexts for prefill, one for decode, reuse phase-dead VTCM; no intermediateDDR or new threads per layer. Capture numerical paths untimed. Necessary source/schema/reference repairs authorized without changing gates.

Independent fold invariance before quantization and unpack/bounds checks per layer. Independent actual arithmetic reference covers complete selected layer,3layers,16layers and fixed64+42 trajectory, all outputs/heads/KV bookkeeping,finite/padding/8MiB/oneRPC/token gates. Rounding contract declared, no equivalence to unrotated model claimed. Existing0068 A8 evidence reused only after manifest checks. Hardware exact implementation correctness required, model quality diagnostic only.

Then5short and10formal alternating paired rounds, repeat10, fixed token trajectory from0068, include embedding/allblocks/finalnorm/head/greedy/FastRPC and every online rotation/preparation/quantization/wait. No speed stopping gate since user requests full cost regardless of overhead; report10percent as descriptive reference, not escalation blocker. Repeat1 auxiliary, no optional stopping. Samebinary true E2E, block/module/core wall and overlapping worker counters distinguished. Preserve failures. Device ownership serialized; preflight/source and deployment hashes verified. Archive and close, no requested workbook update. Do not use historical component costs as E2E measurement.
