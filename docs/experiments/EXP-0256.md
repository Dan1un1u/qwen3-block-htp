# EXP-0256 / PC070: independent integer-path R_MSE confirmation

User2026-09-09 approves only the first proposed step: independent confirmation of already-frozen EXP0255 R_MSE, integer attention primary. No calibration, search, training, new weights, changed rotation, grouped quantization, mixed precision or hardware. Original per-channel C64 W4, EOS151645, dense-only R3, wide_nr64 and all other recipes/binaries frozen. This prospectively replaces PC069 dual-path candidate selection for this experiment; no historical change or gate relaxation. Floating attention is diagnostic, not a veto; no new floating arm is needed here.

Fixed four arms: F16 F, matched W4A16 C64, current R3 wide_nr64 A8 B, same path with byte-identical sealed EXP0255 parameters/R_MSE.json. Changes only55 residual_mid/out sites already declared in EXP0255. Frozen candidate before new dataset generation. Copy verified parent inputs/parameters and retain source hashes/numerical proofs; no new parameter artifact values.

New independent256documents,64 per en/zh wiki/news,4096 targets. Same M64 context and16 teacher-forced target positions, EOS prefix, sequential batch4. Dataset seed256; all samples disjoint prior calibration/training/development/final including EXP0255 by document, fulltext hash and token32 overlap. Freeze protocol/data/input/candidate identities before any score. Independent retokenization and direct token32 audit. No optional stopping, reserve expansion, post-final retuning or extra candidate.

Before full panel scoring: retained EXP0255 numerical184-case proof, parent frozen source arithmetic hashes, allowed parameter-change set and exact parent payload identity. New independent NumPy FP32 U8 quantization versus CUDA implementation for every residual parameter using FP16 zero, representable half-step neighbours and saturation extremes, exact output equality. Check zero preservation. Regression controls: F/C64 on first4 of their EXP0255 reproduction rows; B/R_MSE on first4 of respective EXP0255 development rows. Reproduce all target NLLs/top1 exactly with fixed batch4; do not use these exposed scores for selection. Every scored arm repeats first batch, causal perturbation and independent CE<5e-6; actual cache U8 versus FP16 control, rotation counts and shared residual norm-input identity/counts unchanged. Full model weight hashes before/after each phase and immutable EOS prefix. No changes to inherited residual fanout, attention arithmetic or numerical tolerances.

Primary research effect: R_MSE/B paired equal-cell mean NLL difference and PPL ratio on complete256doc panel. Stratified document bootstrap10000 seed256,64documents per cell, nominal95% intervals overall and eachcell. Overall ratio upper CI<1 confirms improvement; lower CI>=1 supports no improvement/worsening; otherwise inconclusive. Report everycell without hiding heterogeneity. This research-effect label is distinct from unchanged model acceptance: overall PPL/F16<=1.05 and everycell<=1.10, point and confidence passes separate. Report matched C64 as weight control, not mathematical lower bound. Do not infer deployed-model acceptance from development gain or one significance test.

After all four arms, independently reconstruct NLL/PPL, identities, exact paired document sets, regression controls and effect/quality decisions; retain complete report/provenance/evidence seal and synchronize source/memory. Software integer-attention proxy is not full-DSP PPL. Existing dense R3 hardware whole-layer gate unresolved. PC027 stable profile table and E2E N/A. No hardware, automatic promotion or follow-on quantizer optimization in this experiment. Discuss outcome first; later work requires separate protocol with unchanged numerical/physical/5short10formal/stable>10percent slowdown gates. PC037 routine recovery preserves failed evidence.
