# EXP-0232: AWQ-style per-channel input equalization

PC051 user ordered approval activates this third phase after EXP230/231 independent acceptance failed. Exactly one E64 candidate, direct control C64=EXP230, C8 descriptive control, original FP16 reference F. No rotation, group quantization, LPBQ, reconstruction training or further optimization direction. Other recipes/runtime frozen; no automatic promotion.

## Frozen method

Pin official mit-han-lab/llm-awq commit d6e797a42b9ef7778de8ee2352116e0f48a78d61, auto_scale.py/quantizer.py/license before implementation. Adapt its activation-informed scale search to our symmetric per-output-row[-7,7] carrier. Calibration for scale search is the first64C64 calibration windows (the nested8192-token C8 subset), with the frozen C64 quantized calibration trajectories as incoming activations. Verify C64 checkpoint hashes from EXP230 artifact ledger. Never use final/development text for scale selection. Reload original Qwen3 weights for each transformation/export; prior quantized packages supply calibration features and frozen non-transformer tensors only.

For each layer, capture QKV input norm, MLP post-attention norm and Down input from the C64 layer on its retained incoming hidden states, with the unchanged calibration FP32 RoPE construction and FP16 layer arithmetic. For each of three families below, compute FP32 mean absolute channel activation. Search exactly20ratios0,.05,...,.95; scale=mean_abs**ratio clamped to1e-4 and normalized by sqrt(max*min). This is the official activation-only search shape, with FP32 offline statistics. Proxy quantization is symmetric per-row absmax RTN. Select the earliest strict minimum FP32 output MSE over all8192positions: original FP16 attention module for QKV, original FP16 full MLP for Gate/Up, original FP16 isolated Down linear for Down. Each search changes only its named weights in the proxy and restores originals after every trial. No extra clipping/search budget, no selection on PPL. Proxy and final GPTQ errors are explicitly distinct.

Fold selected positive diagonals into fresh original FP32 parameters: input gamma /D1 with Q/K/V input columns *D1; post gamma /D2 with Gate/Up input columns *D2; Up output rows /D3 with Down input columns *D3. The last compensation uses the linear Up branch, not a commutation through SiLU. Skip V/O scaling under GQA, matching the official shape guard. Keep Q/K norms, embedding, final model norm and inherited EXP221A W4 head unchanged. After mathematical/FP16 checks, actual E64 export uses unchanged EXP224 CPU GPTQ, act-order, damping.01, final three output-aware row scales and true-sequential65536calibration tokens, exactly matching C64 budget/context128. Original-coordinate per-channel format remains compatible with the frozen DSP runtime.

## Correctness before quality

Require FP64 independent algebra checks for allthree legal compensations, independent NumPy RTN/scale/choice oracle, positive finite scales and exact20trial selection records. Require original versus transformed unquantized FP16 real-layer and full-model checks on the fixed calibration subset: NRMSE<=.003, cosine>=.99999, finite outputs; whole-model checks include final hidden and last-position logits for8balanced calibration windows. Freeze norms/weights as actually exported FP16 for these checks. No threshold changes. Preserve any failure; do not quantize or score a transformation that fails these gates.

Actual export reloads original tensors anew, folds stored scales, performs all196packing roundtrips and112staged-vs-HF forward checks, records28full512x128x2048FP16 hidden checkpoints, row-range statistics, changed norm files and frozen-tensor hashes. Recompute expected semantic generation tokens for any actual device package. Exact control development regression F/C8/C64, new E64 repeat/causal-mask/independent-CE and source/archive/hash checks are mandatory. If a bounded correctness or software-quality gate fails, close at that boundary with downstream profiling N/A; routine attributable repairs follow PC037.

## Independent data and gates

Reuse EXP230128document development only as descriptive evidence. Freeze NEW512primary and512reserve documents before scale search, balanced English/Chinese x Wikipedia/news, M64+16targets, with document/text/32gram exclusion of every previous role including allEXP230/231 datasets and allWikiTexttrain titles. Independently reconstruct every source window. All variants share tokens, positions and masks.

First run canonical packed FP16 software F/C8/C64/E64. Paired stratified document bootstrap5000seed232; overallPPLratio<=1.05 and every language/domain/cell<=1.10. Any E64/F interval crossing its gate triggers the entire frozenreserve for all4variants. Point failure=fail; allpoints and upperCI passing=pass; passingpoints with crossingCI at maximum16384targets=inconclusive (insufficient). No additional reserve, model search or threshold adjustment.

If software acceptance fails/inconclusive, close the approved three-direction sequence and discuss next steps; no device cost or new direction is automatically incurred. If software passes, continue within this experiment through the existing per-channel DSP path with frozen F/C64/E64: independent implementation/reference and exact before/full/after sentinels, same primary/reserve dataset and gates, with a separately applied identical reserve rule for E64/F actualDSP. The fixed weights are never selected or altered using either backend's final scores. Data used by software is disclosed as already scored; it remains disjoint from training, not a new unseen dataset at DSP stage. Preserve ABI108,8MiBVTCM, no timed intermediateDDR/spill, oneFastRPC/step, exact cache progression. Correctness-eligible device candidate completes5short/10rotated formal pairs versusC64 under PC027, with historical F16/W4U8 table references. No grouped runtime or other-recipe modifications. Device numerical or PPL failure is reported, never replaced by software success.

Retain complete results, evidence/model ledgers, source archives, commands, environment and explicit profiling boundaries. This closes the user-approved ordered sequence; further methods or baseline promotion require discussion.
