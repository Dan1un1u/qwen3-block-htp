# EXP-0295: Qwen0.6/1.7 long-prefill migration
User explicitly authorizes remaining-model migration of L32-0051. Adapt fullmodel M64 chunking, persistent KV/absolute RoPE, valid tails, finalchunk-only head, native4 softmax and bounded parallel GQA to both Qwen sizes, retaining Q/K RMSNorm, original weights/calibration, native per-channel W4/SP2 and FP32residual2. No rotation or other recipe optimization. Source branches preserved. Initial source branch binding only for creation of codex/exp-0295-qwen-long-prefill after successful preflight.
Independent actual arithmetic layer/KV/head checks, 64/65/128/129 boundaries and536+46/741+3 with poisoned padding. Exactly8MiBVTCM, oneHMXowner, oneRPC/fullmodelchunk, no timed intermediateDDR/spill. Preserve failures and original gates. No quality/PPL or automatic promotion.
Five short and ten frozen rotated formal repeat10 cycles comparing freshM64 and optimized536/741; samebinary unoptimizedlong controls where supported. Complete Hostwall with input/RoPEstaging, real tokens only. Report long/M64 ratio and inherited parity gate95percentlower>=1, decode10percentguard. No slowed control or gate relaxation. Resource-limited configurations are reported explicitly.
Llama owns device until released; CPU preparation allowed concurrently. Routine attributable fixes, source commits and ordinary push authorized.

## Frozen-control clarification before formal scoring
Qwen had no previously supported long-prefill runtime. A freshly ported opt0 path contains scalar V packing inherited from its short-cache fallback; this is not a fair performance baseline. Preserve those diagnostic measurements, but do not use that path for formal speedup claims. The formal long control uses opt11 (native K/V packers and vector native4 softmax, sequential GQA); candidate opt31 additionally overlaps the GQA clients. Both receive the same basic vector implementation. All arithmetic/physical gates and five-short/ten-formal-repeat10 counts remain unchanged. Fresh best short M64 uses its existing native segmented-cache layout, via an explicit fixed four-step evaluation suite; long M64 row-major is also measured and separately labelled. Throughput parity is judged against the faster same-binary short control, never a deliberately degraded reference.
