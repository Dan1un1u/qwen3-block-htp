# EXP-0004 — Qwen3 middle-block U8×S8 projection shapes

## Approval

Approved by the user on 2026-08-27 by requesting implementation of the real
Qwen3 projection shapes proposed after EXP-0003 passed.

## Objective

Extend the proven Projection Probe to the two distinct Linear shapes in the
Qwen3-1.7B middle-block MLP without QNN. This experiment must prove that the
project-owned runtime can tile, stage, accumulate, and assemble model-shaped
gate/up and down projections while retaining explicit VTCM ownership and an
independent exact arithmetic reference.

## Fixed arithmetic scope

The execution-row scope remains the native integer-HMX `M=64`. The two
projection variants are:

- `gate_up`: `K=2048, N=6144`, representing both gate_proj and up_proj.
- `down`: `K=6144, N=2048`, representing down_proj.

Both use asymmetric U8 activation with zero point 128, signed S8 weights with
zero point 0, S32 accumulation, and one saturated U8 output conversion. The
authoritative Host reference is
`clamp(sum_k((activation[m,k] - 128) * weight[k,n]), 0, 255)` over the complete
row-major shape. Identity-like, signed, structured, and saturation-boundary
patterns must match byte-for-byte for both variants.

This is a W8-carrier substrate experiment. It does not introduce W4 storage,
per-channel scales, RMSNorm, activation functions, residuals, or a complete
Qwen3 block.

## Physical-plan scope

The complete activation is staged once and remains in VTCM for all N tiles.
The complete physical output also remains in VTCM until one final row-major
Native Boundary copy. DDR weight bundles are streamed through two VTCM slots
with explicit ownership, User DMA completion, DMA Publication Barriers, and
producer/consumer semaphores.

K depth is represented as continuous integer-HMX `deep:cm` streams. A single
stream may cover at most 32 architectural K tiles; larger K dimensions are
split into ordered stream chunks while retaining the same accumulator until
the output tile is converted. The declared VTCM plan must fit both variants
inside one 1 MiB project-owned allocation without using DDR intermediates.

Timing must record activation staging, weight staging, HMX compute, producer
and consumer waits, output assembly, full DSP scope, and Host wall time. The
timing run uses repeat count 10 to exercise sustained slot reuse without
turning the bring-up into a long benchmark. There is no speed threshold.

## Hard gate

Both shapes must build for V79, retain identifiable integer-HMX and User DMA
instructions, and execute through one FastRPC invocation per run with no QNN
or QAIRT dependency. For each shape, all four patterns at repeat counts 1 and
10 must return exact output, valid counters and timings, correct shape
metadata, and zero DSP, DMA, resource, or synchronization errors.

The expected logical HMX tile-pair count is 12,288 per projection repeat for
both variants. Buffer reuse and DMA counts must agree with the declared K/N
tile counts. Any FastRPC stall is inspected without killing the device process
or entering an automatic reboot loop.

Passing establishes model-shaped U8×S8 Projection Probes only. It is not a
complete MLP, a W4 implementation, a performance winner, or a Selected
Baseline.

## Evidence destinations

- Source branch: `codex/exp-0004-qwen3-projections`
- Source worktree: `/home/daniuniu/work/qwen3-block-htp`
- Retained artifacts: `D:\llm_exp\models\qwen3-block-htp\exp0004`
- Formal results: `D:\llm_exp\results\qwen3-block-htp\exp0004`

## Provenance

The runtime lifecycle, integer-HMX tile contract, streamed K-depth, DMA
Publication Barrier, and two-slot producer/consumer plan derive from EXP-0003
source commit `a5791f8f6f46f7745d9608a2f799ffa252827da9`. The maximum 32-tile stream
chunk follows the public multi-tile structure studied from `htp-ops-lib`
commit `85eb88edcafd35afff1a43606a4c47eec9a0ca0b`; no QNN runtime code enters the
project.

## Result

The local gate passed on source commit
`b4a1d9829172987b80240a188a313aed49d42b6c`. The implementation materializes
both real middle-block projection shapes under one runtime-selected layout.
Gate/up uses 64 K tiles and 192 N tiles, while down uses 192 K tiles and 64 N
tiles. Both execute exactly 12,288 logical integer-HMX tile pairs per repeat.
The declared VTCM plans are 657,408 bytes for gate/up and 919,552 bytes for
down, so the full activation, two weight bundles, and the complete tiled
output fit inside the fixed 1 MiB allocation for both variants.

The decisive arithmetic result is that one accumulator remains valid across
multiple HMX stream packets. Gate/up issues two 32-tile `deep:cm` streams and
down issues six after one bias load and one `mxclracc`, then performs only one
saturated U8 store. All four deterministic patterns matched the independent
Host reference byte-for-byte for both shapes at repeat counts 1 and 10. The
16 formal runs reported zero total mismatches and zero DSP, HMX-resource,
DMA, or synchronization errors. The static audit retained the integer HMX and
User DMA instructions and found no QNN or QAIRT dynamic dependency.

At repeat 10, each variant executed 122,880 logical tile pairs and 3,840 HMX
streams. Gate/up recorded 64 one-time activation stages, 1,920 weight-bundle
stages, 1,984 DMA submissions, 3,968 DMA waits, and 1,918 weight-slot reuses.
Down recorded 192 activation stages, 640 weight-bundle stages, 832 DMA
submissions, 1,664 DMA waits, and 638 reuses. The device boot identifier was
identical before and after the full evidence matrix.

Timing is retained as characterization rather than a performance gate. Across
the four repeat-10 patterns, gate/up pipeline time ranged from 236,556 to
411,381 qtimer ticks and averaged 367,262.25; down ranged from 174,300 to
334,354 and averaged 275,959.75. The topology and work counts were identical
within each shape, so this spread shows that the current short probe is
sensitive to device runtime state and is not yet a stable benchmarking
protocol. In every run, staging and consumer wait remained larger than the
reported HMX compute scope, preserving the EXP-0003 conclusion that the next
optimization target is the DMA/prefetch schedule rather than arithmetic
throughput alone.

Formal results are retained at
`D:\llm_exp\results\qwen3-block-htp\exp0004\20260827T034619Z_b4a1d9829172`,
and device artifacts at
`D:\llm_exp\models\qwen3-block-htp\exp0004\b4a1d9829172`. Execution is
complete, evidence is valid, and the local Qwen3 Projection Shape gate passed.
Adoption remains pending user decision; this is still a W8-carrier substrate,
not a complete MLP, W4 implementation, performance winner, or Selected
Baseline.
