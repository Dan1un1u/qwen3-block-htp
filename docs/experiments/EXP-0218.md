# EXP-0218 — Unified lightweight quality and speed evaluation

User approved the retained plan on 2026-09-05. Implement frozen bilingual quality samples, independent cached BF16 teacher, actual DSP recipe scoring/teacher forcing, small deterministic short-answer tasks, multi-sample reuse of loaded weights, and fresh rotated M64+15 speed measurements. Quality mode uses real FP16 or dequantized U8 LM-head values, never a substituted Host head. W4U8 quality failure is measured, not repaired. No projection specialization, weight/scale or quantization change; no baseline promotion. Data and answer rules are frozen before seeing model outputs. Numeric score accumulation can use phase-dead VTCM scratch and return small scalar outputs; formal speed keeps scoring disabled. Artifact/evidence hashes and physical/ledger gates remain mandatory. Short task prompts may be authored to exactly M64; no silent padding/truncation is permitted. If logical prompt length support is needed, implement explicit mask/cache semantics within the approved evaluation scope. Quality score snapshots are not Selected Baseline promotion.

Status: completed; evidence valid as measurements including failures. Local gate FAIL is limited to W4U8 quality determinism; it does not mean the evaluation is unfinished. All three speed/physical/ledger gates and both A16 determinism gates pass. Adoption pending; no Selected Baseline promotion.

The complete result is in [EXP-0218-RESULTS.md](EXP-0218-RESULTS.md); all profiling counters and R1/R10 are in [EXP-0218-PROFILE.md](EXP-0218-PROFILE.md). Frozen data and runner usage are in source docs/LIGHTWEIGHT_EVALUATION.md. Source final HEAD: b2904bbc60b25e6b80f53351655b1c585fd97db2.

Quality snapshot: BF16 NLL 3.641952 / 22 of 24; DSP F16F16 3.633608 / 22 of 24; DSP W4F16 4.231668 / 8 of 24; DSP W4U8 18.3682 / 0 of 24 (diagnostic, not certified). Software W4F16 also scores 8 of 24, but is not numerically equivalent to DSP (445/512 top-1 agreement). Do not retain the earlier expectation that W4F16 text readability establishes useful accuracy.

Quick quality takes 51.52 s, full three-recipe quality 251.84 s, two-sample determinism repeat 24.88 s, excluding the cached BF16 run (47.15 s). Holdout remains unexecuted. Initial per-recipe delta is N/A; future A16 comparisons can use this frozen snapshot without calling it a Selected performance Baseline. Proposed quality alert lines remain proposals.

Recovery/evidence: dataset length/path and executable-mode errors were fixed autonomously before dependent execution; original logs retained. One initial W4U8 instrumented consistency selected-token mismatch was followed by four passing consistency sessions, but fixed repetition still found three logit-statistic differences. The cause remains unresolved. W4U8 quality is intentionally recorded as a failure rather than masking it with retries; no W4U8 quality repair/specialization was attempted. All 30 formal speed sessions reproduce their fixed token sequences. Source/dataset/hashes/physical gates were never relaxed.
