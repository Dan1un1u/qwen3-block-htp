# EXP-0311 concurrent VTCM consumers
User authorized follow-up to EXP0310. Standalone microbenchmarks only; no model baseline change. Compare matched HVX-only, HMX-only and simultaneous readers, over identical layouts and loop kernels. Sweep HVX1/2/4/5/6 workers, HMX FP16 and packed W4, disjoint and shared read-only operands. Use synchronized common time windows, per-consumer completed bytes, start-delay checks, qtimer and cycle frequencies. No writable region aliases. All output validations outside timing. Full8MiB VTCM. Add scalar-feeder contention control if concurrency loss needs attribution. Warmups then repeated formal samples, archive exact byte accounting. Results can establish empirical throughput isolation/contention, not undocumented physical port topology. PC037 repair applies; model three-recipe E2E gates N/A.

Completed. All178 hardware invocations pass data checks,72formal invocations /720retained50ms windows. No model baseline change. See EXP-0311-RESULTS.md. Device released.
