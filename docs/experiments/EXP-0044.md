# EXP-0044 — Native integer-Attention operand preparation

## Approval and source

Approved by the user on 2026-08-29 to continue optimizing the integer
Attention implementation. The implementation starts from the completed
EXP-0042 source head `d5f68b72b799a5c8df6c4f5cecfda218edc1e23d`; EXP-0043 is
an attribution-only child and is not an implementation parent. The source
branch is `codex/exp-0044-u8-attention-native-operands`.

## Evidence-driven hypothesis

At repeat ten, EXP-0042 spends 28272.7 qtimer ticks in the exclusive Attention
interval. Its four Attention contexts accumulate about 110.3 thousand worker
ticks: 46874.6 in U8 Q/K Norm plus RoPE, 34870.5 in K operand preparation,
7695.9 in V preparation, 13737.5 in log2 Softmax, and only 3653.6 in QK plus
AV integer-HMX execution. Dividing aggregate work by four gives a 27572.6-tick
ideal lower bound, close to the measured wall interval. The present schedule
is therefore work-bound rather than primarily under-parallelized.

The first hypothesis is that K is scanned and transformed twice. Native-tile
K Norm-RoPE currently gathers every 128-channel row from four 32-channel HMX
tiles, writes U8 back to those tiles, and a later pass rereads it to centre U8
to S8, transpose into the integer-HMX weight carrier, reduce signed column
sums and construct dynamic asymmetric-zero-point bias. Producing the exact
same U8 codes and K carrier in one pass should remove the second scan and its
scalar reduction without changing arithmetic.

The second hypothesis is that EXP-0042 begins Q/K Norm-RoPE only after all QKV
projections finish, whereas the W4F16 control publishes heads during QKV
projection. After the fused operand pass is correct and faster, integer GQA
tasks may consume generation-tagged Q/K head readiness during projection so
that this work overlaps the QKV stage.

## Fixed contract and gates

- The Execution Unit remains the real Qwen3 layer-14 complete block at M=64.
- W4 values, asymmetric-U8 qparams, integer score carrier, four-bit log2
  Softmax, integer AV arithmetic and output codes remain byte-identical to
  EXP-0042. No accuracy or quantization-recipe change is allowed.
- One FastRPC invocation, one HMX owner, exactly 8 MiB VTCM, zero intermediate
  DDR, zero spill/fill and no QNN path remain mandatory.
- Stage A must prove byte-exact Q, K, signed-transposed K carrier, dynamic bias,
  QK, probability, AV and final block output. It must reduce the combined
  Q/K Norm-RoPE plus K-preparation work and the complete Attention wall before
  Stage B is admitted.
- Stage B may change only dependency publication and task start time. It must
  retain one HMX owner and generation-safe storage reuse.
- The final candidate must strictly improve complete Attention and Host wall
  over the unchanged EXP-0042 control at repeat one and repeat ten. A valid
  negative result remains evidence and cannot change the Selected Baseline.

Formal evidence will be retained under
`D:\llm_exp\results\qwen3-block-htp\exp0044`; generated artifacts will be
retained under `D:\llm_exp\models\qwen3-block-htp\exp0044`.

## Stage A result

Stage A was implemented and frozen at source commit
`0a9f129fb10585141d310492968ddf37ffbcd5fb`. The K-specific HVX path now
normalizes and applies RoPE once, writes the unchanged native U8 K tiles,
centres the same 128-byte row to S8, uses vector scatter to construct the
transposed integer-HMX carrier, reduces the signed row sum in HVX, and emits
the unchanged asymmetric-zero-point bias. The EXP-0042 path remains available
as the same-binary control.

The audit-on run generated the old carrier and bias in a disjoint VTCM slice
and found zero byte mismatches. The independent Host reference also found zero
mismatches for QK, log2 Softmax, and AV; the final block output remained zero
LSB from EXP-0042. Audit-off runs retained exactly 8 MiB VTCM, zero
intermediate DDR, zero spill/fill, one FastRPC invocation, one HMX owner, and
no QNN dependency.

Seven interleaved paired rounds passed at both repeat scopes. At repeat one,
combined Q/K Norm-RoPE plus K preparation decreased 41.78%, the exclusive
Attention interval decreased 29.74%, and Host wall decreased 6.74%. At repeat
ten, the same changes were 42.03%, 30.34%, and 5.63%. Formal evidence is at
`D:\llm_exp\results\qwen3-block-htp\exp0044\20260829T121228Z_0a9f129fb105_stage_a`.

Stage A therefore passes and admits Stage B. Stage B may only publish Q/K head
readiness during QKV projection and start the unchanged fused-K GQA task after
its required heads are generation-ready. It may not change arithmetic,
qparams, carrier bytes, HMX ownership, or the physical residency contract.
