# EXP-0258 — Dense R3 full-model diagnostic E2E

User explicitly approves the proposed one-experiment exception: measure true full-model W4A8 with dense HMX R3 despite its known whole-layer ideal-reference gate failure. Original <=2LSB/cosine>=0.999 remains failed, never relabeled pass. This is performance-only diagnosis, not accuracy acceptance, implementation-baseline eligibility or promotion. Parent EXP0257 source8d56803ac77697be8748e65a9f933b720962af1e; other recipes frozen.

A=noR3, original EXP0246 OFF qparams, fixed offline EOS W4A16-to-U8 prefix, wideNR64. B=plain dense HMX R3 mode1, frozen R3_ALL Q/K parameters and correctly rotated offline prefix K beforeU8 quantization, wideNR64. Fixed C64 per-output-channel W4 and all non-Q/K quantization unchanged. No butterfly, mode5 expensive refinement, scalar candidate fallback, calibration, weight fitting, regrouping or mixed precision runtime. R3 itself uses existing FP16 HMX GEMM. Count every layer/step rotation and measure prepare, dense matmul, finish counters within complete Host wall. Reuse dead phase scratch, exactly8MiB VTCM, no timed intermediate DDR/spill, one RPC per complete28layer model pass. Implement fullmodel telemetry and exporter as necessary.

Verify pinned source/evidence/packages and original noR3 output; validate R3 dense component, head layout, at-most-once transform and rotated seed independent of the new exporter. Preserve known ideal-reference whole-layer mismatch as diagnostic. Three consecutive layer/cache smoke before full28layer rollout; measure fullmodel repeated output determinism, physical properties and closed invocation/per-layer ledgers. Timing never uses audit DDR. Any new corrupt/unstable output, failed physical check or unexplained hash mismatch blocks dependent timing under PC037; the explicit exception covers only previously understood ideal-R3 rounding amplification. EXP0257's original legacy/no-prefix C0 transient nondeterminism remains unresolved; do not reuse it as an accepted control or claim it repaired. Use previously stable wideNR64/noR3 as matched control; failures in either new arm remain visible and stop.

Reuse frozen EXP0257 M64 prompt (1EOS+63body/chat), capacity128,16selectedtokens comprising1prefill and15continuous feedback decode steps. Five short and ten rotated paired formal diagnostic rounds, repeat1/repeat10 independently reset sequences with loaded weights reused. All timings include complete embedding,28layers,finalnorm,nativeU8W4head,greedy,Host-DSP boundary and R3 overhead. End-to-end output16/generation-loop wall reported alongside prefill64/Host and decode15/Host. Startup/ADB/WSL frontend excluded and identified. Quantized-logit grid remains scale0.5 zero128. No new PPL required; text samples optional diagnostic, not acceptance.

User authorizes fullmodel speed cost measurement directly, so >10percent is retained as a failed speed eligibility criterion, not a reason to suppress the requested cost measurement. Five/ten rounds report paired latency percent and95% bootstrap10000seed258, no failed sample deletion or timing selection. If new determinism/physical gate fails, preserve partial scope and stop. No fullmodel throughput extrapolation from layer timings.

PC027/028 stable three-recipe table with nonpaired historical references; detailed matched control/R3 repeat1/repeat10 modules, dense overhead, all counters and provenance archived. Explicit unaccepted numerical and model-quality status in every report. Seal all evidence, close source/memory clean and synchronized. No follow-on algorithm or promotion. Parent branch bound temporarily only for preflight-controlled branch creation.
