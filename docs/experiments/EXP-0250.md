# EXP-0250 — Raw-score maximum before narrow Softmax encoding

PC064: user2026-09-08 approves trying EXP0249's proposed direction. Parent0b85aebb96e9a3ff06e667ec3365d41d69c7dd15; verified parent ledger559391a5579a572e5ef2bca17017b4d2bd69e3a20036720160aab6696034e689. Branch codex/exp-0250-raw-score-max (temporary parent binding for setup).

Implement a bounded software arithmetic experiment first. Freeze C64 per-output-channel W4/scales, EXP0246 A8/R3_ALL parameters, raw EOS prefix, shared residual semantics, explicit dense R3/no butterfly/FWHT, original probability scale discrepancy and all other recipes. No calibration, new weights, DSP changes, hardware runs or baseline promotion in this phase. Integer attention software PPL remains conditional on software linear/norm/R3 arithmetic and the offline prefix, not full-device PPL. Decide device feasibility after the software result; preserve8MiB/no intermediate DDR/one RPC and >10percent single-layer Host-wall stop for future hardware work.

Freeze16 arms before inference: F,C64 and OFF/R3 each with carrier,unbounded,wide_float,score,wide_exact,wide_sole,sole. carrier is EXP0249 full-real-score integer carrier/AV plus standard Softmax. unbounded rounds the accumulator using the same native power-of-two divisor and multiplier but removes both U8 saturations, then standard Softmax. wide_float retains the first native U8 conversion but removes the second U8 saturation, then standard Softmax. score is the unchanged original two-stage-clipped QK plus standard Softmax. wide_exact and wide_sole compute masked row maximum on raw native U8 scores, form (raw_max-raw)*multiplier in signed16, then capped integer exponents and existing exact or SOLE division respectively. sole is the original complete integer attention control. Preserve probability codes and AV conversion in all arms. Differences carrier->unbounded estimate rounding/gain approximation; unbounded->wide_float first-carrier saturation; wide_float->score second saturation; wide_float->wide_exact integer exponent; wide_exact->wide_sole reciprocal approximation. These are conditional and nonadditive. OFF/R3 includes previously frozen Q/K calibration differences, not isolated rotation learning.

Gate before model inference: retain parent exact device-boundary reference tests; new independent NumPy int64/scalar oracle must match Torch implementation exactly for integer stages on retained Q/K/V captures and random masked prefill/decode including prefix/history. Explicit signed16 implementation must agree with widened mathematical differences; maximum delta255*18=4590 and rounding+4=4594. Test causal masks, common shifts without raw clipping, constant rows, single valid key, exponent-cap and SOLE boundary cases. Legacy F/C64 and6 OFF/R3 carrier/score/sole development outputs must reproduce EXP0249 per-token NLL/top1. Each arm repeat/causal/CE and immutable weights/prefix checks. No relaxation of numerical limits; preserve and repair attributable implementation failures under PC037.

Use the exposed EXP0249 development32 documents solely for regression/debug. Fresh independent final128 documents,32 per English/Chinese wiki/news cell,2048 scored targets, batch4 prefill64 +15 teacher-forced cached steps. This is a lightweight diagnostic panel, not a replacement independent deployment acceptance. Exclude all historical/calibration documents,text hashes,token32-grams including EXP0249. Freeze all16 arms, tokenizer/corpus/token windows/protocol/parameters before inference. No candidate selection or tuning on final. Independent token reconstruction audit; paired stratified document bootstrap10000seed250, pointwise95percent intervals and per-cell PPL. Existing5percent overall/10percent cell reference thresholds remain unchanged, with uncertainty reported and no automatic acceptance. Complete source/evidence/memory closure and archived full profiling N/A with verified historical table; E2E token/s N/A. Poor PPL is valid negative evidence, not a reason to change scales or silently expand scope.
