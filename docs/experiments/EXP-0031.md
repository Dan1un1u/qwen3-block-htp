# EXP-0031 — GQA-group Attention pipeline

## Approval and status

Approved by the user on 2026-08-28 after reviewing the formal EXP-0030
critical path and the `htp-ops-lib` KV-head worker schedule. This experiment
starts from EXP-0030 source commit
`3bb967d60c6c91b51f26bf426510f0af99885be7`. It does not accept EXP-0030 or
promote a Selected Baseline.

The user accepted the completed EXP-0031 checkpoint on 2026-08-28 and then
explicitly authorized continuing EXP-0031. Commit
`1cb6995e1d2194834cd80ca13420cdbbebccfd8a` and its formal evidence remain an
immutable accepted checkpoint. The continuation reopens execution only; it
does not promote a Selected Baseline, and a failed continuation cannot revoke
the accepted checkpoint.

## Motivation

The EXP-0030 W4F16 streaming candidate takes 3.019 ms per complete block at
repeat ten. Its QK Norm plus RoPE interval is 6.185 thousand ticks and its
Attention interval is 7.169 thousand ticks. Standard FP16 Softmax alone takes
5.314 thousand ticks, while QK and AV HMX together take only 1.183 thousand
ticks. QK Norm, RoPE, and Softmax currently execute serially on one HVX
context. They therefore account for the largest clearly parallelizable shared
critical path after the gated-MLP work.

Qwen3-1.7B uses sixteen Q heads and eight KV heads, so one independent GQA task
contains two Q heads and one K/V head. The reference Hexagon implementation
assigns work by KV head and lets worker-local HVX processing overlap serialized
HMX ownership. EXP-0031 tests that scheduling structure in the project-owned
runtime without importing its online-Softmax mathematics or changing the
current block arithmetic.

## Fixed contract

- The Execution Unit remains one complete real Qwen3-1.7B layer-14 block at
  `M=64` in one FastRPC invocation. Formal timing retains repeat one and repeat
  ten.
- F16F16 and W4F16 retain the exact EXP-0030 projection, streaming MLP,
  residual, RMSNorm, weight, scale, and output contracts. W4U8 remains outside
  this experiment.
- QK RMSNorm, RoPE, causal masking, score scaling, standard FP16 exponential,
  Softmax reduction order within each row, QK HMX, and AV HMX arithmetic remain
  unchanged. No LUT, log2 approximation, or alternate quantization is allowed.
- Every mode requests and receives exactly 8,388,608 VTCM bytes. Q, K, V,
  scores, probability, Attention output, and worker scratch remain in VTCM or
  accelerator state. Intermediate DDR, spill/fill, extra FastRPC calls, QNN,
  and QAIRT are hard failures.
- HMX retains one serialized ownership domain. Multiple HVX workers may publish
  HMX requests, but direct concurrent HMX programming is not permitted.
- Candidate output must remain byte-identical to its paired EXP-0030 control,
  in addition to passing the existing Implementation Reference and non-finite
  checks.
- Equivalent-scope Host wall is primary. The Block Timing Ledger remains
  exclusive and closed. HMX, HVX, worker work, queue wait, and ready wait are
  diagnostic intervals and may not be summed as latency.

## Ordered milestones

### A — Multi-worker QK Norm and RoPE

Partition the existing head-row work among the calling context and persistent
HVX workers. Each `(tensor, row, head)` task preserves the exact current
sum-of-squares reduction, reciprocal square root, gamma multiplication, RoPE
instruction sequence, and stores. Q and K tasks are disjoint. Retain this mode
only if it reduces the QK Norm/RoPE interval and does not regress complete-block
Host wall for both projection variants.

### B — Multi-worker standard FP16 Softmax

Partition the existing independent `(Q head, row)` Softmax tasks among the same
persistent HVX workers. Preserve one-vector-per-row masking, maximum reduction,
`qhmath_hvx_exp_vhf`, sum reduction, reciprocal, and FP16 output sequence.
Scores and probability remain in their existing VTCM allocations. Retain this
mode only if it reduces Softmax and complete Attention intervals without a
complete-block Host-wall regression.

### C — GQA-group heterogeneous pipeline

Create eight generation-tagged GQA tasks. A worker processes two Q heads and
one K/V head through QK Norm/RoPE, requests the unchanged QK HMX commands,
executes standard Softmax after score readiness, then requests the unchanged AV
HMX commands and writes its disjoint output channels. A single central HMX
owner serializes all QK and AV commands while non-conflicting HVX work proceeds
on other groups. Worker scratch must be proven by explicit peak-live VTCM
accounting.

The full online FlashAttention algorithm is not part of this milestone. At
`M=64`, the complete score and probability tensors total only 262,144 bytes and
already reside in VTCM, so the tested hypothesis is heterogeneous scheduling,
not removal of off-chip quadratic traffic.

### D — Complete-block selection gate

Evaluate the immutable EXP-0030 control, the retained A+B candidate, and the
GQA pipeline candidate for both F16F16 and W4F16. Collect audit-on correctness
and alternating audit-off repeat-one/repeat-ten timing in one stable boot. The
selected local candidate must reduce the combined QK Norm/RoPE plus Attention
exclusive interval and must not regress complete-block Host wall for either
variant at either repeat count. W4F16 must remain strictly faster than F16F16.
No fixed percentage target is imposed. The faster valid candidate may be A+B
even if milestone C is correct but slower.

### E — QKV-to-GQA producer/consumer overlap

Continue on the accepted GQA candidate without changing the experiment family.
The tested hypothesis is that Q/K normalization and RoPE readiness can begin
while later QKV projection work is still in flight, rather than retaining the
current whole-QKV producer barrier before the GQA pipeline starts.

The first implementation step must use the smallest safe schedule: overlap
already-produced Q/K normalization and RoPE with a later projection while
preserving the existing projection weights, output layout, arithmetic, and
single HMX ownership domain. A group-major projection rewrite is permitted only
after this smaller schedule is measured and shown insufficient. Persistent HVX
workers must not race with W4 expansion workers, and every producer/consumer
handoff requires an explicit generation-tagged readiness dependency.

The accepted checkpoint is the paired control. A retained continuation must
keep byte-identical outputs, exactly 8 MiB VTCM, zero intermediate DDR and
spill/fill, one FastRPC invocation, no QNN/QAIRT dependency, and one serialized
HMX owner. It must not regress complete-block Host wall for either F16F16 or
W4F16 at repeat one or repeat ten, and W4F16 must remain faster than F16F16.
Combined `qkv_projection_ticks + qk_norm_rope_ticks + attention_ticks` is a
diagnostic secondary metric; equivalent-scope Host wall remains the speed
gate. If a valid schedule requires changing block mathematics, quantization,
external tensor contracts, or writing Q/K/V intermediates to DDR, stop this
continuation rather than broadening EXP-0031.

## Evidence destinations

- Source branch: `codex/exp-0031-gqa-attention-pipeline`
- Source worktree: `/home/daniuniu/work/qwen3-block-htp`
- Retained artifacts: `D:\llm_exp\models\qwen3-block-htp\exp0031`
- Formal results: `D:\llm_exp\results\qwen3-block-htp\exp0031`

## Completed evidence

Implementation completed on source commit
`1cb6995e1d2194834cd80ca13420cdbbebccfd8a`. The formal matrix contains 110
records: audit-on correctness plus five alternating audit-off rounds at repeat
one and repeat ten for control, QK-Norm/RoPE-only, Softmax-only, combined
parallel-HVX, and GQA-pipeline modes in both F16F16 and W4F16. The boot ID was
stable for the complete collection.

Milestone A reduced repeat-ten QK Norm plus RoPE from 6.193 to 1.762 thousand
ticks in F16F16. Milestone B reduced the corresponding standard Softmax from
5.314 to 1.433 thousand ticks. Both retained the exact current arithmetic and
each independently reduced complete-block Host wall.

Both the combined parallel-HVX mode and the GQA pipeline passed the local
selection gate. The predefined median rule selected the GQA pipeline. It
changed the formal medians as follows:

| Scope | Control | GQA pipeline | Change |
| --- | ---: | ---: | ---: |
| repeat1 F16F16 Host wall | 3.562 ms | 3.077 ms | -13.63% |
| repeat1 W4F16 Host wall | 3.417 ms | 2.950 ms | -13.66% |
| repeat10 F16F16 Host wall | 3.135 ms | 2.665 ms | -15.00% |
| repeat10 W4F16 Host wall | 3.012 ms | 2.528 ms | -16.08% |

At repeat ten, QK Norm/RoPE plus Attention fell by 69.12% in F16F16 and
69.15% in W4F16. W4F16 remained 5.15% faster than F16F16 under the selected
plan. Candidate outputs remained byte-identical to their EXP-0030 controls:
F16F16 `704252c89780e695` and W4F16 `f18b9abbe1487231`.

The selected candidate used one HMX owner, four HVX contexts, eight disjoint
GQA groups, the unchanged 32 QK/AV HMX commands per block, one FastRPC
invocation, and exactly 8 MiB VTCM. Intermediate DDR read/write, spill/fill,
and QNN dependencies were all zero. The local gate passed. The user accepted
this checkpoint on 2026-08-28; no Selected Baseline is promoted. EXP-0031 was
then reopened only for Milestone E continuation under the fixed contract above.

Formal evidence:

- Result set:
  `D:\llm_exp\results\qwen3-block-htp\exp0031\20260828T121215Z_1cb6995e1d21`
- Matrix gate:
  `D:\llm_exp\results\qwen3-block-htp\exp0031\20260828T121215Z_1cb6995e1d21\matrix_gate.json`
- Artifact set:
  `D:\llm_exp\models\qwen3-block-htp\exp0031\artifacts\1cb6995e1d21`

## Milestone E completed evidence

Milestone E completed on source commit
`3638b167afd687939786f714fc48987021c02d94`. The first whole-tensor attempt
started Q normalization only after Q projection and K normalization only after
K projection. It improved F16F16 but left roughly three thousand W4F16 tail
wait ticks, so W4F16 regressed and that schedule was discarded before formal
collection.

The retained implementation instead starts one generation-tagged 24-head
producer/consumer stream before Q projection. Q and K projections publish a
head only after all four 32-channel output tiles for that head have been
unpacked. Three worker contexts consume ready heads for F16F16. W4F16 reserves
worker two for Q/K Norm and RoPE while the main context plus workers zero and
one retain the existing W4 expansion schedule. The same packed normalized
activation is reused by Q, K, and V because their input is byte-identical.
After V projection, all head work is joined before the accepted GQA pipeline;
the GQA workers therefore skip only the already-completed identical Norm/RoPE
instructions.

The formal continuation matrix contains 132 records in one stable boot. It
uses the accepted GQA candidate as the direct paired control. Audit-on output
remained byte-identical: F16F16 `704252c89780e695` and W4F16
`f18b9abbe1487231`. HMX command and tile counts were unchanged. Every run kept
one FastRPC invocation per Execution Unit, one serialized HMX owner, exactly
8 MiB VTCM, and zero intermediate DDR or spill/fill.

| Scope | Accepted GQA | QKV overlap | Change |
| --- | ---: | ---: | ---: |
| repeat1 F16F16 Host wall | 3.060 ms | 3.012 ms | -1.58% |
| repeat1 W4F16 Host wall | 2.956 ms | 2.900 ms | -1.90% |
| repeat10 F16F16 Host wall | 2.682 ms | 2.585 ms | -3.64% |
| repeat10 W4F16 Host wall | 2.536 ms | 2.492 ms | -1.72% |

At repeat ten, the complete continuation scope
`QKV projection + QK Norm/RoPE + Attention` fell by 13.22% in F16F16 and
9.64% in W4F16. The W4F16 head-stream join retained a small 362-tick median
tail, but this no longer outweighed the removed Attention-side Norm/RoPE work.
W4F16 remained 3.59% faster than F16F16. The continuation local gate passed;
adoption of this new checkpoint remains pending an explicit user decision and
no Selected Baseline is promoted.

Continuation formal evidence:

- Result set:
  `D:\llm_exp\results\qwen3-block-htp\exp0031\20260828T123542Z_3638b167afd6`
- Matrix gate:
  `D:\llm_exp\results\qwen3-block-htp\exp0031\20260828T123542Z_3638b167afd6\matrix_gate.json`
- Artifact set:
  `D:\llm_exp\models\qwen3-block-htp\exp0031\artifacts\3638b167afd6`
