# EXP-0253 — Floating attention core diagnostic

PC067. User 2026-09-09 approves the proposed complete floating attention software diagnostic before deciding on HTP FlashAttention. Parent EXP0252 source71f8a62449226127e6befb776abc5f572f1c4a88, ledger c2491088eefd8f96f943be73c63898815697f906a8eab6e898d48639169ccd9c. Branch codex/exp-0253-float-attention.

Freeze original per-channel W4, EXP0246 EOS prefix and OFF/R3 qparams, all non-attention boundaries and original U8 Q/K/V storage; other recipes frozen. New float_core dequantizes actual U8 Q/K/V to existing FP16 values and computes scaled QK, standard masked exponential Softmax and AV in FP32 with TF32 disabled. No internal score quantization, capped log2 exponent, probability QDQ, signed-K carrier clipping, V recentering or integer AV conversion. Cast context to FP16 and retain original attn_context output QDQ exactly once. Dense R3 uses existing explicit matrix multiply; no butterfly, retraining, clipping search or new weights. This diagnoses standard-attention arithmetic, not a native FlashAttention kernel, device performance or proven theoretical bound.

Eight prespecified arms: F,C64 and OFF/R3 x legacy,wide_nr64,float_core. Legacy is historical standard Softmax with probability QDQ, NR64 is sealed EXP0252 integer attention. Existing development32 documents reproduce all six controls exactly per token (legacy EXP0249, others EXP0252); no selection. Fresh128 documents,32 per en/zh wiki/news,2048targets,64context+16targets, disjoint all prior evaluation/calibration/training by document/text/token32 checks, deterministic seed253. Freeze all inputs before scoring. Independent retokenization and overlap audit, repeat/causality/CE/prefix/weight immutability, equal-cell NLL and paired document bootstrap10000 seed253. Same quality gates versus F: overall5percent and every cell10percent; point pass and confidence pass separate. No final-data tuning or optional stopping.

Before PPL validate new core against independent NumPy Float64 dense and independently chunked online Softmax accumulation over all56 frozen configs, causal/decode/masked cases and sharp/flat logits. FP32 context normalized RMS error <=1e-5, maximum absolute error <=2e-5*max(1,maxabs(reference)), probability row sum error<=2e-6, masked probabilities exactlyzero, all finite. Preserve FP16 final rounding, frozen output QDQ and counters proving no probability QDQ and one context QDQ per attention invocation. Original integer arithmetic references reused only with pinned sealed provenance; full original integer denominator sweep need not repeat because integer code unchanged.

Software diagnostic only: no new DSP build, hardware run, model artifact or speed claim. PC027 stable three-recipe profiling table records N/A for unmeasured fields and E2E. Implementation gate separate from model quality. Close evidence/source/memory and discuss next direction, no automatic native FlashAttention implementation or baseline promotion. Routine attributable repairs under PC037 preserve original failed evidence and all gates.
