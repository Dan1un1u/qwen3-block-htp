# EXP-0033 — Dynamic W4 QKV worker scheduling

## Approval and status

Approved by the user on 2026-08-28 after reviewing the completed EXP-0032
complete-block ledger and agreeing that QKV is the next optimization target.
The experiment starts from source commit
`91ba193660513bd0675c0a091db199aa09df1773`. Using EXP-0032 as the source
parent does not accept its pending adoption decision or promote a Selected
Baseline.

## Motivation

In the EXP-0032 repeat-ten formal matrix, the selected W4F16 complete block is
5.83 percent faster in Host wall than the fastest fair F16F16 batch-four
control. Down is 29.91 percent faster and O projection is 14.79 percent faster;
Gate/Up is 0.68 percent faster. QKV is the only major projection scope in which
W4F16 is slower: 8,500.6 versus 7,675.0 ticks, a 10.76 percent regression that
occupies 19.00 percent of the W4F16 Block Timing Ledger.

The accepted QKV-readiness schedule statically reserves one HVX worker for Q/K
Norm and RoPE while the main context plus two workers retain W4 expansion. This
partition cannot adapt when one queue is temporarily empty or when the next
HMX command is about to run out of expanded weight. EXP-0033 tests whether a
generation-tagged priority scheduler can keep the W4 HMX producer fed while
allowing otherwise-idle HVX capacity to consume ready Q/K heads.

## Fixed contract

- The Execution Unit remains one complete real Qwen3-1.7B layer-14 block at
  `M=64`, measured at repeat one and repeat ten in one FastRPC invocation.
- The F16F16 batch-four control and W4F16 EXP-0032 control retain identical
  weights, scales, projection arithmetic, output layout, HMX commands,
  head-readiness semantics, Q/K Norm, RoPE, GQA Attention, residuals, streaming
  MLP, Gate/Up DMA-8 scale-cache plan, Down, and output arithmetic.
- The candidate may change only HVX worker ownership and queue priority during
  W4F16 Q, K, and V projection plus Q/K Norm-RoPE consumption. It may not fuse
  Q/K/V weights, reorder projection arithmetic, modify HMX command shapes,
  change head mathematics, or extend readiness into QK/Softmax/AV in this
  experiment.
- Every variant requests and receives exactly 8,388,608 VTCM bytes. Q, K, V,
  all weights and expansion buffers, scores, probability, and block
  intermediates retain the existing zero-intermediate-DDR contract. Spill/fill,
  extra FastRPC calls, QNN, and QAIRT are hard failures.
- One serialized HMX owner is retained. Worker work stealing must use explicit
  generation-tagged release/acquire dependencies and may not allow two workers
  to expand or normalize the same unit of work.
- Candidate output must remain byte-identical to the paired EXP-0032 W4F16
  control and pass the independent Implementation Reference and non-finite
  checks.
- Equivalent-scope complete-block Host wall is primary. QKV exclusive ticks,
  W4 expansion readiness, HMX starvation, Q/K task completion, worker idle,
  and join-tail counters are diagnostic and may not be summed as latency.

## Ordered milestones

### A — Worker timeline audit

Record generation-tagged Q/K/V projection phase, packed-weight DMA completion,
expansion job claim and publication, HMX command-ready and command-done events,
Q/K head publication and claim, per-worker queue-empty intervals, and final
Norm-RoPE join tail. Audit instrumentation is excluded from formal performance
collection. The audit must determine whether the QKV regression is caused by
HMX starvation, static worker reservation, head-consumer tail, or an unrelated
projection cost before schedule changes are retained.

### B — Priority work stealing

Replace the fixed W4F16 `two expansion workers plus one head worker` ownership
with a bounded priority policy. Work required to maintain the declared
expanded-weight lookahead for the next HMX command has priority. Once that
lookahead is satisfied, any eligible HVX worker may claim one published Q/K
Norm-RoPE task. The scheduler must return capacity to expansion before the HMX
producer starves and must preserve existing worker-phase barriers outside QKV.

Only queue ownership and priority may change. A fused QKV carrier, different
weight order, direct QKV-to-GQA execution, or changed HMX tiling requires a new
user-approved experiment rather than silent expansion of EXP-0033.

### C — Complete-block selection gate

Collect audit-on correctness and alternating audit-off repeat-one/repeat-ten
timing for the immutable F16F16 control, immutable W4F16 control, and retained
dynamic candidate in one stable device boot. At both repeat counts, a selected
candidate must reduce W4F16 QKV exclusive ticks and complete-block W4F16 Host
wall relative to its control. It must not regress F16F16, must keep the W4F16
complete block strictly faster than F16F16, and must preserve byte-identical
output, exact 8 MiB VTCM, zero intermediate DDR, zero spill/fill, one FastRPC,
and one HMX owner.

QKV parity with F16F16 is a desired diagnostic target, not a separate hard
percentage gate. If dynamic worker ownership cannot pass the complete-block
gate, stop with valid negative evidence instead of proceeding to fused QKV.

## Evidence destinations

- Source branch: `codex/exp-0033-qkv-dynamic-scheduler`
- Source worktree: `/home/daniuniu/work/qwen3-block-htp`
- Retained artifacts: `D:\llm_exp\models\qwen3-block-htp\exp0033`
- Formal results: `D:\llm_exp\results\qwen3-block-htp\exp0033`

## Worker-timeline finding

The audit corrected the motivating model of the accepted EXP-0032 schedule.
W4 QKV expansion does not consist of two expansion contexts plus one reserved
Norm context. It already uses three expansion contexts: the FastRPC main
context expands one region partition while pool workers zero and one expand
the other two partitions. Pool worker two is the fourth HVX context and runs
the ready Q/K Norm-RoPE queue. Consequently, the Norm consumer does not remove
one of the three contexts that feed W4 expansion.

The EXP-0032 control's remaining Q/K Norm join tail is small: 413 ticks at
repeat one and 289.2 ticks per block at repeat ten in the final formal matrix.
It is not large enough to explain the roughly 850-tick QKV gap between W4F16
and F16F16. The tested scheduler therefore had little idle capacity to recover.

## Rejected development schedules

An initial diagnostic repartitioned each expansion call from main-two,
worker-one, worker-one regions to main-one, worker-two, worker-one regions so
the main context could consume a ready Norm task earlier. It retained exact
output and zero intermediate DDR, but increased repeat-ten expansion pool wait
from approximately 4,530 to 7,431 ticks and increased QKV from approximately
8,526 to 10,850 ticks. Saving roughly 706 ticks of main-context Norm work could
not offset the deliberately unbalanced expansion tail, so that repartition was
discarded before formal collection.

The retained formal candidate restored the original balanced three-way
expansion partition. After the main context published its own expansion
regions, it opportunistically claimed one ready Q/K Norm-RoPE task while the
other expansion contexts completed. Generation-tagged, mutex-protected task
claims prevented duplicates. This candidate is useful negative evidence
because it isolates worker ownership without changing weights, arithmetic,
HMX command shapes, VTCM layout, Attention, or MLP.

## Formal outcome

The formal matrix contains 33 records in one stable device boot: one audit-on
correctness record plus five alternating audit-off repeat-one and repeat-ten
records for the F16F16 batch-four control, EXP-0032 W4F16 control, and dynamic
W4F16 candidate. Medians below are per complete block; lower is better.

| Repeat | Variant | QKV ticks | Q/K join wait | Projection HMX wait | Host wall |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | F16F16 control | 7,752.0 | 2.0 | 13,522.0 | 2,932,292 ns |
| 1 | W4F16 control | 8,656.0 | 413.0 | 30,517.0 | 2,839,427 ns |
| 1 | W4F16 dynamic | 10,487.0 | 1.0 | 31,978.0 | 2,912,761 ns |
| 10 | F16F16 control | 7,655.0 | 1.9 | 14,354.8 | 2,517,338.6 ns |
| 10 | W4F16 control | 8,506.7 | 289.2 | 30,303.1 | 2,401,104.2 ns |
| 10 | W4F16 dynamic | 10,510.0 | 0.9 | 32,232.4 | 2,504,583.3 ns |

At repeat one, the dynamic candidate reduces the Q/K join tail by 412 ticks,
but it executes about 2,078 ticks of Norm work on the main context. QKV becomes
21.15 percent slower and complete-block Host wall becomes 2.58 percent slower
than the W4 control. At repeat ten, the candidate reduces join tail by 288.3
ticks but executes about 2,109.4 main-context Norm ticks and increases
projection HMX wait by 1,929.3 ticks. QKV becomes 23.55 percent slower and Host
wall becomes 4.31 percent slower.

The causal interpretation is that a ready Norm task is not free tail work for
the main context. Claiming it delays the main context's next projection/HMX
producer action. The saved final join is much smaller than the new serialized
main-context work and HMX starvation. More elaborate work stealing cannot
create a missing HVX context because the control already uses all four
contexts in the intended three-producer-plus-one-consumer pattern.

## Correctness and physical contract

All W4 controls and candidates are byte-identical with output hash
`f18b9abbe1487231`; F16 controls retain `704252c89780e695`. The independent
Implementation Reference reports zero mismatches and zero maximum LSB
difference. The candidate requests and receives exactly 8,388,608 VTCM bytes,
peaks at 7,843,328 bytes, reports zero intermediate DDR bytes, zero spill/fill,
one FastRPC invocation per Execution Unit, one HMX owner, and no QNN or QAIRT
dependency. The stable boot ID is
`77ccafe0-8817-4976-bdae-15af5608d4ff`.

## Decision state

EXP-0033 is `completed`, its evidence is `valid`, its `local_gate` is `fail`,
and its `adoption_status` is `rejected`. Dynamic ownership did not reduce QKV
or complete-block Host wall at either repeat count, so the experiment stops at
the contract boundary. Fused QKV weights, changed HMX tiling, or direct
QKV-to-Attention execution were not attempted and require a separately
approved experiment with a new hypothesis.

The source is retained at commit
`663ac0de9fe3a07756fec2e410d0d049fc35ec6b`. Formal evidence is retained at
`D:\llm_exp\results\qwen3-block-htp\exp0033\20260828T152328Z_663ac0de9fe3`,
with binaries at
`D:\llm_exp\models\qwen3-block-htp\exp0033\artifacts\663ac0de9fe3`.
