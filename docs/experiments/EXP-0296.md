# EXP-0296: long-context throughput optimization loop

User authorizes continued loop optimization of all unmet long-sequence throughput gates. This record owns Qwen0.6B then Qwen1.7B; Llama3B follows under its own registered authority. Llama1B is a regression control, not an unmet target. Frozen EXP0295 packages, actual arithmetic, static scales, no rotation, native per-channel W4/SP2, FP32 residual retained. No new calibration, PPL or recipe changes. No automatic baseline promotion.

Initial source binding to EXP0295 only for preflight followed by branch creation codex/exp-0296-long-throughput-loop. Scope: exact long-attention layout/vectorization and producer-consumer overlap, including single-row decode dispatch. Up to four measured engineering candidates per model; no gate relaxation or repeated sampling of a frozen failed formal candidate. Start with parallel decode admission, then measured attention preparation/softmax or native KV bottlenecks. Preserve opt31 same-binary control and untouched short-native reference. Non-SP2/A16 paths are frozen.

Numerical/physical prerequisites: independent existing actual-arithmetic oracles, 64/65/128/129 and536+46/741+3 all-layer/KV/head checks with poisoned future cache. Existing exact8MiB VTCM, one HMX/DMA owner, one RPC/fullmodel chunk, zero timed intermediate DDR/spill. Preserve original failed evidence and independent reference; attributable repairs allowed, no accepting unexplained hashes.

Final selected candidate receives five short checks then ten frozen rotated formal repeat10 cycles including native64, long64, control31 and candidate at536+46 and741+3. Parity lower95 CI>=1 against faster measured M64 reference; unchanged decode10percent guard versus matched opt31. M64 speed must not be deliberately degraded. Report all samples, exact timing boundaries, additive module ledgers and overlapping counters. Long wall includes Host input/RoPE staging; native64 retains its historical boundary. Persist original failures and distinguish implementation pass from throughput gate.

Update desktop HTP hardware workbook A/B/C with eligible new exact-length measurements and archive replaced rows in D. No named-dataset claims. Commit/push authorized implementation and evidence normally. Device serialized across project authorities.
