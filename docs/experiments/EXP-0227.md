# EXP-0227: Fixed-coordinate block reconstruction of final W4 row scales

User approval: 2026-09-06, "好的，按你的思路实现", referring to the recommended first experiment: freeze rotations, compare original and learned coordinates with equal-budget final-format block reconstruction. This is not approval to resume rotation learning, add teacher-logit training, change weight grouping or other recipes.

## Fixed protocol before execution

- A0: EXP0224 A; R0: EXP0225 step100, chosen because its independent EXP0226 64-window actual validation NLL3.561694 is lower than EXP0226 step1003.573788. No choice uses qbh scores. A/R are their reconstruction candidates.
- All197 GPTQ integer-code matrices remain byte-identical to their respective input packages. Optimize only196 transformer projections'573440 per-output-row FP32 scales. Embedding, LM head (codes/scales), all norms, rotations, activation precision, runtime and both other recipes stay frozen.
- Each scale is s0*exp(theta), theta initialized zero, constrained to[-log(2),log(2)]. Forward uses FP16(code*FP32 scale), identical to packed deployment expansion. Codes never use RTN again and no GPTQ pass follows training. This is an OmniQuant-inspired block reconstruction adaptation with fixed GPTQ codes, not a full OmniQuant reproduction or learned equivalent transforms.
- Teacher is independently reloaded original BF16 checkpoint, freshly folded in FP32 for R using the verified EXP0225 rotation, then FP16 execution. Original A retains gamma. No mllm folded or LPBQ weights.
- Train on frozen EXP0221 64x128 bilingual GPTQ calibration (8192 tokens,32en32zh). Keep coverage unchanged to isolate the objective intervention. Validate on frozen independent EXP0226 64x128 equal-language/equal-domain Wiki/news windows. Verify data identities and overlap separation before training. qbh-lite-v1/holdout never trains or selects.
- Sequential28 blocks. Teacher propagates its own FP16 hidden stream; student propagates its selected final-format quantized stream. Minimize full-block output MSE against teacher output, normalized by teacher output mean square. Inputs include prior quantization drift; student includes attention/residual/MLP/QK norm/RoPE.
- Per block100 Adam steps, batch8 balanced4en4zh, seed227 same batch indices across A/R, learning rate0.003 cosine, no weight decay, FP32 scale parameters, FP16 forward, FP32 loss. Clip global gradient norm1; fail on nonfinite loss/gradient/weights. Parameter log bound is part of method, not evidence tolerance.
- At steps0/50/100 measure block normalized MSE on all64 independent validation windows; select minimum, tie earliest. Propagate chosen packed weights to subsequent blocks. Retain all checkpoint scales and loss curves. Selection never uses full-model qbh. Report full-model independent actual-package NLL at completion, not just local reconstruction gains.
- Unit oracle: independent NumPy decode/dequant, exact zero-step baseline reproduction, scale gradient finite-difference check away from FP16 rounding boundaries using FP32 analytic core, train/packed forward parity and independent dense block forward. Original FP32 folding invariance thresholds remain cosine>=0.99999/NRMSE<=0.003. Check non-head/code/rotation hashes and finite scales.
- GPU smoke runs separately and may not become the selected trained package. Save per-block checkpoints/progress for recovery. Implementation-only repairs preserve failed evidence under PC037; no hyperparameter tuning based on qbh.
- Both final candidates receive software validation/qbh, DSP quick/full/repeat and1warmup5short10four-way formal paired A0/R0/A/R with unchanged ABI108. Correctness/physical/ledger thresholds unchanged. Report local reconstruction, independent NLL, frozen qbh PPL/tasks and full profiling/E2E.
- Effectiveness per coordinate: lower DSP qbh NLL and more strict tasks than own frozen control. Incremental rotation compares R to A on both. Report mixed results honestly, no automatic promotion. Failure of quality effectiveness does not invalidate a correctly executed diagnostic.

## Provenance and storage

Source parent b90d9a7309fe470fb7dc7b8de83b483621ad481c; branch codex/exp-0227-w4f16-block-scale-reconstruction. Models /mnt/d/llm_exp/models/qwen3-block-htp/exp0227; results /mnt/d/llm_exp/results/qwen3-block-htp/exp0227. Record reference algorithm URLs/commit and consumed immutable manifests before work. Inherited layer replay references remain historical placeholders.
