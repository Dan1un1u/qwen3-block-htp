# EXP-0247 — Dense HMX online R3 single-layer gate

PC061 conditional continuation, approved user2026-09-08. EXP0246 both frozen software scopes improve versus A8 and matched recalibration. Parent662386e6acf53f63bbadb3d1e283fc2c1e893555; EXP0246 ledgerde6b2a5bcd10369a9c0f188ae11af115e04f951fb3f970a1b0a0d5914f9e25dc. Branch codex/exp-0247-hmx-dense-r3. Temporary parent binding only for branch setup.

## Scope and immutable inputs

One real C64 layer0, packed W4 codes and per-output-channel scales copied exactly from verified EXP0230/C64 manifest7de4f0758d83f2ba3b58696c695bfbfed72a25dd3bf308475abee0a0f0575a89. No weight regeneration, grouping, LPBQ, W4-to-S8 expansion, R1/R2/R4, training, butterfly or FWHT. F16 and W4A16 recipes frozen. Two performance arms: current direct-W4 A8 layer with original EXP0246 A8 parameters; dense R3 layer with frozen EXP0246 R3_L0 parameters. No range optimization on device outputs. Only Q/K parameters differ. Use the first fixed EXP0246 calibration document for replay input; EOS151645 followed by first71 body tokens, M64 plus8 real M1 steps. Layer0 input is the unchanged packed FP16 embedding, quantized with its frozen input parameters. All RoPE positions and original norm weights preserved. Cache starts empty; EOS key passes through the same online transform once, subsequent stored K remains in rotated basis without history transformation. No synthetic imported KV.

Device program uses explicit dense FP16 HMX matrix multiplication of post-RoPE Q/K FP16 carriers by S128 (+/-1), with normalized FP16 conversion scale round_fp16(1/sqrt128). This arithmetic has an explicitly rounded normalization constant and is not asserted bit-equivalent to EXP0246 FP32 normalized software GEMM. Matrix generated by parity directly, exact dense packing roundtrip. Float64 independent direct matrix oracle uses the declared FP16 normalization constant. Every stage, layout conversion and synchronization is inside full-layer timing. First implementation may establish a QKV completion barrier before dense Q/K processing, reusing dead projection VTCM storage; any resulting pipeline cost counts against the same10percent threshold.

## Numerical and physical gates before formal timings

Verify all parent inputs and model manifests; fresh per-arm package manifests before hardware. Check U8 parameter serialization, score/AV integer configuration and packed weights against independent unpackers. Source and binary hashes, device boot identity and remote hashes recorded. Initial read-only ADB/toolchain readiness may stop with an explicit external prerequisite before building or deployment.

Separate audit runs capture pre-rotation carriers, dense outputs, Q/K U8 and actual append cache. Compare all live rows against independent Float64 dense GEMM with rounded normalization: output error<=one FP16 ULP plus minimum-normal FP16 tolerance for subnormals; U8 result differs at most1 code from independent rounding. Exact parity/orthogonality and identity layout fixtures, finite outputs, deterministic replay, cache length/layout/append and no double rotation required. DSP scalar dense matrix multiplication is an audit oracle only, never timed candidate fallback. On a real layer, HMX versus scalar-oracle final U8 max difference<=2 codes and cosine>=0.999; if this fails, diagnose before timing without threshold changes. Native W4 projection math remains unchanged and independently checked. Exact repeat1/repeat10 output hashes within each arm, with immutable packages, remain mandatory.

Physical timing gate:8MiB requested/granted VTCM, no timed intermediate DDR read/write or spill/fill, one FastRPC per step. Diagnostic dumps excluded from timed runs and separately accounted. First fixed five short paired rounds then ten formal paired rounds, both repeat1 and repeat10, alternating order. Profile complete Host wall, prefill64, eight continuous decode1 steps, module/additive ledger, DMA/HMX/HVX/resources. Bootstrap paired rounds seed24710000, report nominal95percent ratio interval. Either workload stably >10percent slower stops before full-model escalation and discussion. If close to threshold permit only ten extra pairs; unresolved uncertainty pauses. No full-model run within EXP0247. No E2E inference from single-layer timing, no baseline promotion or model-quality claim from this device speed gate.

Finish complete report, user-required applicable profiling table, evidence ledger and memory closure; PC037 attributable recovery applies. If an external device prerequisite is unavailable, record it and preserve a concrete resumable breakpoint.
