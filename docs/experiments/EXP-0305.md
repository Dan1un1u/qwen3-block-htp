# EXP-0305: Qwen1.7 FP intermediate motivation companion

User authorizes real hardware profiling of floating nonlinear islands for the paper motivation; implementation choices autonomous, correctness mandatory, speed and PPL not gates. Serialize device after L32-0063 closure. Reuse current Qwen native-W4 per-channel matrix runtime, FP32 residual/Norm, no rotation, uniform INT16 Down. Preserve verified historical Qwen weights/parameters if recoverable; verify retired payloads against retained manifests before reuse, never accept unknown hashes. If regeneration is needed, original weights only with new provenance, no silent historical replacement.

Port L32-0063 vector FP32 SwiGLU with fused DQ/INT16 Q and existing vector FP Softmax. QK/AV stay integer; QK Norm/RoPE unchanged. Do not add redundant standalone QDQ or scalar exponential loops. Same four-context operator-boundary attention schedule and nonstreamed GateUp->SwiGLU diagnostic; preserve intra-operator vector parallelism, HMX and DMA double buffering. Additive phase shares include dispatch/join and are NOT production critical-path shares. Combine nonlinear+QDQ rather than inventing an internal split. No quality/speedup/default promotion claim.

Component independent arithmetic audit, singlelayer/chain and full28 own-reference correctness precede formal profiling. M64 plus one subsequent fixed decode for correctness only; prefill is the plotting scope. Five short and ten formal repeat10 after warmup. 8MiB fixed VTCM, one HMX owner, no timed intermediate DDR/spill, no CPU fallback and additive ledger checks mandatory. Preserve all attempts/provenance/logs. Routine owned repairs autonomous, gates unchanged. Results results/qwen3-block-htp/exp0305. Existing datasets/workbook and other recipes/models unchanged.
