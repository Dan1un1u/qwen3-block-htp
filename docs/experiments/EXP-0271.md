# EXP0271: user-requested full-model FP32 residual speed confirmation

User explicitly asks "先跑一次全模确认速度" after EXP0270 singlelayer speed failure. This authorizes a bounded full28 diagnostic measurement despite short prefill >10percent. Historical failure stays failed; no promotion or threshold relaxation. Numerical/physical correctness remains required. Parent8268f8f7f5234902f7dd1dffc7c1a6194bae4b7a; temporary parent branch binding permits new codex/exp-0271-fp32-fullmodel-speed after preflight.

Freeze C1+C3 no-rotation native-W4 SP2 FP32 residual arithmetic, all Qwen weights/LUT/scales, original integer attention/U8KV and original offline EOS prefix. Llama/other recipes unchanged. No new speed tuning, quantization or PPL. Only missing multi-layer/frontend glue and independent reference/deployment scripts may be repaired, preserving attempts. PC037 applies.

Reuse sealed selectedlayer checks; next independent consecutive3 M64+decode1 and repeat determinism. Full28 M64+15 actual token-in/token-out with embedding/finalnorm/head/greedy, same prompt, cache128 and prefix policy as EXP0268. Check full physical layer ledgers and deterministic token/logit-code outputs, original integer control against sealed EXP0268; independently check changed high-precision frontend/finalnorm/head boundaries on actual captures where available. Preserve any quality failure separately; this does not accept PPL or text quality.

One fixed diagnostic campaign: one repeat1 run per control/candidate, then two repeat10 pairs in AB/BA order. Warm prepared weight reuse, report total prefill64tokens and decode15tokens denominators, module table and E2E. Not a five/ten-round formal gate; report diagnostic observed ratios without statistical promotion. Speed never stops the requested fullmodel run. No optional resampling, no baseline replacement. Close evidence/source/memory after reporting measured speeds and limitations.
