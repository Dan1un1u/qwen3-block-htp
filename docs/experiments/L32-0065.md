# L32-0065: complete integer/fusion and fair AV-to-O ablations

User2026-09-22 authorizes both proposed core ablations to completion, with no intermediate milestone stop. Historical shared SP2/INT16 carrier pipeline/layout/co-packing evidence is reused without relabeling measurements. Workbook E is out of scope. Llama1B first, Qwen1.7 companion under its own authority afterwards. No automatic baseline promotion or PPL/model-quality claim.

Fix no rotation, per-output-channel packed W4, uniform INT16 Down, FP32 residual/Norm, existing KV and other boundaries, 64-token prefill+42 fixed-token decode. Reuse verified L32-0062 control/folded metadata and owned FP-island implementation; verify required files against retained manifests. Regenerate retired payloads only with explicit provenance and independent checks; no accepting unknown hashes.

Core group1: F vector floating Softmax/SwiGLU with necessary QDQ vs I integer nonlinear with semantically required separate conversion/operand publication vs M fused integer path. Use matched release/worker/scheduling choices and common vectorization/intra-matrix DMA double buffering; avoid scalar or artificial native roundtrips. Register concrete split boundary before measurement; if a requested global split is inseparable, report bounded actual boundary and do not claim all QDQ removed. Core group2: optimized valid-row standalone AV RQ vs multiplier/zero-point folding into O; share M when arithmetic and scheduling permit. No softmax denominator deferral, new quantizer training or topology changes. Additional conditional split attribution is optional only after completing core groups.

Prove independent component/own-contract arithmetic, exact unchanged-path regression, selected layer, consecutive3 then full16. F is a different nonlinear approximation and uses its own independent reference. AV folding retains known FP reassociation caveat; audit clipping and own references, never claim bitwise old-equivalence unless measured. Preserve all failed attempts and fix owned bugs autonomously. All8MiB/VTCM/no timed intermediateDDR/spill, one HMX owner, oneRPC/pass and no CPU model fallback contracts remain.

Five short then ten fixed formal rotated/paired repeat10 rounds with same warmup/token traces and full Host wall for prefill/decode; repeat1 auxiliary. Deliberately slower diagnostic controls do not stop at10%. Record exact flags/source/binaries/data, additive ledgers and overlapping counters separately, and paired descriptive95%bootstrap CIs. No sample selection. Device serialized across projects; ownership recorded here while active.

Outputs: /mnt/d/llm_exp/results/llama32-htp/l32-0065 and source tools/docs; full comparison/report before closure. Add separate workbook ablation sheet only after complete combined two-model results, preserve all existing sheets and E. Completion requires source/memory clean and synced, evidence hashes, device release and companion completion; no milestone-only final response.
