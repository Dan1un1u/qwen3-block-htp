# EXP-0308: matched Table A Down precision comparison

User authorizes audit, repair missing common optimizations, paired remeasurement and rebuilt workbook A updates for Qwen3-0.6B/1.7B. Llama1B separately L32-0068; Llama3B excluded. Register initially against clean parent branch solely for preflight and creating codex/exp-0308-table-a-down-precision; then bind new branch. Initial phase is offline only: shared device remains L32-0068; acquire only after Llama device release and no hardware task running.

Both Qwen sizes use full28, M64+42 fixed A-table prompt and token fixture, per-channel native W4, FP32 residual mode2, no rotation. Identical current binary, common weights/non-Down quantizers, KV capacity/layout, attention/Norm/head, workers, buffering and generation flags. Only Down input A8 versus uniform INT16 scale/LUT and one/two byte matrix implementation differ. A8 uses existing U8_PREFILL_OPT3 to match INT16 mode8 GateUp producer schedule, streaming and Down epilogue; no removed optimizations. Qwen original AV saturation contract retained; no unconditional fold. Both arms share prefix injection setting by model (0.6 none,1.7 frozen prefix), common original A fixed trajectory. Historical EXP0307 decode token fixture mismatch explicitly documented.

Retired derived payload recovery from device is authorized for this requested test only and must match immutable old manifests. Missing historical offline tensor evidence cannot be recreated as original; exclude only demonstrated unused files with recorded scope, never silently alter hash expectations. New package manifests identify original provenance. Validate independent same-arithmetic references, exact selected/three/full28 layers and full fixed trajectory heads, deterministic padding, integer bounds,8MiB VTCM and zero timed intermediateDDR/spill. No model quality claim.

Five short and ten fixed formal alternating AB/BA paired repeat10 per size, full Host wall including embedding/finalNorm/head/greedy/FastRPC, repeat1 auxiliary, no optional stopping. Bootstrap20000 paired rounds. Report E2E plus additive module and relevant overlapping subcounters separately. No arbitrary specialization/performance target beyond investigating actual mismatches. New measurements update only six scoped A rows across two experiments plus corresponding evidence/archive; B/C and Llama3B frozen. No Selected promotion. Results /mnt/d/llm_exp/results/qwen3-block-htp/exp0308. Source/memory clean synchronized preflight before each stateful stage; serialized device.
