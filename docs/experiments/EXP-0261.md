# EXP-0261 — W4A8 full-width dense R4 exploratory cost

User requests an exploratory W4A8 R4 overhead experiment. Parent EXP0260 source0210d840497a134f7256defd7c7c8244e271482a; W4A8 arithmetic/control from sealed EXP0259. Scope single real layer0 M64 plus8M1, frozen EXP0247 R3 fixture, same optimized R3 OPT2/nativeW4/wideNR64 in both arms. F16/W4F16 and Selected baselines unchanged. No mllm.

R4 covers all6144 intermediate channels, R=H12 tensor H512, each normalized; Paley12 and Sylvester512 sign matrices independently checked orthogonal. Explicit dense HMX multiplication only, never butterfly/FWHT. Two factor GEMMs implement the full transform with declared FP16 conversion rounding between stages. Rotation starts from unquantized FP16 SwiGLU values reconstructed through a fresh half LUT from the existing gate/up U8 values, before middle U8 quantization. Store new LUT in existing VTCM LUT space; rotation scratch reuses phase-dead HMX activation and expanded-weight arenas with explicit lifetime checks. Include preparation/transpose/quantization, matrix materialization and lost streaming overlap in full-block cost. Down remains native per-output-channel W4; recompute W_down R from original checkpoint, never old folded weights. Single fixed RTN[-7,7] scale/row for exploratory transformed Down, other W4 unchanged; weights/scales are not claimed activation-only matched. Existing middle static range retained as a declared speed fixture, no fitting/PPL/quality claim. No grouped quantization.

Independent checks before timing: original checkpoint and parent evidence/package hashes; exact matrix orthogonality/dense-factor equivalence and unquantized xW identity in Float64; fresh W4 independently unpacked; half LUT exhaustive256x256 codes checked; real capture gate/up unchanged across arms; each dense stage compared to Float64 dot oracle on its actual input using declared rounded normalization, <=oneFP16ULP plus min-normal tolerance; final U8<=1code; downstream integer projection/physical layout checked independently; repeated full outputs/KV exact within arm. Full-layer ideal-oracle max2codes/cosine.999 remains a separate diagnostic gate, never relaxed. Known R3 whole-layer ideal failure unchanged; no accuracy acceptance implied by requested cost test. If new R4 implementation component fails, repair before timing; no silent fallback.

Five short then ten rotated paired formal rounds, repeat1/repeat10. Primary complete-layer Host wall prefill/decode and paired ratio95CI bootstrap10000seed261. If any stable regression>10percent, stop and discuss; if uncertain, at most10additional predeclared pairs with samebinary and no tuning. No fullmodel within EXP0261; E2E N/A, never extrapolate. Physical8MiB/no timed intermediateDDR or spill/oneFastRPC, all additive and overlap ledgers retained. Component audit DDR is explicit and disabled for timing. Numerical correctness separate from model-quality acceptance and speed eligibility. PC037 bounded attributable recovery; immutable failed attempts. Complete reports/provenance/hash seal/source/memory clean+synced, no promotion.
