# EXP-0043 — Close the W4U8 complete-block timing ledger

## Approval and source

Approved by the user on 2026-08-29 to continue from the completed EXP-0042
source head `d5f68b72b799a5c8df6c4f5cecfda218edc1e23d`. The source branch is
`codex/exp-0043-w4u8-block-time-attribution`. EXP-0042 is the provisional W4U8
source parent. This attribution experiment does not promote EXP-0042 or change
the user-selected `W4F16-EXP0038-NORMS` baseline.

## Problem and hypothesis

The formal EXP-0042 repeat-ten profile reports about 13.17 thousand qtimer
ticks per block outside its named exclusive stage ledger, roughly nine percent
of the complete DSP interval. This is an instrumentation defect: the interval
must be divided into adjacent, mutually exclusive activities before it can
support optimization decisions.

The working hypothesis is that the gap is composed of existing W4U8 boundary
work and scheduler handoffs that are currently measured only by overlapping
engine counters or not bounded at all. Candidate categories include activation
pack/unpack, projection setup and teardown, worker publication/join, HMX-owner
handoff, and final output preparation. No category is assumed to be the cause
until adjacent timestamps prove it.

## Fixed implementation contract

- The Execution Unit remains the real Qwen3 layer-14 complete block at `M=64`.
- The EXP-0042 arithmetic, qparams, tensor layouts, VTCM plan, one-HMX-owner
  schedule, worker counts, command grouping and DMA descriptors remain fixed.
- One FastRPC invocation, an exact 8 MiB VTCM grant, zero intermediate DDR,
  zero spill/fill and no QNN path remain mandatory.
- Source changes may add compact timestamps, counters, field serialization and
  offline validation only. They may not change an operator, numerical result,
  dependency, buffer lifetime, prefetch distance, tile size or schedule.
- The instrumented and uninstrumented modes must produce the same output hash
  and the same declared engine-work counts.

## Measurement method and gates

The DSP records an ordered Block Timing Ledger from the first timed block
activity through the final output boundary. Every ledger entry is bounded by
adjacent timestamps and assigned exactly once. Engine Work Counters remain
diagnostic and are not added to the exclusive ledger.

Formal repeat-one and repeat-ten runs compare the instrumented candidate with
an uninstrumented EXP-0042 control in paired order. Completion requires:

- top-level exclusive intervals reconcile to the complete DSP interval;
- every nested ledger used in the explanation reconciles to its parent;
- median unattributed time is no more than 0.1 percent of the complete DSP
  interval at repeat one and repeat ten;
- output hash, implementation-reference result and physical gates remain
  unchanged;
- instrumentation overhead is reported and must not exceed one percent of Host
  wall at repeat ten for the attribution build to be used in later profiling.

If the overhead gate fails, the attribution result may still be retained as
diagnostic evidence, but the instrumentation may not remain enabled in the
normal performance path. EXP-0043 has no speed gate and cannot change the
Selected Baseline.

Formal evidence will be retained under
`D:\llm_exp\results\qwen3-block-htp\exp0043`; generated artifacts will be
retained under `D:\llm_exp\models\qwen3-block-htp\exp0043`.

## Formal result

The source implementation and validator are fixed at
`638fef441f358a3a4703ccbf5b86a2ef5c3c9d46`. Eleven paired control and
instrumented rounds were collected for repeat one and repeat ten. The formal
record is
`D:\llm_exp\results\qwen3-block-htp\exp0043\20260829T114058Z_638fef441f35_formal`.

At repeat one, the instrumented complete invocation is 148321 median qtimer
ticks per block. The exclusive ledger names 148312 ticks and leaves 8 ticks,
or 0.00539 percent, unattributed. At repeat ten, the invocation is 146869.6
ticks per block, the ledger names 146863.5 ticks and leaves 5.5 ticks, or
0.00374 percent, unattributed. Both are far below the 0.1 percent closure gate.

The former approximately 13.2 thousand tick gap is the boundary around
`qbh_run_w4u8_streaming_mlp`. At repeat ten it contains 13163.1 ticks: 6574.5
ticks convert the post-attention-normalized row-major U8 activation to the
integer-HMX activation layout before Gate and Up, 6573.5 ticks convert the Down
integer-HMX output layout back to row-major U8 before the final residual, and
only 15.3 ticks remain for the surrounding control and handoff work. The two
layout conversions account for 99.885 percent of that boundary and explain
99.958 percent of the former gap.

The result disproves the hypothesis that the missing interval is primarily
hidden HMX, HVX or DMA waiting. It is real synchronous layout work that was
absent from the previous additive ledger. The natural future optimization
hypothesis is therefore a native-layout handoff from the post-attention norm
to Gate and Up, together with a native-layout handoff from Down to the final
residual, rather than tuning engine overlap against an imaginary scheduler
gap. EXP-0043 does not implement either change.

## Gates and disposition

The control and instrumented variants have byte-exact final output, the same
implementation-reference result and equivalent declared physical counters.
Both retain one FastRPC execution unit, one HMX owner, the exact 8 MiB VTCM
grant, zero intermediate DDR, zero spill/fill and no QNN dependency. Median
paired instrumentation overhead is 0.0658 percent at repeat one and 0.1157
percent at repeat ten, below the one-percent gate.

EXP-0043 is complete with valid evidence and a passing local attribution gate.
It is an attribution-only result with `adoption_status: not_applicable`; it
does not change the user-selected `W4F16-EXP0038-NORMS` baseline, and EXP-0042
remains the provisional W4U8 implementation parent.
