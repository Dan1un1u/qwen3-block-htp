# EXP-0255 / PC069: static A8 residual trajectory calibration

User 2026-09-09 says continue after EXP0254 residual repair recommendation. Fixed C64 original per-output-channel signed[-7,7] W4, existing dense R3, EOS151645, all other recipes and binaries. Only static U8 parameters at residual_mid/out may change; SwiGLU-only secondary control separately declared. No runtime extra transforms, grouping, mixed precision, training, weights or device execution in this software phase.

Calibration: reuse sealed EXP0246 128 calibration documents (32/cell); first79 body tokens, M64 then15 sequential one-token calls, actual all-A8 R3 wide_nr64 arithmetic and U8 cache. Collect pre-QDQ residual_mid/out and SwiGLU with baseline parameters fixed during collection; never update parameters online. Fit using eight fixed full-channel token vectors per document at positions0,9,18,27,36,45,63,78 (1024 vectors/site); preserve all calibration values' min/max and saturation diagnostics, explicitly report fitting subsampling. Exclude EOS warm prefix. No PPL fitting or labels used.

Static candidate grid per site: original qparams first; then independent lower/upper magnitude coverage percentiles [0.99,0.999,0.9999,1.0] for negative and positive tails on fixed vectors (missing sign uses0), covering zero; qparams FP32 scale/inverse and U8 zero derived with unchanged floor(x*inverse+zero+.5). For each of16 bounds, integer zero offsets[-2,0,+2], clamped[0,255], same scale; deduplicate exact scale/inverse/zero. R_MSE selects lowest mean squared reconstruction error on each residual site. R_FAN selects normalized reconstruction MSE plus normalized next RMSNorm output MSE with equal coefficients; residual_mid uses same-layer post_attention_layernorm gamma/eps, residual_out uses next layer input norm or final model norm. Norm objective computed independently from module hooks in FP32, respects unchanged gamma. Original candidate is eligible; deterministic tie index. S_MSE independently changes only SwiGLU using reconstruction MSE. All56 residual sites calibrated together per method, no layer search, no iterative refitting, no joint residual+S search. Midpoint QDQ remains before shared skip/norm fanout; residual_out once.

Freeze numerical protocol and fresh256documents before fitting: development128 and final128,32/cell each, M64+16targets, batch4. Document/fulltext/token32 disjoint all historical evaluation/training/calibration including EXP0254. Independent retokenization. Seed255. Existing32doc historical development only reproduces F,C64,R3_float_core,R3_wide_nr64 exactly; not selection data.

After candidate parameters freeze, score B,R_MSE,R_FAN,S_MSE in both unchanged FP32 TwoSum floating core and actual wide_nr64 integer attention proxy on new development panel. Choose one residual candidate among B,R_MSE,R_FAN by minimum worst-mode equal-cell deltaNLL versus same-mode B; tie rounded1e-6 favors B then R_MSE then R_FAN. This is fixed research selection, not model acceptance. Seal selection before final scores. Final fixed arms F,C64,B_float_core,B_wide_nr64,selected in both modes,S_MSE in both modes; duplicate selected B reuses exact control evidence. No selection or tuning on final panel.

Numerical checks: original floating/integer/R3 source hashes and sealed component proofs unchanged; independent NumPy FP32 U8 scalar/round/clamp oracle on candidate parameters including zero, half-step neighbors and large saturation; FP16 decode exact. Fit objective independent NumPy reconstruction of chosen and original candidates on saved vectors, normalized RMSNorm arithmetic tolerance1e-5 relative or1e-10absolute. Verify allowed changed-site set, unchanged other params/weights/prefix, actual U8 caches, rotation counts, residual invocation counts, exact shared residual fanout and sentinel, repeat/causal/CE checks. Full model weights before/after each phase; source/protocol/data/parameter identities frozen. Retain candidate local error and next-norm error on its own executed development trajectory as validation diagnostics, no refitting.

PPL primary: equal-cell mean NLL; paired stratified document bootstrap10000seed255, nominal95% intervals overall/percell. Original overall5percent and everycell10percent gates vs F16, point and confidence separate. Pair candidate versus B by arithmetic mode, no device-PPL claim. F/C64 frozen references, C64 not mathematical floor. No optional stopping/extra candidate. Software failure or insufficient quality closes with report; hardware eligibility still requires existing whole-layer correctness, which dense R3 has not yet passed. Future eligible hardware5short10formal,stable>10percent complete Host-wall stop unchanged, separately registered integration. No baseline promotion. PC037 attributable repairs preserve failures and gates. PC027 profiling/E2E N/A.
