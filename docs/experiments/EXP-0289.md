# EXP-0289 — Qwen3-0.6B W16A16 / W4A16 baseline
User explicitly authorizes implementation and device baselines with HVX vector arithmetic, FP16 HMX, basic DMA double-buffer/compute overlap; no deep tuning. Start from EXP0288 closure5b64c31 and new codex/exp-0289-qwen3-06b-a16 branch; temporary current-branch binding only for preflight before branch creation.
Original0.6B weights and model config verified against EXP0288 original provenance. F16F16 uses original FP16 weights; W4F16 reuses verified fresh0.6B C64 per-output-channel W4/FP32 scales and FP16 execution, never1.7B weights. Preserve Q/K norms, tied original embedding/head semantics, existing floating mathematics and no rotations/SP2 in A16 arms. Freeze prior W4A8-SP2 artifacts/evidence. Do not inherit rejected FP16 residual experiment.
Audit dimension split hidden1024 vs attention2048, FFN3072,28layers and generation length. Reuse EXP0283 FP16 fair refresh OPT3, EXP0260 W4F16 vector decode conversion/movement, valid-row and safe basic head prefetch as applicable. No intentional scalar baseline or kernel deep tuning. Any necessary vectorization uses independently validated fixed floating numerical contracts, no threshold weakening.
New packages isolated exp0289, hardlink only verified immutable identical data, no old hash rewrites. Independent selected0/14/27 and consecutive3 gates before full28, followed by actual greedy text diagnostics and full-boundary verification. Preserve 8MiB grant, one HMX owner, one RPC per token, no timed intermediate tensor DDR/spill and complete additive ledgers. Keep compiler local traffic distinct.
Primary M64+42/cache128/batch1, same frozen prompt and fixed-token timing across A16 arms; actual greedy diagnostics separate. Five short then ten rotated formal rounds, repeat10 primary, repeat1 auxiliary. Retained SP2 M64+42 values historical nonpaired unless separately remeasured. New recipe baselines have no prior same-shape speed denominator; no artificial10percent speed veto against a different recipe. Fairness audit and correctness/physical checks precede timing. Report stable module table and measured complete E2E; no PPL, quality acceptance or automatic promotion. PC037 routine repairs apply; retain original failed evidence.
