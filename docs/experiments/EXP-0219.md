# EXP-0219 — W4F16 offline R1/R2

Authorized by user on 2026-09-05. Parent EXP-0218 b2904bbc60b25e6b80f53351655b1c585fd97db2; pinned mllm reference 2782f7ad9d1ee5e61f65d418cb89ae7217b5fb06, scripts/qwen3_block_rotation.py. Fixed positive Sylvester H2048 and H128; no random seed or search. Adapt canonical float32 OI transforms only, not the prior QNN/LPBQ runtime.

A: frozen original per-channel W4. B: identity gamma fold then same W4 quantizer. C: gamma fold plus R1/R2 then same W4 quantizer. Transform original checkpoint before quantization, embedding E H2048, final head (W gamma) H2048, unit residual/final norm weights. Preserve Q/K norms, RoPE and cache layout; V coordinates change in C. Original checkpoint and both other recipes immutable.

Before quantization verify independent dense-Hadamard vs FWHT and unquantized layer/full-model equivalence: float64 small algebra max error <=1e-10; full FP32 logits cosine >=0.99999, NRMSE <=0.003, all finite (existing composition tolerance; rounding differences visible). For quantized inference preserve physical/scoring gates and deterministic repeat; changed greedy tokens vs old W4 are expected, never relabel old golden hashes. Package/reference provenance explicit.

Use frozen qbh-lite-v1 SHA745331b85f7ad6c3711dc7eadb30df2efe632da8b50d9c4f3a8e2bf5e2cfe8de: quick, full 512 NLL targets/24 strict tasks/4 open prefixes and repeat determinism. Frozen BF16 teacher reused. Eight holdouts stay unused. Quality effectiveness requires C lower NLL and more correct short tasks than A; B attribution reported. No tuning on outcomes. Same-runtime W4 A/C warmup then 5 short and 10 alternating formal sessions, M64+15. Speed report-only (offline transform should add no runtime operations). Archive full module/numeric ledgers; user table retains frozen other-recipe historical columns, ends in direct E2E throughput. No automatic baseline promotion.
