# L32-0064: Llama3B FP intermediate motivation extension

User authorizes extending Figure b to Llama3.2-3B and Qwen3-0.6B, adding their percentage plotting data to existing workbook G. Figure a is frozen pending independently collected historical-code Qwen0.6 data; no merge now.

This experiment owns Llama3B only. Reuse L32-0063 HVX floating SwiGLU and Softmax, native integer W4 projections/QK/AV, FP32 residual/RMSNorm, no rotation and uniform INT16 Down. No automatic AV-O scale-fold promotion. Restore original deployment payloads from device only against retained manifests, or regenerate from originals with new provenance if needed. Do not change old hashes or model quality claims. Reuse vector/native layouts and matrix-internal DMA overlap; adapt dimensions, ownership and reference arithmetic as required, no deliberately scalar control.

Match M64 full-model prefill plus one fixed subsequent decode for correctness. Use operator-boundary QK->Softmax->AV and GateUp->SwiGLU diagnostic, four vector contexts (three SwiGLU workers), same fused QDQ/nonlinear accounting as0063. Independent component, singlelayer, three-layer and full28 arithmetic before timing. Five short and ten formal repeat10 after warmup, no speed/PPL threshold, no selective resampling. Physical contract unchanged: exactly8MiB VTCM grant, one HMX owner, no timed intermediate DDR/spill or CPU fallback, additive exclusive ledgers. Work sums never stacked as wall time. FP nonlinear+fused QDQ combined; these are diagnostic-schedule fractions, not production critical-path percentages or QDQ-only costs. Device ownership serialized with Qwen companion; no metadata scans/builds concurrent with timing. Routine repairs autonomous, failed evidence preserved.

Results results/llama32-htp/l32-0064. Archive full modules/raw logs/configs and percentage CSV; after both models complete, extend G only, keep A-F and historical Figure a unchanged. No baseline promotion.
