# EXP0266 — Qwen3 freeze and Llama 3.2 repository organization

PC080 user approval covers the two proposed development branches, both based on frozen EXP0265. Register before source work; temporarily bind source branch after each new branch is pushed, and rerun preflight. No runtime arithmetic or model artifacts change.

Deliverables: explicit baseline manifest pinning source/build/model/report identities and historical measurement scope; branch-specific recipe defaults with config resolver validation; model-specific configuration boundary and migration documentation; separate Llama memory and development worktrees; Qwen3 frozen source restored. Keep existing CMake/native files and old research entrypoints intact, catalog rather than erase them. Model version/checkpoint is unresolved, hence Llama execution must be explicitly marked unsupported until porting is validated.

Validation: independent native/build-file tree hash equality against parent, exact common files across branches except declared branch defaults, source/report identity checks, configuration rejection checks for incompatible model/rotation/package combinations, Python syntax/help checks, Git clean/branch/origin synchronization and memory bootstrap. No new performance, PPL, device, build or model result. Historical EXP0218 F16 uses ten sessions each RPC repeat1; EXP0259/260/265 use formal repeat10 and these scopes must not be conflated. Preserve known ideal-R3 numerical failure and unaccepted R4 quality.
