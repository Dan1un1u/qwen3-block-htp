# L32-0025 fast HMX incremental repair

User directs improving the original fast implementation. Speed and correct
hardware implementation take priority; defer model-quality PPL/text work.
Retain mode3 as diagnostic control, not the production starting point.
One bounded native candidate: move R4 H16 normalization from converter to
matrix entries (exact +/-0.25), converter unity. Preserve H512, R3, original
HMX call counts, native layouts, overlap, SP2 LUT, weights and independent goldens.
No extra GEMM, online scalar recomputation or butterfly. Keep opt-in mode1.
Check singlelayer0/7/15 M64+decode1 components<=1ULP+minnormal, finite,
conditional actual tail exact, ideal minrowcos>=.999; compare stage1/stage2
and SP2 differences to sealed0022. Check chain3 then full16 if prior gates pass.
Preserve all original CLI exact-reference failures and existing full16 failure.
No promotion for unresolved numerical failure. If numerical improvement warrants
formal timing, freeze ten paired OFF/candidate cycles, 1warmup+10measured
M64+decode1 replays per process, bootstrap20000 seed25025, CI95upper<=1.10
both phases; repeat1 auxiliary only, no optional stopping. Otherwise skip formal
sampling and report diagnostic timings as non-acceptance evidence.
Preserve8MiB VTCM, one HMX owner, no timed intermediate activation DDR/spill.
No new models, calibration, PPL, default changes or Qwen work. If candidate
is not helpful, restore original arithmetic through a forward commit and
record failure without replacing historical evidence.
