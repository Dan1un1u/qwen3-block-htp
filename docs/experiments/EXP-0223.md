# EXP-0223 — transformer per-channel scale/clipping before GPTQ

User approved the proposed transformer per-channel scale/clipping experiment: "那就做一下吧". Parent EXP0222 6c65970b523a80b9a9343f0b246451690332ded5. A original, B fresh gamma-fold, C fresh fixed H2048 R1/H128 R2. Compare each against its frozen EXP0221 absmax/7 GPTQ package and quality. Only196 transformer projection weights/scales may change. Keep each variant's LM head, embedding, all norm files, arithmetic and runtime byte-identical to EXP0221. Other recipes frozen, original checkpoint only, no LPBQ/group scales/learned rotations.

Fix before outcomes: per output row select scale from80 ratios p=1-i/100, i=0..79 (1.00 through0.21). Scale p*absmax(transformed FP32 row)/7; signed[-7,7], nearest-even. Minimize sum(abs(W-round_clamp(W/scale)*scale)^2.4) over all input columns; ties retain largest range. This is weight-local clipping selection, not evaluation search or post-GPTQ candidate search. Then run unchanged activation-weighted GPTQ with that fixed row scale, act-order on/inverted export,1% diagonal damping, FP64 factor/FP32 compensation, block128 computational only. Frozen EXP0221 calibration64x128 tokens, IDs hash a20ed2093b0e7fb075fe47a9532843402e9f69784c8ad0b0467194bee2fa8efd. True-sequential QKV->O->Gate/Up->Down and layers observe new quantized predecessors. Head stays frozen even though hidden input changes, isolating transformer quantizer intervention. No new text, hyperparameters or grids after scores.

Checks: independent NumPy exhaustive row-scale oracle and explicit-scale GPTQ dense elimination oracle, absmax mode parity with EXP0221, all packing/finite checks, original FP32 transformed equivalence within NRMSE0.003/cosine0.99999, staged/HF FP16 checks, frozen nonprojection files matched to manifest, full software quality and actual DSP quick/full/repeat. Retain per-row chosen ratios/scales/objectives, local probe errors, checkpoints/hashes and full provenance. Holdout unscored.

Effectiveness per variant: lower DSP NLL AND more short tasks than corresponding EXP0221 control. Incremental rotation: clipped C improves BOTH versus clipped A. These are diagnostic gates, not reasons to retune. Keep the NLL and task outcomes separate as well. No automatic promotion. Profile overview uses lowest-NLL clipped candidate (ties more tasks, then alphabetical).

If independent16speed tokens and physical checks pass, run6way A0/B0/C0 controls plus A/B/C candidates, one warmup,5short,10formal rotating rounds, M64+15, frozen ABI108 EXP0218 runtime, score work off. Report direct complete Host-wall token/s, unchanged8MiB/zero timed intermediate DDR/spill/one FastRPC/one HMX owner/no QNN. Archive960formal invocation and26880layer ledgers plus full numeric profile comparisons. Failure boundary is retained rather than substituted golden outputs. Both frozen recipe columns remain historical nonpaired references. Return results for discussion; no automatic next direction.

Index aggregate local_gate is pass if at least one of A/B/C meets its declared paired effectiveness gate, otherwise fail; per-variant and incremental rotation gates remain reported separately. This aggregation is fixed before any new quality scores.
