# EXP0275 — individual pipeline knockouts and layer scaling

Registered under user-approved PC086/A2/A6; parent EXP0274 closure338b32137cb962fdec303d0752e5431839361d37. Freeze Qwen nativeW4/SP2mode8,FP32residual2,no rotation,original weights/LUT/scales/EOS/cache128,M64+15,all native interfaces. Five arms: ALL mask0; QKV mask1; FFN readiness mask2; O+Down epilogue mask12; cross-operator O-to-Gate lookahead mask16. Each knockout retains other optimizations,vector kernels and intrinsic GEMM DMA/HMX double buffering. Existing mask implementation unchanged; do not use slowest arm as external competitive baseline. No arithmetic changes,FP32/integer comparison,weight expansion,quality/PPL or promotion.

Full-model all28 layers is primary. First independent selected layer14 and consecutive3 checks of each five-arm exact output/carriers/KV vs sealedEXP0274. Full allarms exact selectedtokens/codes plus one fullALL residual/head audit vs correctedEXP0274 before timing. Five short then ten formal balanced cyclic five-arm rounds [ALL,QKV,FFN,EPILOGUE,LOOKAHEAD],repeat10 primary; one repeat1 auxiliary per arm. All timed profiles must pass physical8MiB/no tensorDDR/spill/additive ledger and identical HMX arithmetic/weight work. Diagnostic slower arms may finish above10percent, no threshold change.

A6: same five masks with fixedlayer14 input and existing consecutive3 package; after numerical gates fixedten balanced cycles repeat10 for both scopes,one repeat1 diagnostic already supplied by audit. Report module/fullHost wall relative changes and absolute costs; fullfrontend retained separately,never extrapolate singlelayer token/s to model. Already measured A1 full-model factorial stays independent rather than merged statistically.

Attribution: existing readiness/wait/command counters plus separately compiled trace-only diagnostic build, software observed submit/completion timestamps for HMX/HVX and DMA. Production timing binary compiles tracing out; no trace logs used for timing. Timestamp intervals are observable software bounds,not measured engine utilization or true hardware busy cycles. If one boundary cannot expose reliable timestamps,report it unavailable rather than invent intervals. Trace runs remain subject to output/physical gates and are untimed attribution only. No arithmetic or baseline tuning prompted by timing outcomes; at most3 attributable tooling/scheduling fixes with all failures retained.

Use pairedbootstrap20000seed275 on ten cycle means,95%CI for each knockout/all ratio,not an additive division of performance. Finish fullmodule tables,trueE2E,layer-scaling report,evidence seal and closure. Remaining Llama phases,A3,A5,A8,A9 separately registered under standing approval. Initial source binding to parentbranch permits clean preflight then additive codex/exp-0275-pipeline-mechanism-ablation creation. Device ownership serialized with L32-0029.
