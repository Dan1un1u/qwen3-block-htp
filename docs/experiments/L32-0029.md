# L32-0029 — Llama full-model format × pipeline factorial

User confirms full-model paper experiment list on2026-09-15. Parent0ae07a92e4d99fd3fd23c89adc3ce4a2b44f7b4f; selected0018 arithmetic/Norm remains fixed, SP2mode8,FP32residual1,noR3R4,original16layer2048/8192/head64 Llama packages,capacity80,M64+15. Qwen model data never reused.

Reuse validated Qwen EXP0274 interface/scheduling method only. B11 native+pipeline(0,0), B01 modular+pipeline(3,0), B10 native+operator schedule(0,31), B00 modular+operator schedule(3,31). Format bits1 both per-block Norm outputs row-major followed by vector consumer packing;2 SwiGLU compact16-bit reconstruction then vector native low/high packing. Preserve original Llama Norm worker count/order/math and exact LUT gathers; no numerical optimization backport. Pipeline bits1 QKV→Q/K preparation after complete QKV,2 Gate/Up/SwiGLU readiness after whole producer,4 O epilogue inline,8 Down epilogue inline,16 disable cross-operator O→Gate lookahead. All arms retain same intrinsic GEMM DMA/HMX double buffers/nativeW4 and decode low/high row co-pack. Frontend/head unchanged and included in full timing.

Before timing: verify sealed0018 ledger and all reused model/reference hashes locally/device; new build/assembly tensor spill audit. Exact independent layers0/7/15 and physicalKV, consecutive3, full16 exact frozen outputs and generated token/logit reference. All-on reproduces0018; other arms match. Existing full16 independent0016 reference can be reused with pinned provenance but new movement/scheduling checked. 8MiB,oneHMXowner,no timed tensorDDR/spill and additive ledger mandatory. Up to3 attributable fixes per boundary, retain originals. No relaxed thresholds, PPL/quality or baseline promotion.

After gates one repeat1 per arm auxiliary then5short+10formal four-arm cyclic order[B11,B01,B10,B00] rotated bycyclemod4; repeat10 primary, complete warm Host prefill/decode. Bootstrap20000seed29029,pairedCI for conditional effects and interaction T11*T00/(T10*T01). Deliberately slower controls no10percentstop, numerical/physical failures block downstream. No resampling or timing-dependent tuning. This includes all-on reproduction, not a separately optimized baseline. Do not assert every tensor format or every form of overlap is disabled.

Remaining independent pipeline knockouts,decode co-pack,A8/SP2 withFP32,layer/shape scaling and log2/FP vector softmax remain separately registered approved work. No W4-to-S8 or integer residual arms.
