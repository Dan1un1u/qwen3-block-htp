# L32-0053: Llama3B long-context throughput loop
User requests continuing loop optimization for unmet long-sequence throughput gates. Frozen L32-0052 Llama3B SP2/FP32/no-rotation math, weights and fixed fixtures; native per-channel W4, no new quality/calibration work. Llama1B historical gate already passes, retain its dispatch as regression. Other recipes frozen.
Port only exact head128 long K/V preparation, causal range pruning and parallel decode admission validated under Qwen EXP0296, preserving Llama wide8/exact normalization rather than Qwen NR64. Up to four engineering candidates, existing opt31 same-binary control. No artificial M64 slowdown or automatic promotion.
Six-shape64/65/128/129/536+46/741+3 independent layer/KV/head and future-cache poison checks; exact8MiB VTCM, one HMX/DMA owner, zero timed intermediate DDR/spill, one RPC/fullmodel chunk. Original tolerances and failed evidence retained. Five short then ten frozen rotated repeat10 formal cycles for final candidate and opt31 controls. Preserve prior L32-0052 long64+3 reference; test native64 as additional reference if compatible. Gate long/M64 prefill lower95CI>=1 against faster same-binary measured control, matched decode slowdown upper95CI<=1.10. No named-dataset claims. Record Host staging boundary, full ledgers, binary/model evidence. Desktop Excel updated with exact-length results and prior rows retained in D.
CPU/source work allowed while Qwen owns device. No Llama device use until Qwen explicitly releases it. Routine repairs and normal commits/pushes authorized.

Engineering candidates registered before any device use: C1 opt927 is the exact Qwen port; C2 opt1951 adds repaired matching V LUT banks after65536 simulator code/lane checks; C3 opt3999 reuses the existing full64/16-row-worker Norm path for partial multirow prefill (only live rows consumed/appended). Keep decode1 and Llama1B unchanged. This directly addresses the observed slow partial-tail dispatch, not new quantization. Validate C3 across all six shapes and C1/C2/control at741; select the lower engineering mean prefill wall among valid C1/C2/C3 before freezing formal. If an earlier candidate is selected, complete its other five audits before formal. Include same-binary old long64 in addition to native64 and candidate long64; parity uses the fastest of these three.

Fourth and final engineering candidate C4 opt8095 removes redundant K/V destination clearing only for head128, where aligned DMA overwrites every byte the packers read. Packers continue to synthesize padding. C4 receives all six audits; C1/C2/C3/control each741, and any selected earlier candidate gets the other five audits before formal. Select lowest engineering741 prefill wall among the four mathematically valid candidates before freezing the seven-arm formal protocol. No device use has started when this amendment is recorded.

## Completed
See L32-0053-RESULTS.md. Numerical/physical checks pass; unchanged performance gate `fail`. Evidence ledger `8f624c63219b3515e98839beac2c5ed5c63db035b12f1f1271a5e7634e63dc6c`. No model-quality or selected-baseline promotion.
