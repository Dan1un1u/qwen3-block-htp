# EXP-0299: generic A16 ABC completion
User 2026-09-19 authorizes sequential completion of missing W16A16/W4A16 ABC rows. Fixed project-owned fixtures, A=64+42, B=536+46, C=741+3; no named-dataset/PPL evaluation. Preserve A8/SP2 and D history.
Implement missing generic chunked prefill with persistent KV, absolute RoPE, valid tails and full token-boundary head. Retain existing math, weight encodings, residual policy (Qwen0.6 W16 promoted FP32 with full-M64 decode scheduling; other A16 FP16). Basic DMA double buffering and vector computation only; no per-model specialization/tuning, deliberate baseline slowdown or mixed precision changes. Existing generic short optimizations remain available fairly.
Only if matched W4A16 full Host wall exceeds W16A16 by >10% in either stage investigate weight expansion/overlap and perform bounded generic repair; report uncertainty. Long/M64 throughput is descriptive here, not permission for specialization.
Correctness precedes profiling: manifest/binary provenance, selected layers/consecutive chain, full stack short and long chunk/tail boundaries (64/65/128/129 plus ABC), actual arithmetic independent references and exact composition/regression checks, causal KV/poison, head/token checks. Preserve historical failed full-floating alignment as failed, never confuse implementation checks with quality acceptance. No relaxed tolerances. 8MiB VTCM, one HMX owner, one full-stack RPC/chunk or token, no timed intermediate DDR/spill, resident weights incl segmented3B and mapping costs.
Five short paired rounds then ten fixed rotated paired formal repeat10; every sample retained, full additive module accounting and Host-wall E2E, cold loading/tokenization excluded. Existing completed formal A rows retained unless matched validation/measurement needed; short-only row upgraded through formal measurement.
Source/device work serialized between EXP-0299 and L32-0055; no concurrent device owner. Derived models/results isolated under new experiment roots; sealed parents never edited, hardlinks unlinked before derived modifications. Routine owned repair authorized. Workbook updated only with verified evidence; no automatic baseline/quality promotion. Commit/push and close authority when completed.
