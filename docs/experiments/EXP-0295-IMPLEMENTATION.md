# EXP-0295 two-size migration

Qwen3-0.6B (H1024,F3072) and1.7B (H2048,F6144), full28,16 query heads/8 KV heads/head128. W4A8-SP2,no rotation,FP32 residual2, original weights/calibration. Runtimes retain Q/K RMSNorm and Qwen wide-score4/NR64 arithmetic, rather than inheriting Llama wide-score8/exact normalization. Qwen1.7 retains its frozen prefix KV; row-major cache append stores the same seed token0 before later chunks consume it.

Migrate fullmodel M64 chunk frontend, persistent cache/absolute RoPE, valid tails and final-chunk-only head. Native four-query softmax keeps raw max-score subtraction before multiplication, preserving the wide-difference contract. Head128 K/V packers and four bounded GQA clients share one HMX/DMA owner. All client slots plus the immutable V LUT must fit the phase overlay. Exactly8MiB VTCM; no timed intermediateDDR/spill. Shape bounds remain prompt<=768,decode<=63,cache832. Tests use project-owned fixed text and teacher-forced trajectories, not a named benchmark or model-quality evaluation.

Preserved failures: unsupported intrinsic spelling fixed to low-halfword multiply; accidental Llama-only unused helpers removed; long row-major generation admitted only under explicit long frontend; omitted native K/V definitions caused undefined project symbols and DSP loader error0x80000406; adding the missing compilation include restored loading. All artifacts/logs retained. The1.7 tokenizer provenance records its actual vocab/merges/config file set, not a nonexistent tokenizer.json.

Migration review caught two old Qwen scalar fallbacks: long decode V packing was excluded by a Llama-only guard, and head128 cache append still used many small scalar copies. Both now use the previously validated native vector methods; the short-cache contract is preserved.

Independent CPU reference retains the SDK-v79 rsqrt ISA simulation from EXP0294 for Q/K normalization; it consumes only reference-derived denominators. No device activations are injected. Qwen1.7 prefix seed is frozen and hashed separately. Six boundary/long shapes and a sequential-vector long control are checked against all-layer hashes, all valid KV, unchanged poisoned future cache and head IDs/codes.

Before formal scoring, the control is explicitly opt11 (native vector K/V and native4 softmax, sequential GQA); opt31 adds bounded client overlap. The newly ported opt0 scalar V path was diagnostic only and is not used for speedup claims. Both arms receive basic vector implementation. Fresh native64 retains existing short segmented-cache format, and long-m64 is measured separately; parity uses the faster short control. Long wall includes input/RoPE staging; original short wall excludes that small host preparation, conservatively favoring the control. Five short rounds, ten rotated six-arm repeat10 cycles; all samples and module ledgers retained. No baseline promotion or PPL claim.

The historical formal_artifacts.yaml appended EXP0289 items at the wrong indentation. A format-only repair preserved every stripped line/value and restored standard YAML parsing; no historical artifact hash was changed.

## Remaining decode scope
The migrated four-client long-attention path is admitted only for logical_rows>1. One-row decode retains the existing sequential dynamic attention path and full-prefix row-major K/V preparation. Its short-cache native segmented path is separately retained as native64 control. Diagnostic0.6B same64+3 comparison attributes most added decode time to QK/Softmax/AV, while projection/Down/head times remain similar. Formal long controls use matched contexts; passing their guard does not mean native-short decode efficiency was preserved. A long-cache decode specialization is outside this prefill migration and remains future work.

## 1.7B fixture cache-slot repair
Native64 passed, but first audit65 stopped before DSP launch with vertical-layer package audit failure. Frozen1.7 package had only segmented-native cache files; long row-major cache filenames were absent. Added explicit empty K/V cache and reference allocation slots in new immutable *-cache832-a02 packages. Every original payload is retained byte-for-byte; old manifests and failed run remain. Source9593bfa changes only fixture preparation Python. Rebuilt1.7 all four native binaries are hash-identical to3a72cd5. Independent CPU references are unchanged; audits refer to final-audit-index.json. This does not bypass a numerical check or substitute expected outputs.
