# EXP-0220 — gamma-fold attribution and R2-only

Approved by user 2026-09-05 after EXP0219 negative result and mathematical analysis. Parent 10a5a19e125c9d7894d3102aa3e289a68c39f914. Original checkpoint only, no LPBQ assets. No active device/runtime changes to F16F16 or W4U8.

Ordered work: (1) host original FP16 versus original-FP32-gamma-fold-then-FP16 full qbh-lite-v1 quality control, layer-output drift and actual-input conditional projection error for sample0/sample20; no W4 in this precision control. (2) DSP W4F16 QKV-only, Gate-Up-only, final-norm/head-only gamma folds with all other baseline package bytes retained. (3) DSP R2-only candidate: shared normalized Sylvester H128 per V/O head; retain every gamma and disable R1. Frozen original W4 and full-fold EXP0219 B quality reused, without rerunning historical exploration.

All changed tensors generated fresh from Qwen3-origin; existing symmetric one-output-scale W4 quantizer and unchanged ABI108 EXP0218 speed binary. No new quantizer, grouping, seed search, R3/R4 or DSP specialization. Pure FP16 quality/rounding effects are report-only diagnostics; require finite results, preserve existing FP32 unquantized equivalence cosine>=0.99999 / NRMSE<=0.003 for R2. Device quick/full/repeat physical and deterministic scores mandatory. Frozen qbh-lite-v1 512 targets/24 tasks/4 open prefixes; holdout remains unused. R2 effectiveness requires lower NLL and more correct tasks than original W4; no retrospective gate changes. Regardless of quality ranking, archive R2/control one warmup, five short and ten alternating formal M64+15 speed sessions if independent normal tokens and physical gates pass; report-only speed, no automatic promotion.

Conditional projection diagnostics evaluate Q(W)Dgamma and Q(WDgamma) on the same real RMS-normalized incoming hidden states, preserving FP16 cast order. These host diagnostic errors are not DSP measurements and not broad quality claims. Localize folding contributions without claiming their losses add linearly. Follow-up full-R1 quantizer adaptation needs a separately declared hypothesis after this attribution run.
