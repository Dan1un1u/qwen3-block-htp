# EXP-0234: static group128 with65536 calibration positions

Second user-approved PC052 phase; execute only afterEXP233 closure and own active preflight. One G64 candidate; no PPL-based parameter selection. This protocol is prepared before EXP233 final scoring.

## Fixed method and comparisons

Reload original Qwen3-origin FP32 tensors. Reuse exactly EXP231 group128 CPU quantizer: original-column static groups128, signed[-7,7], FP32scale pergroup, globalactorder, damping.01, FP64factor thenFP32, true-sequential FP16 calibration, three complete GPTQrange trials(absmax/midpoint/L2.4clip) and peroutputrow fullprojection SSE selection. No rotation/AWQ/learnedrounding/LPBQ. Only calibration expands from EXP231 G8's nestedC8 8192tokens to exact EXP230C64 65536tokens (512x128). Both standalone CPUquantizer and calibration-forward arithmetic remain unchanged; use16threads and existingcorrectness tolerances. Original gamma, QKnorms, embedding, finalnorm and inheritedEXP221A head remain frozen.

Evaluate F, C64, G8=EXP231G128, G64 using the same canonical GPUFP16 software backend. Main group attribution comparesG64/C64 atmatched64K; budget attribution comparesG64/G8 withsamegroupformat. F/C64/existingG8 are frozencontrols. No groupDSP adoption or baselineselection.

## Input freeze and correctness

Reuse hash-verified EXP233PC052panel as a disclosed paired evaluation panel, frozen before these three phases; do not call it a new independent dataset perphase. It is independent of training/calibration; EXP233scores do not select G64algorithm/budget. Reuse exposedEXP230 development128 only fordescriptive comparison/exactF/C64/G8 regressions. Beforeexport, freeze references to panel,calibration,protocol,originalshards and priorpackages. Independent groupdense-Schur/NumPyclipping/output-choice/permutation/dead/zero oracles; all196packedNumPyroundtrips;112staged-vs-HFforwardchecks;28finiteFP16(512,128,2048)hiddencheckpoints. Preserve allcodes/scales/rangechoices/sourcearchives/commands.

## Decision

Primary512documents8192targets then fixed512reserve8192targets only if anyG64/F interval crosses its applicable gate. Evaluate allfour variants for activatedphases, identical M64+16tokenization/masks. Paired stratified documentbootstrap5000seed234. Overallratio1.05 and eachlanguage/domain/cell1.10 unchanged. Pointfail=fail; allpoints+upperCIpassing=pass; crossingatmaxwithpassingpoints=inconclusive. No furtherdata/parameters fromscores. ExistingF/C64 EXP233scores can be verified/reused byte-for-byte on identically activatedphase, clearlylinkedprovenance. Othercontrol/newG64 score fresh.

Host-onlyformatdiagnostic; fullprofilingreport declareshardware/E2EN/A with verifiedEXP230historical references. No projectionofsoftwaretiming. No baselinemotion. Regardlessofoutcome, closeEXP234 thenperform approvedEXP235C64sensitivity.
