# EXP-0032 — W4F16 Gate/Up critical-path pipeline

## Approval and status

Approved by the user on 2026-08-28 after reviewing the complete-block
EXP-0031 continuation evidence and selecting the W4F16 Gate/Up interval as the
next optimization target. The experiment starts from source commit
`3638b167afd687939786f714fc48987021c02d94`. Using that valid continuation as
the source parent does not accept its pending checkpoint and does not promote
a Selected Baseline.

## Motivation

In the EXP-0031 continuation repeat-ten matrix, Gate/Up occupies 23.397
thousand ticks, or 50.47 percent of the W4F16 complete-block ledger. It is
0.43 percent slower than the paired F16F16 Gate/Up interval despite W4F16
storing only one quarter as many weight bytes. The W4F16 block remains 3.59
percent faster overall only because Down and O projection are 27.57 and 13.65
percent faster respectively; QKV is still 11.45 percent slower.

The tested hypothesis is that the current Gate/Up critical path is governed by
the publication and ownership order between packed-W4 User DMA, HVX expansion,
HMX consumption, and the streaming SwiGLU consumer rather than by compulsory
weight bytes. EXP-0032 changes only that physical schedule.

## Fixed contract

- The Execution Unit remains the complete real Qwen3-1.7B layer-14 block at
  `M=64`, measured at repeat one and repeat ten in one FastRPC invocation.
- F16F16 remains the paired EXP-0031 continuation control. W4F16 retains the
  same W4 values, per-channel scales, FP16 activations, projection arithmetic,
  output layout, standard Softmax, GQA pipeline, QKV readiness overlap,
  streaming SwiGLU, Down projection, and residual arithmetic.
- The candidate may change only the W4F16 Gate/Up packed-weight DMA,
  expansion-region publication, bounded lookahead, HMX command issue, and
  Gate/Up-to-SwiGLU readiness schedule. It may not pre-expand weights in DDR or
  change weight, activation, scale, accumulation, or rounding mathematics.
- Every mode requests and receives exactly 8,388,608 VTCM bytes. Gate, Up,
  activated Gate-by-Up, and Down input remain in VTCM or accelerator state.
  Intermediate DDR, spill/fill, extra FastRPC calls, QNN, and QAIRT are hard
  failures.
- One serialized HMX owner is retained. Existing EXP-0031 Q/K head-readiness
  workers may not race with Gate/Up W4 expansion workers across block phases.
- Candidate output must remain byte-identical to its paired control and pass
  the independent Implementation Reference and non-finite checks.
- Equivalent-scope Host wall is primary. Exclusive ledger intervals and
  overlapping engine-work counters remain distinct.

## Ordered work

### A — Gate/Up dependency audit

Instrument generation-tagged timestamps around packed-bundle DMA completion,
each 32-channel expansion-region publication, HMX command-ready and command-done
handoffs, Gate/Up output-region readiness, and streaming SwiGLU consumption.
Account for the tail after the final useful expansion and identify whether the
23.4-thousand-tick interval is limited by DMA, HVX expansion, HMX ownership, or
consumer backpressure. Instrumentation is audit-only and is excluded from timed
performance collection.

### B — Bounded schedule candidates

Evaluate the smallest schedules supported by the audit. Candidate families may
include an earlier first-region handoff, a different bounded producer
lookahead, Gate/Up interleaving order, or direct region readiness publication
to the existing streaming SwiGLU workers. VTCM slot sizes, the number of HMX
commands, mathematical work, and legal DDR boundaries remain fixed unless a
separate diagnostic proves that changing one is required; such a broader change
must stop for discussion rather than silently expanding scope.

### C — Paired selection gate

Collect audit-on correctness and alternating audit-off repeat-one/repeat-ten
timing in one stable boot. A selected candidate must, at both repeat counts:

- reduce the W4F16 Gate/Up exclusive interval relative to the W4F16 control;
- make the W4F16 Gate/Up interval strictly faster than paired F16F16;
- reduce complete-block W4F16 Host wall without regressing F16F16;
- keep W4F16 strictly faster than F16F16; and
- preserve byte-identical output, exactly 8 MiB VTCM, zero intermediate DDR,
  zero spill/fill, one FastRPC invocation, and one HMX owner.

No fixed percentage target is imposed. If no candidate passes, stop with the
valid diagnostic evidence instead of migrating a local microbenchmark win into
the complete block.

## Evidence destinations

- Source branch: `codex/exp-0032-gate-up-w4-pipeline`
- Source worktree: `/home/daniuniu/work/qwen3-block-htp`
- Retained artifacts: `D:\llm_exp\models\qwen3-block-htp\exp0032`
- Formal results: `D:\llm_exp\results\qwen3-block-htp\exp0032`

## Dependency audit and rejected candidates

The instrumented control showed that a literal earlier 16- or 8-output-channel
publication did not improve the useful critical path. It reduced some raw W4
expansion work but increased pool wait, HMX wait, unpack overhead, and
synchronization. This is consistent with the EXP-0028 result: publishing
smaller regions more aggressively creates more readiness traffic and stronger
HVX/HMX interference. These region-16 and region-8 candidates are retained as
valid negative diagnostics and were not selected.

The next comparison separated a real W4 improvement from a generally better
HMX schedule. Gate/Up was issued in groups of four output tiles for both W4F16
and F16F16. This reduced Gate/Up HMX command count and improved both variants,
so the original F16 batch-two schedule was no longer accepted as the fair
speed comparator. The fastest valid F16F16 batch-four result is the control
used by the final local gate.

## Selected physical plan

The selected candidate is `w4_b4_dma8_cache`, implemented at source commit
`91ba193660513bd0675c0a091db199aa09df1773`. It combines three changes within
the approved Gate/Up scheduling boundary:

- Four output tiles are submitted per HMX Gate/Up command. Across the complete
  block this reduces HMX command count from 352 to 256.
- Packed-W4 prefetch advances eight bundles per DMA stage. The second
  compressed slot uses the otherwise-unused tail of the existing expanded
  weight buffers, so the expanded-weight allocation does not grow.
- The host pre-encodes the existing per-channel FP32 scales into the exact FP16
  256-byte scale blocks consumed by HMX. A 98,304-byte read-only cache is copied
  once into dedicated VTCM at invocation entry. This changes neither scale
  values nor rounding; it removes repeated hot-path scale conversion.

An early development attempt placed this scale cache in a compressed-buffer
tail that Down later reused. It therefore failed repeat-ten lifetime checking.
The formal implementation gives the cache a dedicated VTCM lifetime and is
byte-exact for both repeat counts. Peak live VTCM is 7,843,328 bytes within the
fixed 8,388,608-byte grant.

## Formal timing outcome

The formal run contains 55 records collected in one stable device boot. Values
below are medians per complete block; lower is better. `F16 b4` is the fastest
fair F16F16 Gate/Up schedule, rather than the inherited batch-two control.

| Repeat | Variant | Gate/Up ticks | Complete-block Host wall |
| --- | --- | ---: | ---: |
| 1 | F16 b2 inherited control | 23,104.0 | 2,973,698.0 ns |
| 1 | F16 b4 fair control | 21,891.0 | 2,934,375.0 ns |
| 1 | W4 inherited control | 23,197.0 | 2,914,010.0 ns |
| 1 | W4 batch4 + DMA4 | 22,924.0 | 2,881,041.0 ns |
| 1 | W4 batch4 + DMA8 + scale cache | **21,695.0** | **2,816,667.0 ns** |
| 10 | F16 b2 inherited control | 23,267.1 | 2,598,375.0 ns |
| 10 | F16 b4 fair control | 21,935.3 | 2,532,182.3 ns |
| 10 | W4 inherited control | 23,660.6 | 2,496,734.3 ns |
| 10 | W4 batch4 + DMA4 | 22,917.3 | 2,452,099.0 ns |
| 10 | W4 batch4 + DMA8 + scale cache | **21,786.2** | **2,384,614.6 ns** |

At repeat one, the selected W4 Gate/Up interval is 6.47 percent faster than
the inherited W4 control and 0.90 percent faster than the fair F16 batch-four
control. Complete-block Host wall improves by 3.34 and 4.01 percent
respectively. At repeat ten, Gate/Up improves by 7.92 percent versus the W4
control and remains 0.68 percent faster than fair F16; complete-block Host wall
improves by 4.49 and 5.83 percent respectively.

The mechanism is visible in the repeat-ten diagnostic counters. Expansion
falls from 15,639.1 to 14,245.2 ticks, HMX wait from 18,366.4 to 15,686.9,
packed-weight prefetch wait from 911.7 to 402.8, and hot scale initialization
from 184.1 to 11.5 ticks. Because Host wall and the exclusive Gate/Up ledger
both improve, this is not merely a re-attribution of overlapping work.

## Correctness and physical-contract evidence

All F16 controls are byte-identical with output hash `704252c89780e695`; all
W4 candidates are byte-identical with output hash `f18b9abbe1487231`. The
selected W4 result has zero reference mismatches, zero maximum LSB difference,
maximum absolute error 0.0625, and cosine similarity approximately
0.999999996. All 118 parent Block Package hashes were audited.

The formal run keeps Gate, Up, activated Gate-by-Up, and Down input in VTCM or
accelerator state. It reports zero intermediate DDR bytes, zero spill/fill,
one FastRPC invocation per Execution Unit, one HMX owner, an exact 8 MiB VTCM
grant, and no QNN or QAIRT dependency. Static and runtime gates both pass.

## Decision state

EXP-0032 is `completed`, its evidence is `valid`, and its `local_gate` is
`pass`. Its `adoption_status` remains `pending`: this record does not
automatically promote the candidate to Selected Baseline or replace the
accepted EXP-0031 Provisional Source Parent. Formal evidence is retained at
`D:\llm_exp\results\qwen3-block-htp\exp0032\20260828T133911Z_91ba19366051`,
with artifacts at
`D:\llm_exp\models\qwen3-block-htp\exp0032\artifacts\91ba19366051`.
