# EXP-0228: Content-aware short-task scoring audit

User approval 2026-09-06: verify whether current short-task failures are logical or formatting errors; accept correct content despite presentation differences and relax matching or strengthen prompts. This authorizes a separately versioned scoring amendment, not rewriting frozen evidence.

Host-only scope: inspect the existing frozen qbh-lite-v1 outputs; introduce qbh-content-v2 deterministic task grading alongside original strict-v1. Keep prompts, generated token IDs, models, calibration, PPL, speed and all historical evidence immutable. No device/model/training run; profiling and E2E are N/A for this scoring-only experiment.

Rules: normalize harmless presentation; extract a unique asserted answer through bounded full-string grammars; verify arithmetic equations; parse JSON with exact values/types/keys after optional code-fence removal. Preserve case when letter conversion is the task. Do not accept answer substrings, ambiguous alternatives, negation, inconsistent equations, wrong JSON types/extra keys, missing answers or malformed/truncated output. Unsupported prose is unresolved/manual review, not automatically a logic error. This is a deterministic bounded answer evaluator, not a general semantic judge.

Apply the same rules to BF16 teacher, frozen F16F16/W4F16/W4U8, EXP0227 A0/R0/A/R and EXP0226 selected rotation. Preserve strict correctness as instruction-format diagnostic. Verify source evidence against existing authority hashes, reproduce strict grades, inspect all changed/failed answers, and test independent synthetic positive/negative cases (substring collisions, arithmetic contradiction, JSON type/duplicate-key errors, negation and case-sensitive transformations). Keep every qbh NLL record unchanged. Version and document retrospective scoring, no retuning model/checkpoint or promotion on rescored results.

Implement reusable CLI and grading API; save per-item classification, extracted answer, reason, old/new scores, protocol/source hashes and a human-readable report under exp0228. Complete only after clean synchronized source, evidence validation and proper lock release. Next experimental direction is not authorized.
