# EXP-0229: Frozen W4F16 independent PPL acceptance

User approval 2026-09-06: complete the proposed independent W4A16 PPL acceptance, then discuss W4A8 details. This approves bounded evaluation and the previously proposed engineering gates, not weight optimization or W4U8 execution.

Freeze A0=EXP0224 A (GPTQ/output-aware scales), A=EXP0227 A (additional block-scale reconstruction), F=EXP0217 F16F16 package, unchanged EXP0218 ABI108 runtime. Verify manifests/files/binaries against prior authoritative evidence before use. No source runtime edits, model weights/scales/calibration/rotations/retraining, or other-recipe execution. Metadata/evaluation-input/host orchestration changes only. PC037 routine attributable recovery applies; never change scores, thresholds or evidence to obtain a pass.

Data: fixed seed229, four equal cells en/zh x wiki/news. Each document supplies one meaningful raw-text M64 context +16 continuation targets, no padding/chat/no overlap. Primary128distinct documents/cell=512windows/8192targets. Freeze an equally sized reserve with different documents before inference (total1024windows/16384targets). Reuse verified source corpora where available, acquire additional Wikipedia rows if needed. Exclude whole calibration/training/checkpoint-validation documents, text hashes and shared32-token sequences against all previous training/validation/qbh (including unused holdout); enforce pairwise document/32gram disjoint primary/reserve. No old validation set as final test. No model score guides sampling. Store source URLs/revisions/licenses where available, content hashes and exact offsets/tokens before any inference. Routine quick subset first32documents/cell=2048targets.

Execution: actual DSP F/A0/A with identical tokens/context/masks, self-computed KV and unchanged one-FastRPC/8MiB/no intermediate-DDR physical contract. Input shards keep existing binary limits; rotate F/A0/A ordering across shards. Before full run use balanced sentinel within-session repeat checks and frozen qbh numerical reference controls; after full run repeat sentinels. Compare every repeated target NLL/code/top1/rank/ties/saturation exactly. Require finite NLL/logit diagnostics and valid cache progression. Cache original BF16 teacher on GPU with frozen checkpoint/tokenizer, matching masks/context; it is an additional named reference, not replacement for paired DSP FP16. Verify GPU teacher repeat/cache masking on a small slice.

Primary outcome is average NLL then exponentiation, overall and en/zh/wiki/news/four-cell PPL ratios vs actual same-scope DSP F16. Gates overall<=1.05, each language/domain/cell<=1.10. Report stratified paired document-bootstrap95% intervals with5000fixed seed229replicates. If any candidate metric interval straddles its gate after primary, run the complete pre-frozen reserve for all three variants and report combined16384targets. Otherwise stop8192. After bounded budget, passing point gates with upper bounds below gates is pass; crossing intervals are inconclusive (no further automatic expansion); failed point gates are fail. A versus A0 uncertainty is reported; prefer simpler A0 if both eligible and A improvement is not robust, no automatic promotion or test-driven new checkpoint/tuning.

Scope is short-context conditional PPL, not published full WikiText/C4 or long-context acceptance. Both models evaluated regardless of early point quality. Short answers reference-only, no new short-answer generation. No new speed experiment because runtime/packages unchanged; retain explicit N/A profiling plus links/hashes to prior complete formal evidence, no throughput claim from quality runs. End with complete results and discuss W4A8 only; leave W4U8 frozen.
