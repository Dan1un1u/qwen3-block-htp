# L32-0026 H512 fast accumulation and rounding

Continuation of user-authorized incremental fast HMX repair. Inspect local SDK
contracts and frozen0022/0025 captures to distinguish normalization multiplication,
accumulation and final-half rounding. No unsupported architecture guarantees.
Up to two justified fast native candidates using normalization placement,
accumulation order or bounded extra HMX arithmetic; preserve dense multiplication,
existing native pipeline/layouts, one HMX owner and8MiB VTCM. No scalar full
recomputation, butterfly, timed intermediate DDR/spill, new weight/calibration
or reference contract changes. Mode3 remains diagnostic only. Freeze each
candidate hypothesis before hardware sampling; do not tune to evaluation codes.
Singlelayer0/7/15 M64+decode1: components<=1ULP+minnormal, actual conditional
tail exact, finite, independent cosine>=.999, structural/KV checks retained.
For a helpful locally valid candidate, chain3 then full16 independent validation.
Preserve CLI exact-reference exit1 failures. A failed candidate is not promoted;
restore original arithmetic through a forward commit if no helpful result.
If numerical/physical gates justify speed testing: fixed10 paired OFF/candidate,
1warmup+10measured replay pairs perprocess, bootstrap20000 seed26026, both
Host-wall CI95upper<=1.10. Repeat1 auxiliary only; no optional stopping.
No model-quality PPL/text work; no E2E extrapolation. If no supported native
candidate exists or bounded candidates fail, record findings and discuss next
fast-path option. No new Qwen work or other recipe changes.

## Candidate A frozen before hardware
Scale the H512 coefficient181/4096 by16 to181/256 (FP16 bits0x39a8),
and use the exact power-of-two converter scale1/16. Exact mathematical dot
product and frozen FP16 boundary remain unchanged. Same XOR generation cost,
HMX command count/layout/pipeline. Hypothesis: moving intermediate products
away from a narrow low-exponent range may reduce rounding loss; unproven.
This is not arbitrary non-power-of-two normalization in the converter. No
claim that v81 documentation describes v79 internal precision. Local v79
intrinsics expose FP16 convert/store, not a demonstrated FP32 output path.

## Candidate B frozen before hardware
Candidate A local metrics unchanged. Restore original coefficient181/4096 and
unity converter. Reverse the order of sixteen K32 tiles in H512 by feeding
one matched activation/weight tile at a time from15 down to0. Mathematical
products, per-tile channel order, accumulator lifetime and single final
conversion unchanged. No intermediate conversion, extra GEMM or layout copy.
This tests inter-tile accumulation-order sensitivity. More instruction dispatch
may cost speed; no speed claim before formal. Only opt-in Llama mode1 H512
uses the new worker command; all other matrix calls preserve original kernel.

## Closure
A leaves all six phase/layer outputs and captures unchanged. B passes local
components/tails and chain3, but full16 minimum cosines.6090736014/.7699031029
fail. Rejected; native src/include restored through forward commit and fresh
layer0 audit. No formal speed, E2E or PPL.9CLI/18RPC,1exit0/8retained exit1.
Five successful builds. No new models, changed goldens or baseline promotion.
Closure cb8be962f3f31a1b89c8f8cd2bebb9cceef049a7; ledger 599d86cf654b2b41526690c76ad1c0a251b5f206815a57ce4ae12b0024f94ac9.
Report docs/LLAMA32_H512_FAST_ROUNDING.md.
Exact integer-plane H512 is only an offline feasibility direction, not an
implemented or speed-accepted candidate. Any further native work needs a
new bounded experiment and preflight.
