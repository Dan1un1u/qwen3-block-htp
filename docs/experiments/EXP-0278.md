# EXP0278 — log2 versus FP32 HVX softmax, A9

Standing PC086 approved paper campaign. Freeze EXP0277 no-rotation nativeW4/SP2mode8/FP32residual2, all weights/scales/non-target boundaries; compare baseline wideNR64mode4 versus new explicitly named FP32vector mode7. Do not reuse modes1–6. Raw HMX QK U8 score and fixed scale/mask/token lengths identical inputs to component; FP max/exp/sum/normalize without log2 exponent rounding/cap, final same U8 probability and integer AV. Real vendor HVX FP32 exp and vector reductions/conversion, not scalar expf timed control. Ordinary A8 experiment closed; no further calibration or algorithm arms.

First component correctness with diagnostic SP2-probe extension (new non-SP2 mode5/6, unchanged previous modes) returning FP32 probabilities/U8 and stage timing. Fixed seeds278, raw-byte extremes/equal/spike/causal/random, actual archived hardware score captures/configs. Shapes M1/M64, valid lengths1/32/64/65/96/128 supported; valid<=128, causal and finite. Compare FP32 probability to independentFloat64 exp2 raw-score difference*multiplier/2^fractionbits: maxabs<=2e-6, rowmass<=2e-6, maskedexact0. Quantize probabilities consistently to U8 roundnearest nonnegative, ties declared; report any threshold differences and verify against own FP32 result ratherthan silently relaxing FP gate. Log2 own frozen integer reference. Record U8/AV difference as approximation change, not crossarm equality. Fixed5short10formal paired component timing, repeat100 batches if legal, core/packing separately, untimed floatdump. Component not E2E.

Integrate same vector core into static2head row slices and dynamiccache GQA paths, same worker availability/native output and8MiB. No scalarization or unnecessary tensor roundtrip, no timed DDR intermediates/spill. Optional probability dumps untimed only. Preserve originalall-on exact frozen outputs, selectedlayer14 andchain3 independent own branch reference/tail gates, full28 actualfinalNorm/head and frozen own frontend checks beforetiming. Do not claim unchanged numerical contract between softmaxarms or fullmodel quality. At most3 attributable implementation fixes per failing boundary, retainfailures and all samples; no numerical threshold relaxation.

Two complete28layer M64+15 cache128 paths independently: originalgreedy and mode3fixedtokens fromEXP0277SP2 frozenbeforetiming. Each5short10formal alternatingpaired repeat10primary, repeat1aux; bootstrap20000seed278 overpairedrounds. Complete modules/Hostwall/E2E and attention/softmax counters. Sameoriginalweights, HMX tilecounts andweightbytes checked; differingoutputs allowed. Slower diagnosticarm may finish>10percent, no promotion/PPL/qualityclaims or rotations. Keepfailed/incompleteFARFtimeline N/A. L32-0032 ownsdeviceuntilreleases; localworkonly inparallel. AfterclosureA8onesupportedextraM/KV remainsapproved; LlamaA9 separatephase.

## Vector row-mass final-control amendment (before new primary timings)
Prototype f599446 completed all fixed40componentcases and9600fullprofiles, allretainedinroot. It used scalar probability-code rowmass telemetry inactualFP runtime, absentfromoriginalcomponenttiming. Choosefinal FPvector rowmass implementation sharedwithLlama78560f4, removingavoidableper-columnscalar bookkeeping, regardlessofnewspeed. SameFP32exp/rounding/probabilitybytes, exactoldownrefs andbytecomparisons required. Newprimaryunderresults/vsum, new5short10formalpairedLOG2/FP pergreedy/fixed, repeat1aux. Allprototypecosts separate, no pooling/cherry-picking or reuseasfinal-control denominator. Matchedcomponenttelemetryenabledbotharms; nativeM64shuffle4control andactualdecodeareprimary, genericM64+past64scalarfallback supplementaryonly. Samephysical/numericalgates; no calibration, weightchanges orbaselinepromotion.
