# EXP-0292 — temporary Qwen3-0.6B F16F16 FP32 residual
User2026-09-18 explicitly requests FP32 residual for precision alignment with A8. Temporary recipe, not artificial slowdown or replacement of old fastest FP16-residual evidence. Only Qwen0.6 F16F16 changes. Freeze W4A16, A8, model weights, attention/KV, FP16 HMX and head. No rotations/PPL or automatic baseline promotion.

Contract: original FP16 embedding widens exactly to FP32; persistent VTCM residual and both residual sums remain FP32. O/Down keep existing FP16 projection outputs, widen and add once in FP32. Input/post/final RMSNorm consume FP32 residual, compute FP32 reduction/normalization with explicit HVX arithmetic, output FP16 to the unchanged matrix path. No FP16 round-trip of residual before norm. This aligns residual storage/add precision only, not all A8 projection output contracts. Flag0 original control retained; new F16 flag1 only in0.6 build. Ordinary vector operations and existing row worker pool, no intentional delays or optimization removal.
Independent changed-boundary reference and original-control byte regression; single-layer M64/M1 then3-layer slice beforefull28. Physical8MiB, no timed intermediateDDR/spill, oneRPC, finite/cache/head/ledger/determinism gates unchanged. Validate changed norm/add against separately expressed reference, distinguish inherited full-floating model mismatch from local implementation checks. No claim precision improves without evaluation.
Fixed original EXP0289 model weights and M64+42/cache128 fixed trajectory. Samebinary F16 flag0 versusflag1, identical optimization modes; preserve original1710.94/27.68 formal history. Five short rounds then10balanced formal repeat10 if prerequisites pass; repeat1 auxiliary; report actual overhead, no manufactured target. Short diagnosis may precede formal. Full profiling evidence and N/A where not measured. Routine attributable fixes PC037, failures retained.
Temporary parent binding for preflight then codex/exp-0292-qwen3-06b-f16-fp32-residual.

## Closure — 2026-09-18
Completed temporary implementation and formal performance comparison; local full-floating gate remains FAIL, not relabeled. Changed-boundary independent additions exact, Norm<=1FP16 ULP, selected0/14/27 andchain3 pass, wholehead43/43, original2451files exact and candidate2451files repeatedexact;4816KV snapshots pass. Full floating maxNRMSE0.0065323461 control /0.0120673062 candidate exceeds0.003; noPPL or quality/promotion claim.
Five short andten balanced formal repeat10: FP16 residual1709.1233/27.68675;FP32 residual1690.5934/28.13503tok/s, prefillwall+1.09606%,decodewall-1.59332%. New norm/residual processes validdecode rows, unchanged control retains M64 rows; comparison includes this necessary implementation choice, not an isolated datatype-cost claim. Weights and original historical1710.94/27.68 baseline retained.
First run stalled because A8-only FP32 O streaming was triggered in F16; restrict by variant and rerun. Failed attempt retained. Audit checker filename and32row native tile decoding repaired without changing kernel/tolerance. Model manifest old experiment label inherited shared export helper; manifests untouched, actual EXP0292 provenance separately recorded. No unrelated tuning or default adoption.
See RESULTS/MODULES and sealed external FULL_PROFILING_REPORT. User-requested temporary config uses QBH_FP32_RESIDUAL=1, F16F16,OPT4,ABI137; flag0 original remains. Device released; no active experiment.
