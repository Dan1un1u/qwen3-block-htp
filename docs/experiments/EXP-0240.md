# EXP-0240 — W4A8 Qualcomm LPBQ group32 single-layer speed gate

Approved: user requests LPBQ group_size32 W4A8 device speed, single layer first; user selects >10% regression in either prefill/decode as stop condition.

Original Qwen3-1.7B layer14; seven transformer projections quantized fresh to signed4 LPBQ, decompressed8, group32 K and one FP32 base scale/output row. Pin official AIMET implementation and independently verify two-level integer scale and packed reconstruction. Compare actual per-channel direct-n control and LPBQ candidate with same real replay inputs, fixed U8 activation encodings, nonlinear kernels/cache semantics and device settings. LPBQ compressed storage plus scale metadata is transferred within timed projections; expanded S8 only lives in VTCM. Preserve control packages and original shards. Reconstructed-S8 comparison and independent integer/native conversion audit verify candidate; all seven projections plus complete layer deterministic checks required before timing. No accuracy-benefit claim from this speed gate.

Implement bounded SIMD unpack/multiply and pipelined DMA/HMX schedule, exposing altered batch sizes/buffering. Same-format scheduling controls distinguish semantic overhead from implementation scheduling. Five short rounds then ten alternating paired formal rounds, complete Host wall primary, repeat1/repeat10, M64 and M1 with real self-computed cache. Report paired ratios and95% bootstrap intervals. Stop if either stable regression exceeds10%; bounded10 further pairs for ambiguity. Full-model measurement only if both within threshold; no layer extrapolation. Detailed profiling and unavailable E2E retained.

State: completed; evidence valid; local speed gate fail; adoption pending. Five short and ten formal paired rounds complete. Both prefill and decode exceed the 10% stop threshold; full model not started. Source parent ae18bbfbcaac3fceab1a7a3a642c63001ae66f2d. Results /mnt/d/llm_exp/results/qwen3-block-htp/exp0240; models /mnt/d/llm_exp/models/qwen3-block-htp/exp0240. Other recipes frozen.

Closure: [results and current handoff](EXP-0240-RESULTS.md), [complete profiling](EXP-0240-PROFILE.md). Final reporting source e02d01c0d40603bb80fcc9e292d106a7d72be0b9. No baseline promotion.
