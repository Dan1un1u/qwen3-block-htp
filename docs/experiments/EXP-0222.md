# EXP-0222 — host-only LM head precision ablation

User approval: first perform LM head ablation, discuss results before choosing further work. Parent EXP-0221 08a67eee666fe5ca7335a8150f7207635019b588. Scope is the previously proposed software diagnostic, not DSP integration or performance tuning.

Freeze all A/B/C EXP-0221 packed transformer weights/scales, embedding, norm parameters and evaluation. Reconstruct each actual package independently in unchanged HF FP16. Compare its W4 GPTQ head against a fresh FP16 head from original Qwen3-origin: A original W; B W*final_gamma; C (W*final_gamma)*H2048. Retain respective final norm, including identity for B/C; do not double-fold. Detach tied head. No recalibration, requantization, clipping, new rotation or holdout scoring.

Evaluate all frozen qbh-lite-v1 full samples (512 conditional targets,24 strict tasks,4 open prefixes) for each head. Reproduce retained EXP-0221 software W4 controls with exact token/top1 outputs and NLL within1e-5; this is distinct from historical DSP scores. Repeat one English/Chinese NLL and task per configuration exactly. Verify all consumed package bytes against manifests and frozen closure, original shard hashes, head construction and unchanged non-head tensors across head swap. Same-input teacher forcing isolates head precision; greedy generation uses each head's own feedback. Primary outcomes are paired NLL delta, task count delta and A/B/C gaps after head replacement; no minimum benefit is required for an attribution experiment to complete.

No device runtime or frozen F16F16/W4U8 changes. Profiling and E2E token/s are N/A by this user-approved host diagnostic scope; CPU evaluation elapsed time is not DSP throughput. Retain complete result/provenance report with explicit unavailable profiling sections. No baseline promotion. Stop after these results and discuss with the user before any next direction.
