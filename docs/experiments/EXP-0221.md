# EXP-0221 — per-channel GPTQ with offline rotation

Approved by user 2026-09-05: "那就往这个方向做一做试试，同意你的方向". Parent EXP0220 e3a2c869b7a02061af3a976db536296c50f29850. Method source IST-DASLab/gptq 2d65066eeb06a5c9ff5184d8cebdf33662c67faf; original Qwen3-origin shards must match frozen hashes. A=original+GPTQ, B=fresh gamma fold+GPTQ, C=fresh fixed Sylvester R1 H2048/R2 H128+GPTQ. Detach tied head; quantize all 196 transformer projections plus LM head. Reuse frozen EXP0219 RTN A/B/C quality as historical controls, no repeated exploration.

Fix before outcomes: 64 calibration sequences of128 tokens,32 English WikiText-2-raw training and32 Chinese Wikimedia Wikipedia20231101.zh training. Fetch rows in ascending order, form non-overlapping128-token windows within text rows, take first32 eligible per language; fetch next pages only if needed. Exclude exact32-token overlaps with frozen qbh-lite-v1 samples, including holdout, without scoring holdout. Raw responses, exact token IDs, tokenizer/source URLs and hashes are frozen before quantization. No evaluation text/answers in calibration, no subsequent calibration/parameter changes based on scores.

GPTQ: fixed original-transformed-row absmax/7 scales, nearest-even signed [-7,7], no clipping search, groupsize=-1, blocksize128 (computational blocking only), act-order on with final inverse permutation, damping0.01*mean Hessian diagonal. Accumulate X^T X from the variant's actual FP16 sequential activations; use FP64 damped-Hessian factorization for numerical stability and FP32 compensation. Recompute each layer's calibration after already quantized preceding layers, and process QKV->O->Gate/Up->Down sequentially within each layer; final-head calibration follows quantized stack. All variants use identical calibration and settings. Row-chunk GPTQ to bound head memory, same shared Hessian. Restore existing HMX layout exactly; no runtime nodes or ABI changes.

Correctness: independent small dense inverse-Hessian elimination oracle must match blocked GPTQ integer codes, packing roundtrip exact, all finite, manual sequential calibration forward must match unchanged HF layer within existing FP16 NRMSE0.003/cosine0.99999 and FP32 transformed full-model invariance same thresholds. Quality: all A/B/C actual DSP quick/full/repeat and independent software full qbh-lite-v1, frozen512 targets/24 tasks/4open prefixes; holdout never evaluated. A improvement gate requires lower NLL and more tasks than frozen original RTN. Rotation incremental gate requires C lower NLL and more tasks than A. B attribution report-only. Failures retained, no retrospective tuning.

Regardless of quality ranking, run original RTN plus all three candidates one warmup,5short,10formal rotating order at M64+15 fixed feedback steps if independent exact speed tokens and physical gates pass. Score work disabled during speed, unchanged EXP0218 ABI108 binary,8MiB VTCM/zero timed intermediate DDR/spill/one FastRPC/one HMX owner/no QNN. Speed report-only. Stable user table uses lowest-NLL GPTQ candidate (tie: more tasks, then A/B/C order), explicitly diagnostic if quality fails, frozen other columns nonpaired; no automatic promotion. Archive all control/candidate ledgers and direct E2E denominators. Pre-speed failure reports unavailable profile sections with reason rather than new golden hashes.
