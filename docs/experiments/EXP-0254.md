# EXP-0254 — Ordered joint attention restoration and conditional attribution

PC068. User 2026-09-09 approves the proposed ordered research: joint attention-boundary restoration first, then targeted attention attribution if near C64, otherwise residual/SwiGLU joint attribution. Parent EXP0253 source6d130035cf089ba099c8f161799952bf1d70a385, ledger f8dbb4d50965546f69c2ad853bcdd05d49d3fa8c36ce79f0c64d4f967d5bec1d. Branch codex/exp-0254-joint-attention.

Freeze per-channel C64 W4, EOS151645 prefix, EXP0246 R3 and parameters, FP32 TwoSum floating attention core from EXP0253, all other recipes and native binaries. Restore selected boundaries to FP16 only as software diagnostics; no deployable mixed precision, new weights, calibration, training or hardware. R3 remains explicit dense multiplication, including prefix and body K. All unselected A8 sites use unchanged parameters. Attention mask/scaling/GQA and shared residual fanout unchanged.

Definitions across28layers: B=current R3_float_core; J=joint restoration of q_out,k_out,v_out,q_rope,k_cache,v_cache,attn_context. Include Q/K projection outputs before Q/K normalization, not just caches. V output/cache form one tied logical boundary. K restores k_out+k_cache; QK restores q_out+k_out+q_rope+k_cache; V restores v_out+v_cache; C restores attn_context. R restores residual_mid+residual_out; S restores swiglu. J leaves norm_qkv/linear inputs, O output, MLP, embedding and head A8 unchanged. F/C64 are original EXP0253 matched controls. Restored caches store FP16; other caches truly U8. Output context QDQ once if enabled, zero if restored. No probability QDQ in floating core.

Freeze both new panels before inference: triage128documents and confirmation128documents,32each en/zh wiki/news per panel,2048targets each,64context+16targets, fixed sequentialbatch4. All256documents disjoint each other and all historical evaluation/calibration/training by document/text/token32 checks, independent retokenization. Seed254. Historical32development documents only for implementation/reproduction and prespecified diagnostic scores, no quantizer fitting.

Order: numerical/cache/hook checks and development F,C64,B,J; triage F,C64,B,J; then seal route.json before any confirmation scores. Route attention if J/C64 point PPL ratio<=1.05 overall and<=1.10 every cell; otherwise route downstream. This is a prespecified research-routing heuristic, NOT a new quality gate or statistical acceptance. Confirmation is independently held out from this routing decision. Attention route fixed confirmation arms F,C64,B,J,K,QK,V,C. Downstream route fixed factorial arms F,C64,B,J,R,S,RS,JR,JS,JRS. Develop only missing selected arms, then score all selected confirmation arms without tuning or further selection. No unbounded scan or final-data-driven extra candidate. Report triage and confirmation separately; route reproducibility, pairwise effect uncertainty and interaction uncertainty mandatory.

Unchanged model quality gates vs matched F16: overall5percent, everycell10percent, point and confidence separate. Equal-cell mean NLL/PPL, per-cell results and paired stratified document bootstrap10000seed254. Restorations measure conditional effects, not additive causal shares or achievable deployment bounds. Report added A8 NLL relative C64 as diagnostic reference, not mathematical floor.

Checks: retained EXP0253 floating core hash and all280 numerical cases unchanged; new mixed U8/FP16 cache versus independently implemented FP16 QDQ cache on identical call shapes must produce exact logits for every selected mask; full per-boundary invocation counts and exact restored/pass-through or frozen-QDQ sentinel; prefix/body cache type and lengths; at-most-once Q/K rotation; zero probability QDQ; repeat/causal/CE; original whole-model/prefix hashes unchanged. F,C64,B development reproduce sealed EXP0253 every token exactly. Fresh high-range FP16 QKV random causal cases compare Float64 dense and online reference with unchanged EXP0253 normalized RMS1e-5/max2e-5scale/rowmass2e-6 tolerances. Original failed evidence retained; routine repairs PC037.

Close all scores, independent reconstruction, reports, provenance and evidence seal. PC027 stable three-recipe table records unmeasured hardware/E2E as N/A. No native FlashAttention, baseline promotion or hardware timing of diagnostic FP16 restorations. Future deployable A8 repair requires a concrete separately registered numerical protocol; successful software candidate then follows original component/whole-layer gates,5short10formal and stable>10percent Host-wall stop. Current approval covers the ordered diagnosis and conditional follow-up research; do not infer approval to relax numerical/physical/model thresholds.
