# L32-0069: HVX butterfly R4 cost on ordinary A8 boundaries

User 2026-09-23 explicitly requests another hardware ablation: ordinary W4A8 without SP2/INT16 Down, insert HVX butterfly Hadamard at the actual R4 site, measure HVX wall only; no E2E required. This supersedes the dense-only/no-butterfly restriction solely for this diagnostic.

Llama3.2-1B, intermediate8192, full normalized Sylvester H8192 after Gate/Up SwiGLU and before one fixed A8 quantization/native Down input. No rotation of already-quantized middle bytes. Isolated existing FastRPC probe; no projection execution, new model folding, quality claim, production default or baseline promotion. VTCM input at the post-SwiGLU boundary can be reconstructed from retained real Gate/Up captures when available, otherwise use declared deterministic representative A8 Gate/Up fixtures plus original qparams/LUT; never describe synthetic fixtures as real trajectories.

Implement vector butterfly without dense HMX or scalar activation loops. FP32 working arithmetic, FP16 prequantization SwiGLU input compatible with existing R4 producer; output ordinary U8. Measure pure normalized transform and separately input/output preparation. One-lane and pooled four-lane arms with identical arithmetic; decode may use one worker if parallel overhead dominates. Validate numerical output against independent float64 Walsh/Sylvester reference, relative L2 <=2e-6 and max absolute <=2e-5*max(1,maxabs(ref)), independently exact A8 output for declared staged FP32 contract; identity H(Hx), impulses, constant, random, outlier and finite-input cases. Do not impose quality or speed threshold. Preserve failures.

Five short correctness rounds then ten formal rounds, ten in-RPC repetitions, warmup outside samples. DSP qtimer wall includes synchronization when applicable, never sum worker busy times. Record single layer and directly timed sequential16-transform workload for M64 and M1; no model throughput extrapolation. Compare only explicitly historical nonpaired L32-0068 ordinaryA8 full16 Host wall (64prefill,42decode), decode per step labeled averaged historical scope. This is a cost reference, not incremental E2E slowdown or proof of all possible kernels.

8MiB VTCM grant, no timed intermediate tensorDDR/spill; load/audit outside core. Serialized device ownership, build/source hashes and remote artifacts verified. Keep existing source branch and opt-in component mode only; routine repairs authorized. Archive source, raw measurements, independent correctness, uncertainty and a report. Close experiment and release device after completion.
