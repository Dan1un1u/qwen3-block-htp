# L32-0008: SP2 Down native W4 feasibility prototype

User authorizes the annotated signed16 radix256 SP2 decomposition on Llama.
Owner no-rotation, parent b9932bd. W16/W4A16, other A8 projections, rotations,
original model and frozen Qwen remain unchanged. No quality/baseline promotion.

Stage A: define a bounded signed SP2 codebook before scoring (zero and at most
two powers, shared scale along K); independent int64 oracle. Probe actual V79
native packed W4 U8 partial accumulations and lossless wide-result extraction.
Test signed boundaries, carries, cancellation, negative weights, long K8192,
zero corrections, and decode spatial row packing. Preserve all failed attempts.
Stage B: use original-derived sealed Llama W4 Down tensors and independent
Llama activation fixtures (not Qwen data); compare ideal SP2 to decomposition
exactly and record local error versus unquantized activation/U8 separately.
Fuse producer where feasible; no earlier U8 middle rounding in SP2 generation.
Stage C: fixed ten alternating paired component timings M1/M64, identical K/N,
complete component Host/DSP timing and physical checks. Component times are
not full-block or E2E speeds and cannot pass the full-block 10% gate. If the
prototype supports full-block integration within this bounded work, require
independent single-layer arithmetic then fixed ten AB/BA paired complete Host
wall, both prefill/decode paired-bootstrap95% upper <=1.10; stop expansion on
failure. No model PPL or E2E claimed from component evidence.

Require exact8MiB VTCM acquisition, one HMX owner, one RPC per declared probe,
no timed intermediate DDR/spill. Model inputs and baseline artifacts read-only;
new artifacts results/models llama32-htp/l32-0008. Stage A impossibility or
physical failure is reportable; do not hide it with U8 partial requantization.
Ordinary owned tooling repairs are authorized, retaining failed evidence.

## Completed

# L32-0008：Llama SP2 Down 原生 W4 原型

**结论：原生 HMX W4 可以精确执行这个 SP2 码本的 Down 整数点积，无需展开成 S8。** 本次为局部原型，不是完整 block 或全模型验收。

SP2 码本固定为 241 个值（零、带符号单幂、两个不同幂之和，指数 0～14），可用 8 位索引；最大整数幅值24576。尺度来自已冻结的 Llama middle 校准范围，与评测分开。原型输入为预先重构的 signed16 值，设备将其拆成低 U8 与高 U8；尚未融合 SwiGLU 生产。

保持原有 per-output-channel signed[-7,7] W4 与 FP32 权重尺度。四次不饱和 retain 转换按24/16/8/0指数读取累加器四字节，无额外舍入；HVX 重构并按 L+256H−32768*sum(W) 合并。decode 两分量复用64行tile；prefill 两遍计算。返回精确 int32 点积，最终 alpha*beta、输出重定标及残差尚未接入。

最终20个硬件功能用例、934,912个输出均与独立整数参考完全一致；其中 Llama 第0/7/15层、prefill/decode 的 SP2 输出共399,360个。包含K32/K8192、正负极值、零、进位、抵消和两种分量调度；CPU模拟器另验证22,049个累加器。

局部 Down NRMSE（同一份W4，参考为量化Gate/Up重构后、尚未做middle量化的SwiGLU；不是FP16全模型参考）：

| 层 | 阶段 | 原min-max U8 | SP2 |
|---|---|---:|---:|
| 0 | prefill | 19.406% | 6.688% |
| 0 | decode | 70.434% | 5.287% |
| 7 | prefill | 40.951% | 3.426% |
| 7 | decode | 100.000% | 5.852% |
| 15 | prefill | 14.386% | 2.946% |
| 15 | decode | 100.000% | 4.093% |

固定10轮ABC/CBA三臂配对；每进程1次未计时warmup+1次计时RPC。完整局部Host wall包含逻辑载体载入/打包、权重DMA、计算、转换、合并及最终输出发布；不含模型加载、SwiGLU/SP2量化、最终输出scale/residual和完整block。

| 局部组件 Host wall，μs | U8单次饱和转换控制 | U8精确整数控制 | SP2精确整数 |
|---|---:|---:|---:|
| prefill | 5212.57 | 5284.60 | 6779.78 |
| decode | 962.26 | 963.55 | 987.42 |

单次饱和转换控制采用identity/sat U8，并统一扩成int32返回缓冲，以测一次转换的指令路径；它不是现有优化全block基线，也不具有与SP2相同的输出精度。U8精确整数控制与SP2均返回未损失的整数点积。

| SP2局部Host wall比值 | 对U8单次转换 | 对U8精确整数 |
|---|---:|---:|
| prefill | 1.3007 [1.2933, 1.3081] | 1.2829 [1.2726, 1.2922] |
| decode | 1.0261 [0.9915, 1.0521] | 1.0248 [1.0220, 1.0273] |

以下为SP2局部DSP分解，μs。HMX异步执行，因此MAC列是指令发射时间，转换列包含等待完成；不把发射时间当成实际矩阵计算耗时。

| 阶段 | 载入 | 打包 | 权重DMA | MAC发射 | 转换/等待 | HVX合并 | 输出发布 |
|---|---:|---:|---:|---:|---:|---:|---:|
| prefill | 716.03 | 3118.66 | 181.19 | 14.77 | 127.56 | 88.10 | 1737.23 |
| decode | 15.21 | 230.60 | 160.54 | 7.54 | 64.45 | 31.67 | 27.17 |

实际申请8MiB VTCM，峰值2,797,568B；一名HMX owner，零权重展开，零计时中间张量DDR或张量spill。64B DMA描述符位于DDR；编译器有128B符号掩码常量栈存取，单独记录，不是激活张量。60条完整Host/DSP计时账本全部对账。

初版逐字节合并成本高，改为HVX精确解包/合并及载体打包后重新验证。保留两次DMA配置导致的DSP失败、一次构建失败后误部署旧DSP的无效尝试；后者明确排除，已增加完整构建seal与部署HEAD/哈希校验。详见recovery_notes.txt。

下一步优先将SP2查表量化与SwiGLU的native载体生产融合，并接入Down输出scale/残差；随后做完整block正确性和固定十轮Host wall的10%门槛。局部结果不能判定整层门槛通过，也不能外推E2E或PPL。

**E2E token/s：N/A，本次没有运行全模型。PPL：N/A。**

Final source 0a00e0a28d7961846648922c47e4be264e6b78df; tested native4ed16cf. Evidence ledger 087f79a79669b658a1eccedde28b73e4cf71444405a755a64443bdcca850a50c (606 files).
