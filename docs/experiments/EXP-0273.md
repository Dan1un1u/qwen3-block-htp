# EXP0273: approved no-rotation ablation campaign, phase A0

User approves updated paper ablations on2026-09-15, excludes W4/S8 and integer residual comparisons, adds log2/FP vector softmax. See PC086 and docs/PAPER_NO_ROTATION_ABLATION_PLAN.md. This bounded first phase reproduces Qwen EXP0272 all-on full28 baseline; freezes native source/weights/SP2/FP32 mode2/R3R4OFF/cache128/EOS prefix. Initial parent branch binding allows creation of codex/exp-0273-paper-ablation-baseline after successful preflight.

Prepare new experiment-owned runner/result paths, independently verify prior seals and package hashes, rebuild/reseal current native baseline; do not relabel old build/gates as fresh checks. Since native src/include are unchanged from exact accepted EXP0272, inherited selectedlayer/chain3 proofs are allowed only with explicit native-tree identity and preserved sealed references. Reproduce full28 generation and physical/module closure on one repeat1 auxiliary run then five repeat10 baseline runs; archive all. This is A0 reproducibility, not formal comparative speed claims or new optimization acceptance. No integer-residual run. New comparative formal campaigns need registered frozen arm/cycle protocols under existing user approval.

Device connection/provenance check is allowed immediately after preflight; if unavailable retain transport evidence and stop dependent builds/profiling while completing approved documentation/interface analysis. A0 completion alone does not complete the approved batch. Llama native unchanged in this phase; later Llama phase owns its project-memory lock.
