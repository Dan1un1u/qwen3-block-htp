# EXP0274 — Qwen full-model format × pipeline factorial

User 2026-09-15 confirms full-model B00/B10/B01/B11 and asks to execute. Fixed EXP0272 native W4/SP2, FP32residual mode2, no rotation, original weights/LUT/qparams/EOS/cache128, real M64+15 full28. Parent dd1e3a56a5db61877cb160fb60718c4b00c33e98. Initial branch binding permits codex/exp-0274-fullmodel-format-pipeline after preflight.

B11: native producers+collaborative schedule (format_disable0,pipeline_disable0). B01: modular producer interfaces+collaborative schedule (3,0). B10: native+operator-level schedule (0,31). B00: modular+operator-level schedule (3,31). Format sites: both FP32 Norm outputs in every block produce row-major U8 before vector consumer packing instead of direct native stores; SwiGLU writes actual compact uint16 reconstruction values then vector consumer prepares low/high native operands instead of direct plane production. Preserve all arithmetic and parallel Norm reduction, LUT vectorization, intrinsic GEMM DMA double buffering. Use existing dead buffers only, no native→generic→native roundtrip, no activation spill. Frontend/head unchanged and included in all full timings. This is a model-wide intervention at enumerated boundaries, not claim every tensor layout changes.

Pipeline bits:1 QKV→Q/K preparation begins after QKV compute completes (preserve QKV DMA/HMX ring);2 Gate/Up/SwiGLU stage dispatch instead of early publish and consumption;4 O raw output epilogue inline instead of overlap;8 Down epilogue inline;16 disable cross-operator O→Gate lookahead while retaining local weight double buffers. Preserve decode low/high row co-packing; its independent ablation remains future authorized phase. Main factorial does not disable all DMA/HMX overlap or degrade vector kernels. Both schedules can use same compact consumer pack fusion where independent work allows it.

Numerical and physical prerequisites: compile/assembly tensor spill audit; independent selectedlayers0/14/27 and chain3 with frozen output/native boundaries, then four-arm full28 token/logit/residual capture compare to sealed EXP0272. Inherited unchanged math is not a substitute for checking new movement/schedule. All-on must match accepted baseline. 8MiB,one HMX owner,no timed tensorDDR/spill,additive ledger required. At most3 attributable layout/scheduling fixes per boundary; preserve failures. If resource lifetime cannot support a legitimate arm, document limitation rather than silently reduce to FFN-only.

After prerequisites,5short four-arm cycles then10formal cyclic order [B11,B01,B10,B00] rotated by cycle mod4, repeat10 primary,one repeat1 per arm auxiliary. No performance stop for intentionally less-fused diagnostic arms; no numerical gate relaxation. Use pairedbootstrap20000seed274 for main effects and interaction ratio T11*T00/(T10*T01), paired CI. Report full module table and actual warm E2E. Historical timings are not denominator. No quality/PPL claim or baseline promotion. All remaining approved Llama/A2/A3/A5/A6/A8/A9 tasks stay outstanding; no completion claim for entire campaign.
