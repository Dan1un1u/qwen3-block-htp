# L32-0016: SP2 mode8 FP32 residual integration
User approves the preceding dataflow contract on2026-09-14. Ordinary owned fixes
proceed autonomously. L32-0015 rotations are deferred, not accepted or erased.

Immutable baseline L32-0012 mode8, original frontend l32-0010/frontend-a01 and
singlelayers l32-0009/packages-a01. Keep per-output W4 weights,241-level radix256
SP2, alpha, A8 calibration, QKV/attention/KV/GateUp/SwiGLU/LMheadU8 contracts.
First phase does not recalibrate. No R1/R2/R3/R4, smoothing, grouping or weight refit.

Restore original FP16 embedding input. O and Down retain native packedW4 HMX
integer multiplication, recover signed full dot before output saturation and
scale to FP32. FP32 residual add and cross-layer storage. Both prenorms compute
FP32 then quantize into existing A8 native layouts. Finalnorm reads FP32 and
produces A8 into existing W4U8 head/U8 logits. No dequantizing an already-clipped
U8 O/Down output to pretend highprecision. No float GEMM projection fallback.

Preserve oneHMXowner,8MiBVTCM,no timed intermediateDDR/tensor spill and full
Host/DSP ledgers. Reshape owned VTCM lifetimes as needed; distinguish float hidden
boundary DMA from illicit intermediate spill. Native low/high Down raw slots and
DMA/epilogue ownership continue to apply. No speed claim until measured.

Independent reference should reproduce frozen integer attention/projection/
SP2 and FP32 boundary arithmetic; reference exact signed accumulators, report
FP32 rounding error separately, all finite. Validate isolated changed boundaries,
singlelayers0/7/15 prefill/decode, consecutive3 then full16, frontend/text and
matched lightweight PPL as feasible. Quality may remain limited by attention/KV/
head; do not gate implementation on usable text or silently relax arithmetic.

After fixed-scale FP32 evidence, independently evaluate training-only A8
recalibration and FP16 residual storage with FP32 add/norm, without conflating
changes. Keep FP32 control. Any formal speed comparison uses fixed10 paired
Hostwall and existing10%CIgate; repeat1 auxiliary. No promotion by assumption.
Models/results under llama32-htp/l32-0016; source/builds in owning worktree.
Record all attempts/seals/hashes and source versions. Frozen Qwen and other
Llama branch/float recipes unchanged.
