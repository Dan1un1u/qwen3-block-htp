# EXP-0224 — post-GPTQ output-aware per-row scale selection

User approved first proposed direction: 同意方向，先做第一项. Parent EXP0223 source c9c2491ff70875068f9a043a4305d07892cf5271. Two fresh candidates A original, C gamma-fold plus fixed H2048 R1/H128 R2. A0 control=EXP0221 A; C0 control=EXP0223 C. Freeze each candidate's head, embedding and all norm files to its control. Only196transformer projection weights/scales per variant change; regenerate from verified Qwen3-origin. Other recipes and selected baselines frozen.

## Fixed before outcomes

Frozen EXP0221 64x128=8192calibration tokens, IDs SHA256 a20ed2093b0e7fb075fe47a9532843402e9f69784c8ad0b0467194bee2fa8efd. True-sequential QKV->O->Gate/Up->Down and layers observe newly quantized predecessors. For each transformed FP32 output row compute p_clip using the unchanged EXP0223 80ratio weight-L2.4 rule. Evaluate exactly three scale vectors, in order p=1, p=(1+p_clip)/2, p=p_clip, each multiplied by absmax(row)/7 (zero row base1). For each candidate run full unchanged GPTQ: act-order/inverse export,1%H-diagonal damping,FP64 factor/FP32 compensation,block128 computational only,signed[-7,7],nearest-even.

Select each row by sum over ALL8192calibration inputs of squared projection error of its FINAL exported weight: delta=FP16(codes*scale).float()-originalFP32weight; score=sum((X.float()@delta.T)^2), FP32 matrix multiply and FP64 sum, evaluated in512token chunks. This is undamped actual-input linear output reconstruction, not damped GPTQ proxy or RTN score. Ties retain earlier/larger range. Retain all3per-row scores/scales and selected indices, packed output hashes; assert selected score no worse than either included endpoint on identical current inputs. This is a local property, not a claim of whole-model improvement or pairing across differing preceding hidden states. No changing candidates/metric based on qbh-lite or holdout.

Oracles: independent NumPy exhaustive post-GPTQ output scoring/choice, explicit-scale dense GPTQ parity, old absmax parity, zero-row ties and packed roundtrip;392projection packing checks;112staged/HF forward checks with NRMSE<=.003/cos>=.99999; original FP32 transformed invariance; all frozen package files verified. Independent16token speed references; full software diagnostic and actual DSP quick/full/repeat with scalar/token determinism. Retain56hidden checkpoints and392row-search NPZ records. Model generation and scoring are offline only.

Per-variant effectiveness: lower DSP NLL AND more short tasks than its stated control. Aggregate local_gate passes iff at least one effectiveness passes; per-metric outcomes also reported. Cross-coordinate candidate C versus candidate A is diagnostic only; also retain old A comparison. No baseline promotion. Profile overview chooses lowest new DSP NLL, ties more tasks then alphabetical.

If token/physical correctness pass, A0/A/C0/C rotating four-way1warmup,5short,10formal M64+15 with frozen EXP0218 ABI108 runtime, scoring off.640formal invocation/17920layer ledgers; complete full numeric profiling archive and stable three-recipe overview then direct E2E token/s with token counts and Host-wall denominators. Exact8MiB VTCM,zero timed intermediate DDR/spill,one full-model FastRPC,one HMX owner,no QNN. Historical F16/U8 columns explicitly nonpaired. No automatic further direction.

## Completed result

A: DSP NLL 3.873038 -> 3.660316, tasks 8 -> 19; C: DSP NLL 3.956556 -> 3.892162, tasks 9 -> 17. Per-variant gates {"A": true, "C": true}; selected C versus A cross-coordinate diagnostic False. All correctness/determinism/physical checks pass; 4way5short/10formal complete. Evidence valid; aggregate local_gate pass; no promotion. Full results EXP-0224-RESULTS.md and EXP-0224-PROFILE.md. Final source cf8a239a636bb33dd9bf49c7aa03760df9af6c81.
