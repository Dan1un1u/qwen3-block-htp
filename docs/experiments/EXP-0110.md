# EXP-0110 — W4F16 QKV specialization factorial

## Decision and scope

The user approved the first Recipe Specialization after the EXP-0109 Public
Common Layer freeze. The experiment is W4F16-only and does not modify the
frozen fair F16F16 or W4U8 configurations. It reopens two valid but previously
cross-recipe-rejected components under their declared conditions: EXP-0108's
Crouton-native `qkv_norms` carrier and EXP-0085's fixed
`q_prefix4_k_all` projection order.

## Factorial

Exactly four cells are allowed: frozen EXP-0109 W4F16 `norms` with Q-major
order; carrier-only `qkv_norms`; order-only Q0-Q3, all K, Q4-Q15, all V; and
the carrier-plus-order combination. There is no additional prefix, group,
batch, worker-count or prefetch-distance search.

The carrier keeps Q/K/V projection output in HMX Crouton order and feeds Q/K
Norm-RoPE, QK and AV without full row-major Q/K/V materialization. The order
candidate changes only projection/readiness order. Neither component may
change weights, arithmetic, qparams, tensor shape, head semantics, Attention,
O projection, MLP, residuals or the frozen public configurations.

## Gates

All cells must preserve output hash `f18b9abbe1487231` and pass the independent
QKV operand audit with zero mismatch and zero maximum LSB. They must request
exactly 8 MiB VTCM, use zero intermediate DDR and spill/fill, one FastRPC
invocation, one HMX owner and no QNN while preserving W4 bytes and HMX tile
work.

Carrier cells must report 128 skipped QKV unpack tiles, 24 direct QK operands,
eight direct AV weights and zero operand mismatches. Prefix cells must publish
exactly Q0-Q3, K0-K7, Q4-Q15 and V0-V7 in that order without delayed or early
head consumption.

A candidate is eligible only if both the QKV-plus-Q/K-Norm-RoPE interval and
complete Host wall strictly improve over control in ordinary and paired
repeat-one and repeat-ten medians. The selected candidate is the lowest
complete Host-wall cell among eligible candidates. Passing creates a W4F16
specialization candidate; it does not change the Frozen Public Baseline
without an explicit user adoption decision.

## Formal result

The implementation is fixed at source commit
`d18e901339f4eadd760b590e91f004c3f76e9cb6`. Formal evidence is archived at
`D:/llm_exp/results/qwen3-block-htp/exp0110/20260831T163813Z_d18e901339f4_formal`,
with retained binaries at
`D:/llm_exp/models/qwen3-block-htp/exp0110/artifacts/d18e901339f4/20260831T163813Z_formal`.
The device boot remained
`77ccafe0-8817-4976-bdae-15af5608d4ff` throughout collection.

All four cells preserved output hash `f18b9abbe1487231`, zero final mismatch,
zero maximum LSB and an independent three-tensor Q/K/V operand audit with zero
Q, K and V mismatch. All cells requested and acquired exactly 8 MiB VTCM,
used zero intermediate DDR, zero spill/fill, one FastRPC execution unit, one
HMX owner and unchanged W4 traffic and HMX work. Carrier cells reported three
direct QKV projections, 128 skipped unpack tiles, 24 direct QK operands and
eight direct AV weights. Prefix cells reported the exact 64-command schedule
hash `020bd2ecac75eb83` for repeat one and `75942e8f4d761383` for repeat ten.

| Repeat | Cell | Host wall | Host ordinary | Host paired | QKV plus prep | QKV ordinary | QKV paired | Eligible |
|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | carrier | 2558.178 us | -1.274% | -1.227% | 412.969 us | -6.860% | -6.476% | yes |
| 1 | prefix4 | 2574.844 us | -0.631% | -1.175% | 442.135 us | -0.282% | -0.094% | no |
| 1 | combined | 2585.417 us | -0.223% | -0.841% | 416.198 us | -6.132% | -6.228% | yes |
| 10 | carrier | 2122.677 us | -1.914% | -2.019% | 410.453 us | -6.444% | -6.136% | yes |
| 10 | prefix4 | 2171.224 us | +0.329% | -0.150% | 436.891 us | -0.418% | -0.373% | no |
| 10 | combined | 2135.484 us | -1.322% | -1.565% | 411.833 us | -6.129% | -6.223% | yes |

The local gate passes and selects carrier-only. Q-prefix4 does not pass because
its repeat-ten ordinary Host wall regresses by 0.329%; adding it to the carrier
also weakens the carrier's complete-block improvement. The result therefore
supports only the W4F16 `qkv_norms` carrier. The user accepted that cell on
2026-09-01 as the W4F16 Recipe-Specialized Baseline, tagged
`baseline-w4f16-specialized-exp0110`. The Frozen Public Baseline remains
unchanged; Q-prefix4 and the combined cell are not adopted.

Evidence hashes are: gate summary
`8eb6b07a115d5f3948cd1e0f4e761ac5721743f6a5c67d2196fb00a3144b9b4d`,
complete profiling report
`ead4696f022d643192120c64ac4bace32941eca844cb789aff215622ba5b142a`,
and evidence ledger
`d90e3b8613e122a2cac426c312e2188687983672e0623cdac30925837f9996ea`.
