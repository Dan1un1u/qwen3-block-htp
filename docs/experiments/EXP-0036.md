# EXP-0036 — Crouton-native Gate/Up-to-SwiGLU handoff

## Approval and status

Approved by the user on 2026-08-29 after reviewing the formal EXP-0032
complete-block profile. The experiment starts from the fastest valid EXP-0032
source commit `91ba193660513bd0675c0a091db199aa09df1773`. This source choice does
not promote EXP-0032 or EXP-0036 to Selected Baseline. Implementation and the
two ordered formal stages are complete. Evidence is valid and the local gate
passes; adoption remains pending because only the user may accept an experiment
or promote a Selected Baseline.

## Motivation

At repeat ten, the fastest W4F16 control spends 21,786.2 qtimer ticks, or 48.7
percent of the complete DSP ledger, in Gate/Up plus streaming SwiGLU. Gate and
Up contain twice the Down projection MACs and packed-weight bytes, but their
exclusive interval is 3.47 times Down and improves only 0.68 percent over the
fair F16F16 Gate/Up control. Down improves 29.9 percent.

The retained streaming path still unpacks each Gate and Up HMX Crouton result
to full row-major tensors, computes SiLU-by-Up into a full row-major Middle
tensor, and repacks each 64-channel group to the Down Crouton activation.
Because elementwise SwiGLU preserves element correspondence regardless of
layout, these row-major round trips are not mathematically required.

## Fixed contract

- The Execution Unit is the real Qwen3-1.7B layer-14 complete block at `M=64`,
  measured with repeat one and repeat ten in one FastRPC invocation.
- The Stage A control is EXP-0032 W4F16 batch-four + DMA-eight + pre-encoded
  scale cache at source commit `91ba193660513bd0675c0a091db199aa09df1773`.
  Attention, QKV overlap, residuals, norms, Down projection, W4 values, scales,
  FP16 activation arithmetic, HMX arithmetic, and standard Softmax are fixed.
- Stage A may change only the Gate/Up output lifetime and the SwiGLU-to-Down
  physical handoff. Matching Gate and Up HMX output tiles use bounded VTCM ring
  slots. HVX applies the existing FP16 SiLU-by-Up instruction sequence directly
  in Crouton order and writes each result to its final Down activation tile.
- Full row-major Gate, Up, and Middle tensors may be omitted from the timed
  path only if audit-mode evidence proves byte-identical Down input and final
  block output. They may not be replaced by DDR workspaces.
- Every mode requests and receives exactly 8,388,608 VTCM bytes. Intermediate
  DDR, spill/fill, a second FastRPC execution unit, QNN, and QAIRT are failures.
  One serialized HMX owner is retained.
- Equivalent-scope Host wall is primary. The exclusive Block Timing Ledger and
  overlapping DMA/HVX/HMX work counters remain separate.

## Ordered milestones

### A1 — Crouton element-correspondence proof

Audit the existing HMX output and Down activation tile address maps. Prove that
Gate and Up tiles share identical logical-to-physical element order and that a
tile-local elementwise result can be written directly at the corresponding
Down K-tile offset without transpose or row-major staging.

### A2 — Batch-four ring implementation

Introduce bounded Gate/Up Crouton output slots with generation-tagged producer
and consumer handoff. While HMX advances to a later Gate/Up group, HVX consumes
a completed pair, executes unchanged SiLU-by-Up SIMD, and writes the final Down
Crouton carrier. Remove timed row-major Gate/Up/Middle materialization and the
associated unpack/repack work.

### A3 — Stage A formal gate

Collect audit-on correctness and five alternating audit-off repeat-one and
repeat-ten runs for the immutable control and candidate in one stable boot.
The candidate must be byte-identical, strictly reduce the Gate/Up exclusive
interval, and strictly reduce complete-block Host wall at both repeat counts.
It must keep zero intermediate DDR, zero spill/fill, one HMX owner, and the
fixed 8 MiB VTCM grant. If any condition fails, stop before Stage B.

### B — Batch-eight follow-up, conditional on Stage A

Only after Stage A passes, use the released VTCM to test eight output tiles per
Gate or Up HMX command, reducing Gate/Up command count from 96 toward 48. Give
F16F16 the same batch-eight search opportunity and use its fastest valid plan
as the fair comparator. Stage B must retain Stage A mathematics and physical
contracts and improve both Gate/Up and complete-block Host wall at repeat one
and repeat ten.

## Evidence destinations

- Source branch: `codex/exp-0036-crouton-native-swiglu`
- Source worktree: `/home/daniuniu/work/qwen3-block-htp`
- Retained artifacts: `D:\llm_exp\models\qwen3-block-htp\exp0036`
- Formal results: `D:\llm_exp\results\qwen3-block-htp\exp0036`

## Implementation record

Stage A writes matching Gate and Up HMX Crouton outputs to a bounded VTCM
producer/consumer ring. The existing FP16 HVX SiLU-by-Up instruction sequence
operates directly on corresponding Crouton elements and writes the result to
the final Down-input Crouton address. The timed path therefore no longer
unpacks full Gate and Up tensors to row-major storage or repacks a row-major
Middle tensor for Down.

The first correct implementation used two ring slots. It was slower because
the HMX producer could back-pressure behind only one available SwiGLU worker.
The retained implementation uses a bounded 48-slot group ring in VTCM and
restores the four-way post-producer drain. This establishes that a zero-DDR
ring's capacity is part of the schedule contract, not merely a storage detail.

Removing the row-major Middle arena exposed another implicit contract: the
complete-block GQA implementation uses aligned bounded portions of the
Gate/Up/Middle arenas as four per-worker HMX scratch contexts. Reusing all of
Middle for MLP state corrupted Attention. The final plan preserves a dedicated
aligned 64 KiB GQA scratch reservation while still eliminating the MLP Middle
materialization.

Stage B extends both W4F16 and F16F16 Gate/Up controls to eight output tiles per
HMX command. For W4F16 this reduces Gate/Up HMX commands from 96 to 48 and the
complete-block HMX command count from 256 to 208. W4F16 peak live VTCM is
8,171,008 bytes, below the fixed 8,388,608-byte grant. F16F16 peak live VTCM is
6,875,904 bytes.

## Formal Stage A result

The formal set contains audit-on correctness and five alternating audit-off
runs for repeat one and repeat ten, with 22 records in one stable device boot.

| Scope | Repeat 1 change | Repeat 10 change |
| --- | ---: | ---: |
| Gate/Up exclusive interval | -4.16% | -3.38% |
| Complete-block Host wall | -1.41% | -1.49% |

The control and candidate have the same W4 Down-input byte hash
`b0d16655c164a22b` and final output hash `f18b9abbe1487231`, with zero
implementation-reference mismatches. Stage A therefore passes its correctness,
physical-contract, and speed gate.

## Formal Stage B result

The Stage B set contains 44 records covering W4F16 batch four/eight and F16F16
batch four/eight under the same correctness and alternating timing procedure.

| W4F16 batch 8 versus batch 4 | Repeat 1 | Repeat 10 |
| --- | ---: | ---: |
| Gate/Up exclusive interval | -9.87% | -10.46% |
| Complete-block Host wall | -3.76% | -5.25% |

The W4F16 batch-eight candidate is also faster than the fastest valid F16F16
plan: Gate/Up improves by 13.29% at repeat one and 13.49% at repeat ten, while
complete-block Host wall improves by 8.33% and 10.68%, respectively. F16F16
batch eight does not reproduce the W4 gain: its repeat-one Host wall is 0.63%
worse than batch four and its repeat-ten change is effectively neutral at
-0.04%. This is evidence for a W4 pipeline improvement rather than a generic
benefit from fewer command submissions.

For W4F16 repeat ten, batch eight reduces overlapping Gate/Up expansion work
by about 15.9%, HMX wait by about 18.7%, and packed-weight DMA work by about
11.4%. The larger HVX streaming tasks add about 15.8% local work, but this is
off the critical saving and does not erase the wall-time gain. By contrast,
F16F16 prefetch wait rises by about 27.7%, explaining why command reduction
alone is not beneficial there.

W4F16 remains byte-exact with Down-input hash `b0d16655c164a22b` and output
hash `f18b9abbe1487231`; F16F16 remains byte-exact with Down-input hash
`72076aa944fc97e4` and output hash `704252c89780e695`. All formal modes retain
zero intermediate DDR, zero spill/fill, one FastRPC Execution Unit, one HMX
owner, the fixed 8 MiB VTCM grant, and no QNN dependency.

## Conclusion

Both ordered gates pass. The validated implementation facts are the
Crouton-native SwiGLU handoff and the W4F16 Gate/Up batch-eight schedule. The
formal evidence is retained under
`D:\llm_exp\results\qwen3-block-htp\exp0036`; source commits are
`29afb4cc6bf31aa05c0244354990c8b1ab7c7f2b` for Stage A and
`a959219dfa95d6b7b1b45aa18d4e03f3d60d656c` for Stage B. This is a local pass,
not an automatic adoption or baseline promotion.
