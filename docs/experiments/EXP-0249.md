# EXP-0249 — Integer attention arithmetic attribution

User 2026-09-08 approves starting the recommended QK-score / exponent / SOLE diagnosis. PC063. Parent source 3587939fe7a727153a19f080a9d0d1c219f66083; parent EXP0248 ledger 9df15e39189ee4f9904c765321370b5262abdf46cde70e1f12ae39cd6eb9fc1e. Target branch codex/exp-0249-integer-attention-attribution (temporarily parent-bound during setup).

Freeze C64 per-channel W4 codes/scales, EXP0246 A8 and R3_ALL parameters, EOS raw offline prefix, shared residual semantics, dense-only post-RoPE R3, other recipes. No calibration, weight search, butterfly/FWHT, DSP mutation, device run or baseline promotion. This phase measures conditional software PPL with independently validated integer attention; it is not whole-device PPL (linear/norm/R3 arithmetic and offline prefix remain software).

Before scoring freeze twelve configurations: F, C64, plus OFF (EXP0246 A8 params) and R3 (EXP0246 R3_ALL params), each with legacy / carrier / score / exponent / sole attention. Legacy reproduces previous FP16 QDQ floating attention exactly. Carrier adds exact integer QK with clipped signed K, recentered signed V, U8 probability and native two-stage integer AV output conversion, while retaining uncompressed real QK scores and standard Softmax. Score additionally applies actual native centered U8 score conversion and F3 log2 scale. Exponent replaces standard exponent with rounded, capped integer base-2 exponents, retaining exact rounded division. Sole changes only division to the current 145/209 reciprocal approximation. All modes retain identical boundary scales and masks; all nonlegacy modes use probability scale 1/255, zero 0 as required by current config. Report ladder differences as conditional, nonadditive effects, and paired R3 differences (Q/K recalibration is included; no isolated rotation-only claim).

Numerical gate precedes PPL: independent NumPy int64/scalar oracle versus GPU tensor path, exact discrete QK/probability/AV on retained actual-device prefill captures (EXP0042 and EXP0248); randomized masked prefill/decode, negative ties, saturation, single valid position, SOLE power boundaries. Float integer dot products allowed only with TF32 off and proven absolute-sum bound below 2^24, validated against int64. Preserve failed attributable attempts and repair without weakening thresholds. Legacy F/C64/A8/R3 development per-token output must reproduce EXP0246. Every scored mode repeat/causal/CE and immutable weights/prefix checks.

Data: exposed EXP0246 development32 documents (512 targets) solely for debugging. Fresh final256 documents, four balanced English/Chinese wiki/news cells, 4096 scored targets, batch4 prefill64 plus15 cached teacher-forced steps. Exclude all prior calibration/evaluation documents/text hashes/token32-grams including EXP0246. Freeze corpus provenance, tokenizer, token windows, config parameters and protocol hashes before inference; independent reconstruction audit. No final-based tuning or checkpoint selection. Report PPL per cell, paired document bootstrap10000 seed249, and uncertainty. If arithmetic is valid but PPL poor, retain valid negative results. No hardware speed claim; archive full profiling scope as N/A and E2E token/s N/A. Existing <=5percent overall / <=10percent cell PPL quality references and >10percent hardware speed stop remain unchanged for future acceptance; this diagnostic does not promote a recipe. Complete immutable evidence, report and project-memory closure. PC037 routine recovery applies.

## Factual parameter audit correction before model scoring

The initial statement that all frozen probability scales equal1/255 was an assumption rejected by the configuration freeze assertion (attempt1 retained). All layers have probability zero0, but26/28 scales are slightly smaller after prior clipping (scale*255 about0.971..0.998). Preserve these parameters and the actual exporter semantics: the integer normalizer emits codes relative to255 while AV uses the unchanged serialized probability scale. All nonlegacy ladder arms use the same arrangement, so later score/exponent/SOLE increments stay controlled; legacy-to-carrier includes this existing normalization/scale discrepancy. No new quantizer, ranges, arm selection or threshold change. The original protocol is retained with the immutable dataset freeze; the amended authority hash is separately bound by the subsequent configuration freeze.
