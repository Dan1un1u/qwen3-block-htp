# L32-0036 exact log2 normalization optimization

User-authorized sequential stage after L32-0035. Native row1 decode retained. No rotations; native perchannelW4, SP2mode8, FP32 residual1; original weights/qparams. Keep current score clipping/rounding and exact division contract, no numeric change for fast log2. Candidate uses one u32 quotient and exact shift/remainder derivation in place of16 u64 divisions per row; skip unused SOLE bank. Runtime wide8 is legacy clipped score with exact-fast normalization (not widened scores). Original wide0 and FP wide7 retained; FP own frozen references and different softmax semantics explicitly declared. Component diagnostic10; existing rotation7/8 and gather9 preserved.

Before timing: exhaustive host denominator1..257*32768 times16codes equivalence;32 frozen component shapes including ties,masks,lateKV, both prefill/decode exact or ownFPfloat64gate. Selectedlayer7,chain3,chain16 and full16 greedy/fixed own independent original references,8MiB/nointermediateDDR/no spill/no unaccounted ledger. Exact fast must match original, never relax numeric gate. At most2 candidate refinements if necessary. Five short and ten formal alternating/rotating paired rounds repeat10; fixed tokens primary for fair FP comparison, greedy numeric gate and repeat1aux only. Isolated32fixtures gates then matchedM64p0 andM1p64 5short10formal timing. All attempts archived and sealed; no fullfactorial/PPL/quality or baseline promotion. Device exclusive. Follow-on two-model bounded latest generalization after completion.
