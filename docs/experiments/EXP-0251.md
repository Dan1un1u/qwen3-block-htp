# EXP-0251 — Native-U8 score difference hardware speed

PC065 user2026-09-09 requests hardware speed of EXP0250 repair. Parent7448a5d662af3dba67a458512bc9e49203bd07b2, sealed EXP0250 ledgere82e72cec87543319561493abd975cf828bcdea1b2ee46b18c57fcb53e206b0a. Branch codex/exp-0251-wide-score-device; temporary parent branch binding for setup only.

Implement the frozen wide_sole arithmetic on DSP: retain native HMX raw U8 score output, masked row maximum, signed16 difference times original positive multiplier before capped log2 exponent and unchanged SOLE division. Cover M64 prefill and M1 segmented cached decode, including masks and all existing QK/AV conversions. Preserve native weight.n per-channel W4, EXP0247 original C64 layer0 control/R3 packages and replay inputs (EOS+71), weights/scales, U8/cache formats, no butterfly/FWHT, no QNN. No new calibration, reciprocal/exponent method, full model or PPL in this bounded speed experiment. Other recipes and baselines frozen.

Declare three candidates before timing: C0 original no-R3 narrow score path; W0 same no-R3 package with wide score repair; WR plain dense HMX R3 (mode1) with wide score repair. Primary incremental repair speed W0/C0. Also report WR/W0 and WR/C0 if WR meets numerical gates. Never time EXP0248 mode5 expensive refinement as the new candidate. Untimed scalar dense mode2 with wide repair is a declared oracle for WR; old mode1 and identity may be replayed for diagnosis. A failed WR gate blocks its formal speed only; W0 can continue if its own independent checks pass. Preserve and report ineligible variants, no threshold relaxation or hidden fallback.

Before timing: exact independent NumPy int64/scalar implementation comparison on captured raw QK/probability/AV and cached decode; frozen parent controls must reproduce original outputs. Wide exponent boundaries, mask/future maxima, shift invariance and multiplier range retain EXP0250 checks. Dense matrix <=1FP16ULP+minimum-normal, Q/K<=1code, whole-layer <=2codes and dequantized cosine>=0.999 versus independent Float64 dense oracle. Whole-layer native integer values downstream of actual captured Q/K must match the declared arithmetic exactly. Use explicit audit DDR outputs only in untimed mode; repeated capture and no-audit runs must agree. No alteration of weight or package hashes.

For eligible candidates run five short and ten rotated paired formal rounds, each repeat1 and repeat10; M64 then eight real teacher-input M1 appends, one RPC per step. Fixed8MiB requested/acquired, same VTCM grant, report peak; zero timed intermediate DDR/spill, complete additive ledgers, seven native W4 projections, counters and binary/model/source identity. Retain all rounds, no outlier removal. Median paired-round mean Host wall (decode averages8 positions), paired median ratios and10000 bootstrap seed251. Either prefill/decode stable >10percent regression stops escalation. If interval straddles10percent, at most10 extra paired rounds; unresolved uncertainty pauses. Measure fixed eligible arms in rotating order; no schedule search or kernel retuning on formal results. Numerical failure is valid negative evidence, not authority to benchmark an invalid candidate as deployment-ready.

No full-model continuation in this registration. E2E token/s and device PPL N/A; never extrapolate from layer timing. Archive complete repeat1/repeat10 ledger/counter/physical/provenance tables plus stable three-recipe module presentation with unavailable equal-scope recipes N/A. Keep measured W0/C0 overhead prominent for user request. Close source/evidence/project-memory and discuss follow-up; no baseline promotion. Routine attributable repairs and transient transport retries under PC037.
