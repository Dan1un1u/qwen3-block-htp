# EXP-0252 — Normalization and dense R3 error amplification

User2026-09-09 approves the proposed ordered normalization/R3 ablation and conditional integration. PC066. Parent EXP0251 source27972a15d770ada194f712805b1be01e1a44dd68; sealed ledger e3d088b0ece57e5c36baa3a0ac3098306e950af73d4d162f7495fccf4afc7334. Branch codex/exp-0252-normalization-r3.

Freeze original per-channel W4, EXP0246 parameters/prefix, all other recipes, native HMX weight.n, wide raw-U8 maximum/int16 difference and existing capped integer exponents, probability scales, K/V/AV conversions. No calibration, learned rotations, butterfly/FWHT, groups, mixed-precision deployment or expensive mode5 dense refinement. Single fixed candidate NR64: normalized sum x=sum/2^floor(log2(sum)), Q30;64 reciprocal initial values at bin midpoints 1/(1+(i+.5)/64), rounded Q30, then exactly one integer Newton step r=r*(2-x*r). Probability round-half-up of255*2^(15-exponent)*r / 2^(leading+30), clipped U8, original single-key and mask semantics. Keep truncation points explicit. 256byte constant table; no per-element division in candidate. Exact integer division is diagnostic reference, SOLE direct control. No parameter/candidate search on development or final results.

First independent NumPy/int64 and GPU arithmetic checks: every denominator32768..72*32768 and16 exponents against independent exact reference, candidate error<=1LSB; masks, power boundaries, ties, single-key and length up to128; all56 layer configs and frozen EXP0251 raw-QK/probability/AV captures. Existing SOLE controls must reproduce sealed per-token development scores and all old capture boundaries. Fixed captured HMX and Float64-dense input pairs compare probability, AV, O, residual and final output under SOLE/exact/NR64; downstream boundaries executed by DSP, no software surrogates claimed as hardware.

Software PPL: prefreeze F,C64 plus OFF/R3 x wide_sole/wide_exact/wide_nr64,8arms. Existing disjoint development inputs for reproduction/diagnosis, no selection. Fresh128 documents (32 each en/zh wiki/news),2048 targets, independent of all prior evaluation/calibration/training by document/text/token32 checks. Same fixed EOS prefix, tokenization,64context+16targets. Equal-cell NLL, paired document bootstrap10000seed252, language/domain breakdown. No reranking on final data. Original quality gates overall5percent/everycell10percent vs F16 remain; conditional software PPL is not device PPL. Freeze before scoring, check repeat/causality/CE/prefix/weight immutability.

If software candidate reduces normalization error and improves PPL versus SOLE, map NR64 plus exact diagnostic to existing single-layer0 M64+8M1 hardware. Use captured raw scores from same Q/K for direct normalization attribution; compare separately actual HMX R3 versus untimed Float64 dense oracle. Unchanged component1FP16ULP+min-normal, QK1code, whole2LSB/cosine0.999 gates. Low-level candidate must match its independent integer arithmetic exactly. Failed R3 arm blocks only its formal speed; eligible no-R3 normalization can be profiled independently. No threshold relaxation or silent oracle fallback.

Eligible hardware arms: five short and ten rotated paired formal rounds repeat1/repeat10, median paired-round mean Host wall, decode mean8positions; bootstrap10000seed252. Include all rounds.8MiB fixed VTCM,zero timed intermediate DDR/spill/audit,one RPC/step,seven native W4 projections. Native binary/package/source proof and complete PC027 reports required. Stable>10percent in either scope stops escalation; CI straddling10percent permits at most10 additional paired rounds then discuss if unresolved.

Conditional later integration (separately registered after closure): only numerical/physical/speed-eligible candidate may proceed to two-to-three consecutive layers and longer KV, then full-model device PPL/text/E2E. Model-quality failure or remaining R3 numerical failure is discussed before selecting another method. No automatic baseline promotion. Routine attributable repairs under PC037, preserve failed evidence. Current experiment closes with source/evidence/memory and explicit remaining limits.
