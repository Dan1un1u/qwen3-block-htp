# EXP-0298: ordinary A8 versus SP2, ABC completion
User authorizes Qwen0.6/1.7 ordinary W4A8 with only Down SP2 removed, using the current optimized SP2 pipeline. Defer further small-model speed optimization. Same binary, fixed identical input trajectories; A=64+42, B=536+46, C=741+3, with native M64 reference in the same measurement campaign. Restore frozen original middle affine qparams and ordinary SwiGLU LUT; keep identical W4 weights, all other qparams, FP32 residual, no rotations, and current per-model runtime options (927/65439). Reuse approved exact reference arithmetic and existing proven ordinary-A8 packages. No new calibration, weight encoding, performance candidate search or quality claim.

Initial source binding is EXP0297; after successful preflight create codex/exp-0298-a8-tables and update binding. Llama companion L32-0054 is separately governed; device access serialized, Qwen first. New outputs only under exp0298; preserve sealed parents and all failed attempts. Verify manifest SHA and explicitly classify every changed file; hardlink unchanged weights and unlink before editing derived files.

Before timing, independently verify actual integer/SDK arithmetic, all-layer hidden hashes, head logits/tokens, KV validity and poison where applicable, short and long chunk boundaries (64/65/128/129, full536+46/741+3). One full-model RPC per chunk/token, exactly8MiB VTCM, one HMX owner, serialized DMA, no timed intermediate DDR or spill. Existing numerical/physical tolerances unchanged. Reuse valid SP2 reference evidence where the mathematical contract is unchanged; new ordinary-A8 reference must never derive from candidate hardware outputs.

Five paired short rounds followed by ten paired formal rounds, repeat10, rotated order. Keep all samples and additive module accounting; repeat1 auxiliary only. Report ordinary A8/SP2 cost without assuming which wins. Long/M64 throughput lower95 confidence bound >=0.90, same-shape regression wall upper95<=1.10; these are reported independently from numerical correctness, no automatic baseline promotion. No actual dataset-quality benchmark claim for project-owned timing fixtures.

Update measured A8/SP2 rows in desktop workbook A/B/C, preserve manual edits and D curation (no obsolete append). Close with provenance, immutable evidence ledger, reports, commits and remote synchronization.

## Completion
Measurements and workbook update complete. See docs/experiments/EXP-0298-RESULTS.md. Numerical/physical and measurement-completion gate pass; long/M64 throughput separately reported without reinterpretation. Evidence ledger SHA256 ca07e29994a372f198de08a24fdf0a8ddc82de3d1efbe26d3b05640a20d632bb.
