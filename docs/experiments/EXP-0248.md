# EXP-0248 — Repair dense R3 integer-chain numerical divergence

User2026-09-08 explicitly approves attempting repair of EXP0247, including locating attention/O/residual amplification and aligning actual HMX arithmetic. Parent18fdaac2dfeb758e9443ff96728991fb9cba2a54, evidence ledger8752c15d95d9bb5dced802cda7d7d64cb92897b1c6c16fc9a81f909f80ec0965 verified187files. Branch codex/exp-0248-r3-numeric-repair (temporarily parent-bound only for branch setup).

Freeze C64 layer0 W4/scales, EXP0247 control/R3 packages, EOS+71 replay, all A8 ranges, native HMX W4 reads, dense matrix multiply (no butterfly/FWHT), other recipes and baselines. Diagnose actual boundaries QK score, probability, AV, O, residual/norm/MLP with separately identified audit DDR exports. Independent scalar integer attention reference must use actual captured inputs, not ideal teacher. Preserve old evidence and failed attempts. Up to three attributable repair variants may address proven arithmetic, layout or lifetime defects or dense numerical precision; no quantizer/weight search. Identity and HMX-state controls retained where relevant. No threshold relaxation: dense <=1FP16ULP+minimum-normal, Q/K<=1code, whole layer<=2codes and cosine>=0.999 versus independent declared dense oracle; exact integer boundary checks where arithmetic is specified.

Only after correctness passes run five short and ten paired formal single-layer rounds repeat1/repeat10 with audit disabled, alternating control/candidate order; stable >10percent prefill or decode complete Host-wall slowdown stops before full model. Same8MiB VTCM,zero timed intermediate DDR/spill,one RPC/step. If no repair passes, close valid negative evidence and discuss remaining cause. No full-model deployment in this bounded repair. Device PPL requires validated full-model arithmetic; if unavailable explicitly N/A, never reinterpret software proxy PPL as device PPL. Conditional follow-on PPL scope requires frozen protocol and successful numerical/physical/speed prerequisites. Complete evidence/profile/memory closure, no automatic promotion.

## Repair variant 1 frozen before execution

Retain dense HMX GEMM; after it completes, recompute dense dot products near U8 rounding thresholds with independent HVX SF32 arithmetic (direct sign-matrix rows and ordinary dot reduction, no FWHT/butterfly). Guard spans two adjacent FP16 spacings plus minimum-normal tolerance. This is a bounded candidate relying on the component error gate, not a universal HMX error theorem. All exposed replay outputs and exact Q/K/cache bytes must be checked against the Float64 oracle before timing. Scalar Float64 oracle remains audit-only and is never a candidate fallback. Frozen ranges and integer attention unchanged. Counters include guarded elements and complete cost.
