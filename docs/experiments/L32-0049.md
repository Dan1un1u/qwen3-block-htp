# L32-0049: complete generic Llama3B W16A16 with segmented resident weights
User explicitly approves implementation following successful L32-0048 delayed mapping probe.
Scope: export fresh original BF16->FP16 weights; segmented host rpcmem allocations and single-session DSP delayed mapping within one token-level RPC. Preserve existing generic FP16 HMX/HVX/basic DMA overlap, original FP16 residual/KV/nonlinear semantics. No specialization tuning, deliberate slowdown, QNN/CPU inference fallback, quantized recipe changes, multi-session or model-quality claim.
All weights resident in DRAM before warm timing; bounded DSP windows; no disk/host weight copying per token. Use buffer ID/local offsets, validate each range, drain HMX/DMA before unmap, propagate errors and clean mappings. 8MiB VTCM, no timed intermediate DDR/spill. Mapping cost included in additive DSP/Host ledgers.
Correctness before timing: fresh provenance/hash and packing checks; selected layers0/13/27 prefill+decode with nonzero KV; chain3; full28 fixed trajectory including tile-tail boundaries; compare independent floating reference and separately exact same-math monolithic-vs-segmented device captures where capacity permits. Full28 segmented versus layer-by-layer composed hardware diagnostic permitted untimed only; no per-layer RPC fallback in production. Existing numeric thresholds unchanged; distinguish mathematical FP16 rounding disagreement from transport regression, retain failed evidence. Bounded1B and3B quantized regression after ABI changes.
Primary speed M64+42/cache128 full28, existing common prompt/fixed input tokens. One auxiliary repeat1; five short then ten fixed formal repeat10 processes once implementation correctness established; report full module ledger and E2E including embedding/finalnorm/fullhead/greedy/FastRPC, exclude cold loading/tokenization. No speed-gain gate against nonexistent3B FP16 baseline; no automatic baseline promotion.
Artifacts isolated under models/llama32-htp/l32-0049 and results/llama32-htp/l32-0049. Check device ownership and capacity before use. All failures archived; routine fixes authorized. Finish evidence ledger, reports, source/memory clean+remote synced and normal experiment closure.
