# EXP-0307: Qwen1.7 companion integer/fusion ablations

User2026-09-22 authorizes complete paired ablations. L32-0065 owns device first; Qwen offline preparation may overlap but device runs wait for recorded transfer. Reuse verified EXP0305 recovered EXP0284 uniform INT16 package, no rotation, fixed W4 per-channel, FP32 residual/Norm, existing KV/prefix seed, M64+42 fixed decode. Preserve all historical artifacts and E; no baseline or quality promotion.

F: vector FP Softmax/SwiGLU with necessary QDQ. I: existing integer nonlinear producers with valid-row standalone AV RQ. M: fold AV common multiplier and zero correction into O with prevalidated raw24/int32 bounds. F/I share control metadata; I/M share integer binary. All arms share scheduler/layout/worker choices; no manufactured identity conversion, scalar fallback or extra intermediate DDR. Fusion attribution is bounded to AV-to-O, not all QDQ. Existing shared SP2/INT16 layout/pipeline evidence reused with original measurement labels.

Use own-contract independent arithmetic, exhaustive nonlinear components, single/three/full layer exact checks before timing. Check clipping and numerical effect of reassociation; never assume cross-arm bit equality. Keep physical8MiB, no timed tensor DDR/spill, one HMX owner, ledger closure. Five short and ten formal rotated paired repeat10 rounds, repeat1 auxiliary; diagnostic controls not stopped for10% slowdown. Record all attempts without sample exclusion. Same token trace, warmup and full Host wall. Report paired bootstrap95% CI and overlapping worker counters separately from additive wall ledger. Complete both-model summary and archival before final response.

Device transfer2026-09-22: L32-0065 formal10 and postflight complete; device released to EXP-0307. Llama remains open only for combined reports/closure.

## Numerical eligibility result before timing
Unconditional M rejected: candidate reference has 1310/6078464 AV values outside the original output range; already 6 in layer0 on the same input. Saturation cannot in general commute through O. Own-contract correctness alone would not justify calling this a scale-only ablation. Preserve candidate package/reference but do not promote or time it as a valid fusion benefit. Complete F/I paired profiling under identical production schedules. This is a completed negative result for Qwen fusion eligibility, not a relaxed numerical gate. Llama M remains eligible on its checked unsaturated trajectory, with recorded FP reassociation caveat.

## Completed2026-09-22
All eligible arms complete5 short+10 formal repeat10 rounds at M64+42, full own-reference and physical checks. Reports and counters archived. Source closure `8e6e015a66ccfafb44ba3d17052b85a3928d2501`. Evidence ledger SHA256 `50364ace033f4fca97aad525c8c07f6031dc6c5a609c32043a23b23e26a12179` (1666 files). Device released, no baseline or quality promotion. E and all workbook sheets unchanged. Companion work complete, combined report: `/mnt/d/llm_exp/results/paper-integer-fusion-20260922/REPORT.md`.

Llama F/I prefill wall -33.019%, decode -0.305%; fair I/M decode -0.408%, prefill benefit unconfirmed. Qwen F/I prefill -33.462%, decode -0.833%. Qwen M rejected before timing for active original saturation (1310 candidate values,6 first-layer same-input); this is a completed negative eligibility result, never a valid fusion speedup. Historical shared SP2 carrier/layout evidence retains its original quantizer labels.
