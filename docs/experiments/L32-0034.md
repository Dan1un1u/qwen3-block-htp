# L32-0034 — A8 supported longer KV

Standing approved paper campaign appendix. Retain original no-rotation nativeW4/SP2mode8/FP32residual1, wide0 log2 and frozen all weights/scales/nonlinear/head. A9 FP is an ablation, not promoted. Full16 M64+33 (34 generation outputs, KV reaches97), supported cache128, versus main M64+15/cache80. Same all-on format0/pipeline0 versus FFN disable2 (A2 representative mechanism); no new tuning or runtime redesign.

Fresh linked model overlay, only enlarged zero cache/reference carriers and RoPE beyond old15 decode steps. Verify first15 RoPE exact to frozen source and original16-step greedy teacher remains exact. Independent CPU full16 all34 steps frozen before device timing; fullall-on/control numerical selected token/logit codes must equal own CPU and each other. Physical plan <=8MiB, native KV lengths/append, no tensor DDR spill, identical HMX work/weightbytes for pairedsame step. No rotation/PPL/qualityacceptance.

Existing generation kernel supports up to193 tokens andcache128; evaluation repeat harness currently caps row2 at16. Permit greater-than16 ONLY greedy mode2 up toexisting maximum; keep NLL/forced-token modes bounded16 because target record has16 entries. No ABI or DSP arithmetic change. Targetids remain unused past16 in mode2. Benchmark repeat10 is ten complete independently reset warm sessions in same process, existing semantics.

Component validlength up to128 already A9 verified, fullmodel fresh exact CPU gate before timing. Two untimed fullfrontend gates (all-on and FFN); fixed5short10formal alternatingpaired, repeat10primary,repeat1aux. Aggregate actual complete-model prefill/decode wall; also partition decode by validlength<=96 versus97 to expose tile transition (last bin onlyone step, descriptive). Paired bootstrap20000seed30034 across10rounds; no speed-driven stop/resample, diagnostic >10percent permitted, numerical/physical gates unchanged. All failures retained; up to3 attributable implementation/collection repairs, never relax math gate.

Qwen EXP0278/vsum owns device until explicitly released; Llama CPU reference/build/report only in parallel. Complete A8 does not authorize promotion or quantization changes.
