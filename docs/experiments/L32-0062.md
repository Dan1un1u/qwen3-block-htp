# L32-0062: fullmodel AV-to-O scale folding
User approves one full-model configuration and archiving in F of desktop HTP hardware workbook. Llama3.2-1B-Instruct64prefill+42decode, original fixed trajectory from0060/0057, no rotation, FP32 residual, uniform INT16 Down in both arms. Two arms: original AV post-HMX RQ; raw AV carrier consumed by O with folded scale and zero128. If a layer has multiplier1 preserve its original zero/scale because there is already no AV RQ; all groups must agree on contract. No other runtime optimization. Retain0061 caveat that short1B decode original RQ processes fullM64, not row4-minimal comparison. No model-quality claim or baseline promotion.

Recover device-resident historical packages only against preserved manifests; source original regeneration allowed if needed, never rewrite old evidence. Separate new directories. Build current unchanged native runtime for16layers. CPU independent full16 layer/KV/finalnorm/head reference for each arm on identical fixed inputs. Record per-layer saturation magnitude on each arm trajectory, distinguish clipping contract change from FP32 reassociation. Per-matrix signed24/S32 universal bounds. Exact own-reference hardware outputs/head, cache and physical gates; no bitwise-old-contract claim. Selected and3layer checks before full16 audit. Capture untimed; no intermediate DDR/spill timed;8MiB VTCM; single HMX owner. Five short then ten formal AB/BA paired repeat10; fixed seed620062,20000bootstrap. E2E includes embedding/alllayers/finalnorm/head/greedy/FastRPC; excludes coldloading/tokenizer. Preserve all failures and original baseline controls; no extrapolation. Update only new F sheet; keep A-E and other user workbook contents/styles unchanged. Routine repairs autonomous. Device serialized with Qwen project after lock check.
