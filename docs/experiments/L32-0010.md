# L32-0010: full-model SP2 Down E2E speed

User requests actual E2E speed and slowdown versus original integer implementation.
Owner no-rotation, parent625073a. Fixed L32-0009 mode4,241-value SP2 codebook,
calibration-only alpha,original per-channel W4. Only all16 Down activation LUT/
qparam change; original embedding/head/other quantizers and weights fixed.
No rotations/grouping/PPL/quality gate or baseline promotion. Frozen Qwen and
rotation worktree unchanged.

Validate all16 packages and signed24/Q31 bounds; independent SDK/CPU integer
reference,consecutive3-layer prefill64/decode1 withKV,then complete16-step
generation greedy IDs and logits codes against independent reference.
Baseline original L32-0003 goldens retained; new SP2 reference never uses device
output as golden. Preserve all original manifests and owned failed attempts.

After correctness, fixed10 AB/BA paired process invocations of same64-token
prompt and15 continuous autoregressive decode tokens,capacity80. Token trajectories
may differ by recipe; lengths/shapes identical. Same final build and hardware.
Report full Host wall prefill64/time,decode15/sum(time),module us/Host shares.
Both paired-bootstrap95%upper<=1.10 for10%gate. No optional stopping or extra
unchanged runs to obtain a pass; repeat1 functional data auxiliary only.
If formal fails, report observed stacking slowdown and discuss; no further
performance redesign within this experiment. Ordinary owned integration fixes
authorized preserving numerical and physical thresholds.

Require8MiB VTCM,oneHMX owner,oneRPC per full token-boundary execution,no timed
intermediate tensorDDR/spill,complete additive ledger. Build seals and immutable
deployment hashes required. Models/results under llama32-htp/l32-0010.
