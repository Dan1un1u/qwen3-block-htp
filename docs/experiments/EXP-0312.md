# EXP-0312: Explicit QDQ timing attribution
User2026-09-24 authorizes explicit QDQ replacing fused FP nonlinear/QDQ for the G panel-b diagnostic, results appended at workbook end. Scope includes the four previously profiled models; Qwen phase uses EXP0305/0306 fixtures, Llama phase separately registered and serialized.
Keep original fused implementation as control. Freeze weights, quantization parameters, scalar rounding order, vector FP approximation, fixed64+1 trace, native integer matrices, FP32 residual and INT16 Down.
Separate input dequantization/preparation, FP nonlinear arithmetic, and output scaling/quantization/native packing through materialized FP32 buffers. Tiled VTCM scratch may reuse only proven phase-dead buffers; no timed intermediate DDR/spill or silent scalar fallback.
Use additive phase-boundary wall measurements, not sums of overlapping worker timers. Dispatch/join and buffer movement remain declared in their measured phases; optional work counters are overlapping diagnostics.
Validate component rounding, fused/split output equality and full-model own-reference hashes before formal data. If explicit FP materialization changes rounding, diagnose and preserve original arithmetic; no numerical threshold relaxation.
Five short and ten alternating formal repeat10 pairs per model; repeat1 auxiliary. No speed or model-quality gate. Capture complete E2E and phase/module ledgers. New explicit-stage cost is not a retrospective decomposition of fused timing. Preserve all historical G data, append a new worksheet with provenance and percentage/latency.
No automatic baseline promotion. PC037 routine recovery applies. Device currently absent from Windows and WSL ADB; offline implementation/build may proceed after preflight. Claim device ownership only when connected and no competing owner.
