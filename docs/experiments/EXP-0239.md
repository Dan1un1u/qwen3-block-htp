# EXP-0239: OmniQuant LWC per-output-channel W4A16 reference

Approved by User2026-09-07 and PC054; starts only after EXP238 closure. Use pinned official OpenGVLab/OmniQuant UniformAffineQuantizer for learnable weight clipping, adapted to Qwen3 native decoder modules. Weight-only LWC, LET=False, activation quantization disabled, no shift/equalization/rotation. Set symmetric=True, disable_zero_point=True, group_size=None, dynamic_method=per_channel; narrow qmin to -7 before optimization. Official sigmoid low/up bounds initialized4, FP32 scales with upstream clamp[1e-5,1e4].

Follow official block reconstruction with distinct full-precision teacher and quantized student hidden trajectories; no augmented loss. Fixed20 epochs, batch8, AdamW LWC lr0.01, weight_decay0, seed239, deterministic sample order, FP32 optimization and FP16 autocast forward, gradient scaling. Only low/up clipping parameters train; original weights, norms, gamma, embeddings and head frozen. Use final epoch, no checkpoint selection. Native Qwen3 module adapter preserves Q/K RMSNorm and canonical RoPE/attention. Verify unquantized adapter parity, official quantizer against independent NumPy including gradient flow, and final FP32-scale packed reconstruction.

This is a constrained OmniQuant-LWC reference on our common grid and corpus, not a claim to reproduce the paper's native asymmetric/grouped results. No GPTQ/AutoRound postprocessing. All hyperparameters are fixed before EXP238 final scoring to keep the shared final panel out of method selection.

## Shared predeclared comparison contract

PC054; original Qwen3-1.7B checkpoint and canonical EXP217 F16 package. Reuse EXP230 C64 and EXP233 AR-P as frozen enhanced-GPTQ and AutoRound controls; label C64 as the project-enhanced GPTQ recipe, not unmodified upstream GPTQ. All candidates replace the same 196 transformer projections. Norms, embedding and rotary semantics remain canonical. All quantized full-model comparisons inherit exactly the same per-channel W4 LM head from EXP230 C64 (original EXP221 head); no head tuning, FP16 restoration, grouping, rotation or online transform. Signed integer grid[-7,7], one positive FP32 scale/output row, FP16 dequantized weights/activations. Software nibble export is not a DSP deployment package.

Calibration is the exact verified EXP230 C64 512x128=65536 token panel, with equal English/Chinese Wiki/news cells. Existing EXP230 development is diagnostic only; no candidate/checkpoint selection or hyperparameter sweep. Freeze one new shared 1024-document/16384-target final panel, balanced 256 documents/cell, before either solver export. Exclude every prior calibration/development/final document, text hash and overlapping 32-token span through EXP236; reconstruct windows independently from retained public corpora. Reuse this fixed suite panel for EXP238 and EXP239, whose methods/hyperparameters are both declared here before any suite final score. No tuning after exposure.

Evaluate actual packed reconstructions in canonical CPU-half-then-GPU FP16 model construction, eager attention, batch4, M64 context +16 scored targets, no cache, TF32 off, reduced-precision FP16 reductions off, deterministic algorithms. Mandatory same-input repeat, causal masking, finite logits/NLL, independent CE difference<5e-6; raw-token math.fsum NLL and document bootstrap5000. Report overall and each language/domain paired PPL vs F16 and C64, with 95% intervals. Existing overall5% and cell10% gates unchanged; require upper interval within gate for confident pass; inconclusive is reported without retuning. Short questions auxiliary, not a gate. No device performance claim or automatic promotion.

Verify original checkpoint hashes, upstream code provenance, fixed non-transformer state, per-row scale and integer grid, all 196 independent nibble/FP16 roundtrips, and block replay before final scoring. Preserve compact evidence and final packages only; avoid persistent per-layer full calibration caches. Routine attributable implementation/transport/numerical repairs follow PC037 and are logged; never tune numerical hyperparameters on final scores.
