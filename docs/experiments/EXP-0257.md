# EXP-0257 — Independent native A8 device text and E2E validation

User2026-09-09 explicitly requests an independent real-device profiling/text-output test of the best accuracy repair compatible with existing A8 speed. This authorizes necessary full-model integration and diagnostic evaluation; it does not promote a baseline or relax implementation/physical/speed gates. Parent EXP0256 source073a39de0f452c938cd5705c82ae722611fb9396; hardware reference EXP0252.

Candidate: fixed C64 per-output-channel W4, fixed EOS151645 prefix and original OFF static A8 parameters from EXP0246, native packed-W4 HMX projections, wide raw-score differences and fixed NR64 normalization. No R3: its original whole-layer hardware gate remains failed. No FP16-restoration attention, expensive refined rotation, residual R_MSE, recalibration or weight fitting. Candidate is the best established no-R3 speed-eligible integer normalization repair versus wide SOLE on matched EXP0252 data, not the absolute software-PPL winner. Control uses identical weights/prefix/parameters and wide SOLE; preserve original recipes and baseline artifacts.

Before device execution: verify all package/reference/binary hashes and preflight. Implement isolated full-model ABI/build and exporter as necessary, keeping fixed8MiB VTCM, zero timed intermediate/logit DDR/spill, one RPC per complete model pass, persistent per-layer U8 KV. Validate arithmetic on retained actual captures and single block, then three consecutive layers and cache transitions before full28-layer expansion. Independent software integer reference/original whole-layer gates remain unchanged; deterministic token feedback and repeated runs must match. Routine implementation/collection defects repaired under PC037 with failed evidence retained. If original numerical/physical gate cannot pass, stop dependent formal profiling and report the actual failure, never substitute software throughput.

Freeze a small bilingual prompt suite before candidate outputs: include factual short answers and English/Chinese prose continuation, retain complete prompts/token IDs/generated IDs/text, use content-aware auxiliary judgment, no post-output prompt tuning. Reuse frozen qbh-lite-v1 prompts where practical. Greedy generation through complete embedding/28blocks/final RMSNorm/W4 LMhead, EOS stop explicitly recorded. Expose selected token only in timed mode; untimed audit may expose logits/hidden boundaries. This test checks usability; it is not new PPL acceptance or proof of general text quality.

Five short paired rounds then ten rotated formal rounds with repeat1/repeat10 complete token-boundary profiling, matched M64 prompt and fixed continuous decode budget. Timing includes all model work and token selection; report actual token counts/Host-wall denominators. Exact length/capacity frozen before execution based on existing supported runtime, at least15 feedback decode steps for speed and enough tokens to inspect text. EOS override allowed for fixed-budget speed only, distinct from semantic outputs. Include prefix cost in measured prompt count and wall; tokenizer/detokenizer/ADB/startup reported separately. Stable>10percent Host-wall regression in prefill or decode stops escalation/discuss; near threshold at most10additional paired rounds. No threshold change, silent fallback or per-layer throughput extrapolation.

Complete PC027/028 three-recipe module table with non-paired historical F16/W4F16 columns and directly measured A8 E2E, detailed archived control/candidate ledgers/counters/physical checks/texts, immutable evidence seal, source/memory clean/synchronized closure. No new algorithm or promotion. Parent branch temporarily bound solely for successful-preflight branch setup.
