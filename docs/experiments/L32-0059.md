# L32-0059: SP2 HVX shift-add versus HMX
User explicitly requests implementing SP2 power-of-two multiply/add on HVX and comparing current implementation, one representative configuration. Llama3.2-1B complete Down8192->2048, frozen layer7 SP2 operands from existing FP32-residual/no-rotation trajectory. Probe prefill64 rows and decode1 row. Fullmodel integration only if user selects that comparison scope; no component-to-E2E extrapolation.
Preserve exact SP2 codebook and existing per-output-channel W4 codes/scales. HVX implements two shifts and signed addition with signed32 accumulation, no activation/weight integer multiplication in dot product. Optimize basic vector loops, four HVX workers, native W4 input and double-buffer DMA. Count any input preparation/weight unpack/layout and output publication in component total. Current HMX comparison uses three-digit raw extraction and decode low/high co-pack, same packed weights and operands, basic overlapping DMA. Record one-worker auxiliary if helpful; do not represent old unoptimized probe as current production baseline.
Independent exhaustive codebook and signedW4 product/decomposition plus full matrix INT64 oracle; every integer output exact. Native HMX and HVX must match their independent reference. Preserve8MiB VTCM, no intermediateDDR/spill, oneHMXowner, bounded allocations, complete additive ledger. FP32 epilogue unchanged if integrated; respect actual compiled instruction contract and signed zero.
Five short and ten formal paired AB/BA repeat10 cycles for both M64/M1; warmup/repeat1 auxiliary. Report complete component wall and matrix-only cost separately, include overheads, all samples, paired bootstrap20k seed590059. Deliberately slower experimental arm allowed, no baseline promotion or model-quality claim. User authorized routine implementations/owned repairs; no repeated permission for tooling/build/reference fixes. Other recipes, weights and historical records unchanged; no workbook update.
