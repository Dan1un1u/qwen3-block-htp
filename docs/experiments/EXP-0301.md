# EXP-0301: decode optimization coverage audit and repair

Authorized current request: inspect all other ABC decode configurations >10% slower than the same model/recipe A64+42. Repair actual omitted optimizations sequentially, validate and remeasure affected rows. Current screen: Qwen0.6 four recipes B/C (8), Qwen1.7 W4A16/A8/SP2 C (3); Llama none. The latest bounded audit includes previously deferred Qwen0.6. Historical D is not the reference and remains unchanged.

Freeze model weights, quantizers, no-rotation, residual type, head/KV arithmetic, fixed token trajectories and full-model timing boundaries. Preserve Qwen0.6 W16 FP32 full-M64 residual/Norm decode row parity. A16 gets only generic vector, data movement, basic DMA and existing worker reuse; no deep model specialization or artificial baseline degradation. No new model-quality claims.

First migrate already validated EXP0297 A8 mechanisms from option927 to65439 on Qwen0.6 (same selectable binary), after capacity/alias and full numerical audits. Diagnose Qwen1.7 residual gap from archived modules/source, not presume it is the same defect. Then inspect A16 long decode vs existing generic implementations; at most three attributable candidates, preserving selectable controls and exact valid outputs. Do not redesign the cache merely to force cross-context parity. A legitimate context-dependent KV/attention cost may remain and must be reported honestly.

Before device work verify pinned parent ledger/manifests and inherited reference artifacts read-only. Candidate checks cover 64/65/128/129/536+46/741+3, padding poison and full valid layer/KV/head equality as applicable; changed layouts cannot borrow candidate-generated references. Preserve prior A16 full-floating failure and all failed experiments. Hardware: one HMX owner, exactly8MiB VTCM, bounded nonaliasing scratch, no timed intermediate DDR spill. Correctness/physical constraints never relaxed.

Adopted repairs require five short rotated paired rounds followed by ten formal rotated paired repeat10 rounds; repeat1 auxiliary only. Same-length candidate/control prefill and decode Host-wall CI upper<=1.10 guards; long/M64 prefill TPS lower95%>=0.90 remains separately reported. New decode screening/parity TPS lower95%>=0.90 versus same-round native A reported separately from the same-length guard. Do not redefine one gate as the other. Include unchanged native reference and both A8/SP2 or W16/W4 when sharing a repair.

Archive commands, hashes, all samples, complete additive module ledgers and per-model results under /mnt/d/llm_exp/results/qwen3-block-htp/exp0301. Update only affected ABC cells and provenance after formal verification; preserve unrelated Llama rows, curated D and historical sealed evidence. Routine reversible tooling repairs and normal commits/pushes authorized. No automatic baseline promotion or quality acceptance. One device owner, serialized campaigns.
