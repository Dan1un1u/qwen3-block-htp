# EXP-0122 — W4U8 ring16 Gate/Up eight-bundle DMA chain

## Decision and scope

The user authorized a continuing W4F16/W4U8 recipe-specialization loop and
routine autonomous recovery. EXP-0122 branches from the locally passing
EXP-0120 ring16/worker3 candidate. It changes only the number of adjacent
Gate/Up weight bundles submitted in one linked DMA chain.

## Evidence and hypothesis

EXP-0120 provides sixteen compressed slots and consumes eight output tiles per
HMX command, but the producer still submits linked DMA in groups of four. Each
HMX command therefore requires two chain submissions and two groups of
descriptor completion/publication bookkeeping. EXP-0121 shows that adding HVX
concurrency is counterproductive. The new hypothesis is that one eight-bundle
chain can match the HMX command boundary, reduce producer control overhead and
improve readiness without changing decoder density or HVX/HMX contention.

## Gates

Control and candidate are runtime selectable from one binary. Both use ring16,
three persistent workers, batch-eight HMX, the original decoder and the
EXP-0112 single-fence path. Control preserves four descriptors per linked
chain; candidate uses eight. Weight bytes and descriptor count remain equal;
only DMA chain submission, wait and publication grouping may change.

Final output and existing stage audits must remain byte-exact. Exact 8 MiB
VTCM, zero intermediate DDR and spill/fill, one FastRPC unit, one HMX owner,
unchanged weights, descriptor count, HMX commands, tile pairs and peak VTCM are
hard gates. The candidate is eligible only if Gate/Up and complete Host wall
strictly improve in ordinary and paired repeat-one and repeat-ten medians over
five rotated rounds. PC-027 and PC-028 are mandatory. Only the user may promote
an eligible candidate.

## Formal result and rejection

Source commit `521841bd0140047e5ca73067b1a3443238cecc11` was measured for
five order-rotated rounds. Formal evidence is archived at
`D:/llm_exp/results/qwen3-block-htp/exp0122/20260831T222533Z_521841bd0140_formal`,
with retained binaries at
`D:/llm_exp/models/qwen3-block-htp/exp0122/artifacts/521841bd0140/20260831T222533Z_formal`.

Both cells preserve output hash `69f22eeb035e5ec5`, zero final mismatch and
zero maximum LSB. They retain the exact 8 MiB VTCM grant, zero intermediate
DDR and spill/fill, one FastRPC execution unit, one HMX owner and identical
weight bytes, descriptor count, HMX work and global peak VTCM.

The eight-bundle chain does remove control work: MLP chain count falls from 96
to 48, DMA submits from 128 to 80 and DMA waits from 640 to 544. Repeat-ten
weight-stage work falls 13.76%. This does not shorten the critical path.
Gate/Up HMX compute increases 7.63%, producer-slot wait increases 171.70%,
Gate/Up wall increases 4.48% and complete Host wall increases 0.98%. Repeat
one shows the same direction: Gate/Up increases 3.39% and Host wall 0.72%.
All paired medians also regress.

The causal boundary is therefore chain granularity rather than DMA call count:
one chain of eight delays publication of the first four ready bundles until
the second half of the batch is complete. The saved submission bookkeeping is
smaller than the lost producer/consumer overlap and the resulting HMX
contention. The strict speed gate fails, the candidate is rejected, and
EXP-0120 ring16/worker3/chain4 remains the provisional W4U8 source parent.

Evidence hashes are: gate summary
`f2f175b82744e0dcc61420250269161a9ed85879896b8d02a2e582bf9c244b11`,
complete report
`96c3293ee2884959e7188a2d2820f10421b729756bd65e738a6a1b01753a96ed`,
and evidence ledger
`b6fba8910a676439a2ae48ac59d7b88c2641b2b7855220b079e727ec254a60bd`.
