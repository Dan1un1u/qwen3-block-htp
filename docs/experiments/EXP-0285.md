# EXP-0285: FP16 residual storage
Approved user2026-09-17. Select current Qwen latest no-rotation W4A8-SP2 (EXP0284 SP2 arm, inherited EXP0282 optimized scheduling), not uniform INT16 Down arm. Llama and other recipes unchanged.

## Contract
FP32 control mode2 unchanged; candidate mode3 stores a real packed FP16 residual, including embedding initialization, O residual add, Down residual add, inter-layer state, final norm input. Integer native-W4 dot and FP32 scaling/add unchanged. Round each completed residual add once to FP16 using documented SDK conversion; RMSNorm keeps original ordered FP32 sum/epsilon/gamma/A8 quantizer. No hidden full-sized FP32 residual copy or DDR spill. Conversion should occur at producer/consumer boundaries and use vector operations. No rotations/new weights/calibration/PPL/quality gate. Implement candidate-specific independent rounding reference; different formats need not match one another.

## Validation and performance
Check finite edge cases/rounding/layout and selected actual layer prefill/decode, consecutive3 and full28 execution/determinism/physical accounting. Preserve original FP32 control outputs against prior evidence. New contract correctness remains nonnegotiable. Full model FP16 reference audit where available; do not claim quality or unstated reference scope.
Same binary flags except residual mode. Original SP2m8, wide4, no rotations, all shared optimization enabled, cache128/frozen prefix. Fixed original64-token prompt and15decode token trajectory, complete embedding/transformer/finalNorm/head/greedy/FastRPC Host wall. Five short and ten AB/BA paired formal rounds repeat10, one warmup/auxiliaryrepeat1. All attempts retained, no optional stopping. Tenpercent overhead diagnostic, no automatic promotion; report slowdown even if no gain. Full module attribution and physical8MiB/oneRPC/oneHMX/zero timed intermediateDDR. Device ownership Qwen only; check no foreign running task.
Initial parentbranch binding permits preflight before new branch creation; immediately rebind after creation. Preserve original artifacts and historical evidence.


## Control fairness repair, before final comparison
Initial same-binary fixed5short/10formal complete and retained, not discarded. FP16/control wall ratio1.09519 prefill; however adding runtime storage dispatch also changes generated FP32 control code. Archived original-baseline verification (manifest/ledger/local and device SHA256 exact) reproduces1851.48prefill vs new-control1795 range. Do not classify this as FP16 within10percent of the actual latest baseline. Under PC037 repair the comparison by adding one predeclared fixed5short/10formal AB/BA campaign: unchanged sealed EXP0284 SP2 FP32 binary versus identical frozen EXP0285 FP16 candidate. No candidate tuning/optional stopping. Fresh same-session original control is primary for the user's latest-baseline question; initial same-binary results remain supplementary with their own scope. All weight/flags/tokens/resources and actual own-reference output checks identical; source/binary identities explicitly differ and are recorded. Original control is never slowed artificially. This is measurement fairness recovery within the approved comparison, no gate relaxation or baseline promotion.
