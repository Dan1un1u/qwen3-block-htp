# EXP0248 后续方向

本轮修复了固定单层回放的数值对照：dense HMX R3 + 边界值 HVX 稠密点积重算，九个步骤 Q/K/最终输出与 Float64 参考完全一致。但正式单层 prefill23.75倍、decode2.00倍左右延迟，超过10%停止门槛，未进入全模。该实现不采纳，旋转方向本身未被否定。

首个误差放大位置已定位到整数 attention：QK中间U8的粗网格、整指数舍入和SOLE分段归一化共同造成跳变；另发现静态K不对称零点转有符号载体时的既有饱和语义。建议下轮先分开量化这些误差，再验证更平滑/更精确的 attention 数值路径，保持原生HMX W4线性层与C64权重。不要继续用高成本舍入补偿作为部署路径。需要独立协议、实际硬件边界参考，以及满足硬件门槛后新冻结的PPL评测。

无active experiment，next249待讨论；无新算法授权、无基线提升。F16/W4A16冻结。PPL和E2E token/s为N/A，不复用软件proxy作为设备结果。读EXP-0248-RESULTS.md与EXP-0248-PROFILE.md。
