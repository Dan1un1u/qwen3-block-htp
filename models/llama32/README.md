# Llama 3.2 model adapter

`model.json` is the development contract. The exact checkpoint is unselected
and native support is pending. Keep model dimensions, tokenizer/template,
special tokens, RoPE/normalization behavior, original-weight export and
calibration ownership here as the port is implemented. Do not import Qwen3
weight packages or its A8 calibration/prefix into this model.
